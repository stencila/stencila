[package]
edition = "2024"
name = "stencila-node"
version = "2.7.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
eyre = { workspace = true }
napi = { version = "3.3.0", default-features = false, features = [
    "async",
    "napi8",
] }
napi-derive = "3.2.5"
serde_json = { workspace = true }
stencila-codecs = { path = "../rust/codecs" }

[build-dependencies]
napi-build = "2.2.3"
