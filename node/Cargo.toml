[package]
edition = "2021"
name = "node"
version = "2.0.0-alpha.23"

[lib]
crate-type = ["cdylib"]

[dependencies]
codecs = { path = "../rust/codecs" }
common = { path = "../rust/common" }
napi = { version = "2.14.4", default-features = false, features = ["async", "napi8"] }
napi-derive = "2.14.6"
schema = { path = "../rust/schema" }

[build-dependencies]
napi-build = "2.1.0"
