rule:
  any:
    - pattern: const $TARGET = $READER.readFileSync($PATH)
    - pattern: let $TARGET = $READER.readFileSync($PATH)
    - pattern: var $TARGET = $READER.readFileSync($PATH)
    - pattern: $TARGET = $READER.readFileSync($PATH)
    - pattern: $READER.readFileSync($PATH)
    - pattern: const $TARGET = readFileSync($PATH)
    - pattern: let $TARGET = readFileSync($PATH)
    - pattern: var $TARGET = readFileSync($PATH)
    - pattern: $TARGET = readFileSync($PATH)
    - pattern: readFileSync($PATH)
    - pattern: const $TARGET = $READER.readFile($PATH, $$$ARGS)
    - pattern: let $TARGET = $READER.readFile($PATH, $$$ARGS)
    - pattern: var $TARGET = $READER.readFile($PATH, $$$ARGS)
    - pattern: $TARGET = $READER.readFile($PATH, $$$ARGS)
    - pattern: $READER.readFile($PATH, $$$ARGS)
    - pattern: const $TARGET = readFile($PATH, $$$ARGS)
    - pattern: let $TARGET = readFile($PATH, $$$ARGS)
    - pattern: var $TARGET = readFile($PATH, $$$ARGS)
    - pattern: $TARGET = readFile($PATH, $$$ARGS)
    - pattern: readFile($PATH, $$$ARGS)
    - pattern: const $TARGET = $READER.createReadStream($PATH)
    - pattern: let $TARGET = $READER.createReadStream($PATH)
    - pattern: var $TARGET = $READER.createReadStream($PATH)
    - pattern: $TARGET = $READER.createReadStream($PATH)
    - pattern: $READER.createReadStream($PATH)
    - pattern: const $TARGET = fetch($PATH)
    - pattern: let $TARGET = fetch($PATH)
    - pattern: var $TARGET = fetch($PATH)
    - pattern: $TARGET = fetch($PATH)
    - pattern: fetch($PATH)
