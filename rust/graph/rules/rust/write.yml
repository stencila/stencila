id: rust-write
language: Rust
message: Rust file write
severity: info
rule:
  any:
    - pattern: std::fs::write($PATH, $$$ARGS)
    - pattern: fs::write($PATH, $$$ARGS)
    - pattern: File::create($PATH)
    - pattern: Writer::from_path($PATH)
