[package]
name = "parser"
version = "0.0.0"
edition = "2024"

[dependencies]
format = { path = "../format" }
regex = { workspace = true }
schema = { path = "../schema" }
seahash = { workspace = true }

[lints]
workspace = true
