[package]
name = "model"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
format = { path = "../format" }
schema = { path = "../schema" }
secrets = { path = "../secrets" }
serde = { workspace = true }
strum = { workspace = true }

[lints]
workspace = true
