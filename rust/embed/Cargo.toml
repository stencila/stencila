[package]
name = "embed"
version = "0.1.0"
edition = "2024"

[dependencies]
dirs = { path = "../dirs" }
eyre = { workspace = true }
# Ensure that rustls is used to avoid adding a dependence on OpenSSL
fastembed = { version = "5.1.0", default-features = false, features = [
    "hf-hub-rustls-tls",
    "ort-download-binaries",
] }

[lints]
workspace = true
