[package]
name = "plugins"
version = "0.0.0"
edition = "2021"

[dependencies]
app = { path = "../app" }
cli-utils = { path = "../cli-utils" }
codec = { path = "../codec" }
common = { path = "../common" }
common-dev = { path = "../common-dev" }
kernel = { path = "../kernel" }
model = { path = "../model" }
semver = { version = "1.0.22" }
