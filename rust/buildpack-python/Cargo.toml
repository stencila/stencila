[package]
name = "buildpack-python"
description = "A Cloud Native Buildpack for Python"
version = "0.0.0"
edition = "2021"

[dependencies]
binary-poetry = { path = "../binary-poetry" }
binary-python = { path = "../binary-python" }
buildpack = { path = "../buildpack" }
buildpack-apt = { path = "../buildpack-apt" }
sys-info = "0.9.1"
