[package]
name = "node-map"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
derive_more = { workspace = true }
schema = { path = "../schema" }

[lints]
workspace = true
