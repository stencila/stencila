[package]
name = "node-structuring"
version = "0.0.0"
edition = "2024"

[dependencies]
codec-biblio = { path = "../codec-biblio" }
codec-links = { path = "../codec-links" }
codec-text-trait = { path = "../codec-text-trait" }
common = { path = "../common" }
schema = { path = "../schema" }
strum = { workspace = true }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
