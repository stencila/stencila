[package]
name = "test-props"
description = "Utility functions and strategies for property testing"
version = "0.0.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
proptest = "1.0.0"
stencila-schema = { path = "../schema" }
