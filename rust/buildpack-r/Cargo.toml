[package]
name = "buildpack-r"
description = "A Cloud Native Buildpack for R"
version = "0.0.0"
edition = "2021"

[dependencies]
binary-r = { path = "../binary-r" }
buildpack = { path = "../buildpack" }
buildpack-apt = { path = "../buildpack-apt" }
sys-info = "0.9.1"
