[package]
name = "kernel-r"
description = "A microkernel for R"
version = "0.0.0"
edition = "2021"

[dependencies]
binaries = { path = "../binaries", features = ["binary-r"] }
kernel = { path = "../kernel" }
kernel-micro = { path = "../kernel-micro" }

[dev-dependencies]
test-utils = { path = "../test-utils" }
