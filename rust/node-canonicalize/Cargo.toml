[package]
name = "node-canonicalize"
version = "0.0.0"
edition = "2021"

[dependencies]
base64 = { workspace = true }
codec-cbor = { path = "../codec-cbor" }
codec-openalex = { path = "../codec-openalex" }
codec-text = { path = "../codec-text" }
common = { path = "../common" }
human_name = "2.0.3"
schema = { path = "../schema" }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
