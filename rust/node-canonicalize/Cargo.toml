[package]
name = "node-canonicalize"
version = "0.0.0"
edition = "2021"

[dependencies]
base64 = { workspace = true }
codec-cbor = { path = "../codec-cbor" }
codec-text = { path = "../codec-text" }
common = { path = "../common" }
schema = { path = "../schema" }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
