[package]
name = "stencila-codec-polars"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/stencila/stencila"

[dependencies]
polars = { version = "0.51.0", features = ["csv", "json", "parquet", "ipc", "lazy", "dtype-date", "dtype-datetime"] }
stencila-codec = { path = "../codec" }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
