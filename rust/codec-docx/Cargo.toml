[package]
name = "codec-docx"
version = "0.0.0"
edition = "2021"

[dependencies]
codec = { path = "../codec" }
codec-json = { path = "../codec-json" }
codec-pandoc = { path = "../codec-pandoc" }
codec-utils = { path = "../codec-utils" }
dirs = { path = "../dirs" }
node-reconstitute = { path = "../node-reconstitute" }
roxmltree = "0.20.0"
rust-embed = { version = "8.7.0", features = ["interpolate-folder-path", "include-exclude"] }
zip = { workspace = true }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
