[package]
name = "codec-markdown"
version = "0.0.0"
edition = "2021"

[dependencies]
codec = { path = "../codec" }
codec-json5-trait = { path = "../codec-json5-trait" }
codec-markdown-trait = { path = "../codec-markdown-trait" }
codec-text-trait = { path = "../codec-text-trait" }
codec-utils = { path = "../codec-utils" }
markdown = "1.0.0"
media-write = { path = "../media-write" }
winnow = "0.7.8"

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
