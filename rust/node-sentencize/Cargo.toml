[package]
name = "stencila-node-sentencize"
version = "0.0.0"
edition = "2024"

[dependencies]
stencila-schema = { path = "../schema" }

[dev-dependencies]
pretty_assertions = { workspace = true }

[lints]
workspace = true
