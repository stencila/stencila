[package]
name = "node-sentencize"
version = "0.0.0"
edition = "2021"

[dependencies]
schema = { path = "../schema" }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
