[package]
name = "stencila-linter-styler"
version = "0.0.0"
edition = "2024"

[dependencies]
stencila-linter = { path = "../linter" }
stencila-tools = { path = "../tools" }
tempfile = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
