[package]
name = "codec-links"
version = "0.0.0"
edition = "2024"

[dependencies]
codec = { path = "../codec" }
winnow = "0.7.12"

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
