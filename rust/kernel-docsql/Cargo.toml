[package]
name = "kernel-docsql"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel-docsdb = { path = "../kernel-docsdb" }
kernel-jinja = { path = "../kernel-jinja" }

[lints]
workspace = true
