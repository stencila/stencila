github.code("function", ...part_of(.stars > 1000))
---
GET https://api.github.com/search/code?q=function+repo:stencila/stencila


github.code("function", ...part_of(.language = "rust", .stars > 100))
---
GET https://api.github.com/search/code?q=function+repo:stencila/stencila


github.code("function", ...part_of(.topic = "machine-learning", .forks > 50))
---
GET https://api.github.com/search/code?q=function+repo:stencila/stencila


github.code("test", ...part_of(.created > "2023-01-01"))
---
GET https://api.github.com/search/code?q=test+repo:stencila/stencila


github.code("import", ...part_of(.foo = "bar"))
---
Filter `foo` in subquery `part_of` is not supported for GitHub `code`
