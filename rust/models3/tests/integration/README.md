# Integration Tests

Put env-gated real-provider tests in this directory.

Guidelines:

- Skip unless required API key environment variables are present.
- Keep these tests minimal (smoke/parity checks), not exhaustive.
- Avoid flaky assertions tied to exact wording or latency.
