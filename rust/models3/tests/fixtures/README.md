# Fixtures

Store static test fixtures here:

- Provider request/response JSON payloads
- SSE chunk fixtures (including chunk-boundary edge cases)
- Error body fixtures for classification tests

Use small, focused fixtures named by provider and scenario, e.g.
`openai_response_text_only.json`.
