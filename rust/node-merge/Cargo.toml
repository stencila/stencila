[package]
name = "node-merge"
version = "0.0.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
schema = { path = "../schema" }

[dev-dependencies]
codecs = { path = "../codecs" }
common-dev = { path = "../common-dev" }
