[package]
name = "stencila-node-stabilize"
version = "0.0.0"
edition = "2024"
license.workspace = true
repository.workspace = true
description = "Stabilize node UIDs for deterministic site rendering"

[dependencies]
stencila-codec-text = { path = "../codec-text" }
stencila-node-id = { path = "../node-id" }
stencila-schema = { path = "../schema" }

[lints]
workspace = true
