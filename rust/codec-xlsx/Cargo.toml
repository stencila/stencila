[package]
name = "codec-xlsx"
version = "0.0.0"
edition = "2024"

[dependencies]
codec = { path = "../codec" }
calamine = "0.30"

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true