[package]
name = "codec-csl"
description = "A codec for Citation Style Language (CSL) JSON"
version = "0.0.0"
edition = "2021"

[dependencies]
codec = { path = "../codec" }

[dev-dependencies]
test-snaps = { path = "../test-snaps" }
test-utils = { path = "../test-utils" }
