[package]
name = "codec-yaml"
version = "0.0.0"
edition = "2024"

[dependencies]
codec = { path = "../codec" }
serde = { workspace = true }
version = { path = "../version" }

[lints]
workspace = true
