[package]
name = "kernel-store"
description = "A kernel that simply stores nodes"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel", version = "0.0.0" }

[dev-dependencies]
tokio = { version = "1.13.0", features = ["rt", "macros"] }
