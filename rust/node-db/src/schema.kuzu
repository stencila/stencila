// Generated file; do not edit. See the Rust `schema-gen` crate.

CREATE NODE TABLE IF NOT EXISTS `Admonition` (
  `admonitionType` STRING,
  `isFolded` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Annotation` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ArrayHint` (
  `length` INT64,
  `itemTypes` STRING[],
  `minimum` STRING,
  `maximum` STRING,
  `nulls` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ArrayValidator` (
  `itemsNullable` BOOLEAN,
  `itemsValidator` STRING,
  `contains` STRING,
  `minItems` INT64,
  `maxItems` INT64,
  `uniqueItems` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Article` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `pagination` STRING,
  `frontmatter` STRING,
  `extra` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `AudioObject` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `bitrate` DOUBLE,
  `contentSize` DOUBLE,
  `contentUrl` STRING,
  `embedUrl` STRING,
  `mediaType` STRING,
  `transcript` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `AuthorRole` (
  `author` STRING,
  `roleName` STRING,
  `format` STRING,
  `lastModified` TIMESTAMP,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `BooleanValidator` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Brand` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `reviews` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Button` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `code` STRING,
  `programmingLanguage` STRING,
  `executionBounds` STRING,
  `executionBounded` STRING,
  `name` STRING,
  `label` STRING,
  `isDisabled` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CallArgument` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `name` STRING,
  `label` STRING,
  `value` STRING,
  `default` STRING,
  `validator` STRING,
  `derivedFrom` STRING,
  `code` STRING,
  `programmingLanguage` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CallBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `source` STRING,
  `mediaType` STRING,
  `select` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Chat` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `isEmbedded` BOOLEAN,
  `targetNodes` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ChatMessage` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `author` STRING,
  `role` STRING,
  `isSelected` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ChatMessageGroup` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Cite` (
  `target` STRING,
  `citationMode` STRING,
  `citationIntent` STRING,
  `pagination` STRING,
  `citationPrefix` STRING,
  `citationSuffix` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CiteGroup` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Claim` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `claimType` STRING,
  `label` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CodeBlock` (
  `code` STRING,
  `programmingLanguage` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CodeChunk` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `code` STRING,
  `programmingLanguage` STRING,
  `executionBounds` STRING,
  `executionBounded` STRING,
  `labelType` STRING,
  `label` STRING,
  `labelAutomatically` BOOLEAN,
  `isEchoed` BOOLEAN,
  `isHidden` BOOLEAN,
  `executionPure` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CodeExpression` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `code` STRING,
  `programmingLanguage` STRING,
  `executionBounds` STRING,
  `executionBounded` STRING,
  `output` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CodeInline` (
  `code` STRING,
  `programmingLanguage` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CodeLocation` (
  `source` STRING,
  `startLine` UINT64,
  `startColumn` UINT64,
  `endLine` UINT64,
  `endColumn` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Collection` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Comment` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `commentAspect` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CompilationDigest` (
  `stateDigest` UINT64,
  `semanticDigest` UINT64,
  `dependenciesDigest` UINT64,
  `dependenciesStale` UINT64,
  `dependenciesFailed` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CompilationMessage` (
  `level` STRING,
  `message` STRING,
  `errorType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ConstantValidator` (
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ContactPoint` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `emails` STRING[],
  `telephoneNumbers` STRING[],
  `availableLanguages` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `CreativeWork` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Datatable` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DatatableColumn` (
  `name` STRING,
  `values` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DatatableColumnHint` (
  `name` STRING,
  `itemType` STRING,
  `minimum` STRING,
  `maximum` STRING,
  `nulls` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DatatableHint` (
  `rows` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Date` (
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DateTime` (
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DateTimeValidator` (
  `minimum` TIMESTAMP,
  `maximum` TIMESTAMP,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DateValidator` (
  `minimum` DATE,
  `maximum` DATE,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DefinedTerm` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `termCode` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Directory` (
  `name` STRING,
  `path` STRING,
  `parts` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Duration` (
  `value` INT64,
  `timeUnit` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `DurationValidator` (
  `timeUnits` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Emphasis` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `EnumValidator` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Enumeration` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ExecutionDependant` (
  `dependantRelation` STRING,
  `dependantNode` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ExecutionDependency` (
  `dependencyRelation` STRING,
  `dependencyNode` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ExecutionMessage` (
  `level` STRING,
  `message` STRING,
  `errorType` STRING,
  `stackTrace` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ExecutionTag` (
  `name` STRING,
  `value` STRING,
  `isGlobal` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Figure` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `label` STRING,
  `labelAutomatically` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `File` (
  `name` STRING,
  `path` STRING,
  `mediaType` STRING,
  `transferEncoding` STRING,
  `size` UINT64,
  `content` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ForBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `code` STRING,
  `programmingLanguage` STRING,
  `executionBounds` STRING,
  `executionBounded` STRING,
  `variable` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Form` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `deriveFrom` STRING,
  `deriveAction` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Function` (
  `name` STRING,
  `returns` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Grant` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `sponsors` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Heading` (
  `level` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `IfBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `IfBlockClause` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `code` STRING,
  `programmingLanguage` STRING,
  `executionBounds` STRING,
  `executionBounded` STRING,
  `isActive` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ImageObject` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `bitrate` DOUBLE,
  `contentSize` DOUBLE,
  `contentUrl` STRING,
  `embedUrl` STRING,
  `mediaType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `IncludeBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `source` STRING,
  `mediaType` STRING,
  `select` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `InstructionBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `instructionType` STRING,
  `activeSuggestion` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `InstructionInline` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `instructionType` STRING,
  `activeSuggestion` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `InstructionMessage` (
  `role` STRING,
  `parts` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `IntegerValidator` (
  `minimum` DOUBLE,
  `exclusiveMinimum` DOUBLE,
  `maximum` DOUBLE,
  `exclusiveMaximum` DOUBLE,
  `multipleOf` DOUBLE,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Link` (
  `target` STRING,
  `title` STRING,
  `rel` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `List` (
  `order` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ListItem` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `item` STRING,
  `isChecked` BOOLEAN,
  `position` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `MathBlock` (
  `code` STRING,
  `mathLanguage` STRING,
  `mathml` STRING,
  `label` STRING,
  `labelAutomatically` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `MathInline` (
  `code` STRING,
  `mathLanguage` STRING,
  `mathml` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `MediaObject` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `bitrate` DOUBLE,
  `contentSize` DOUBLE,
  `contentUrl` STRING,
  `embedUrl` STRING,
  `mediaType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ModelParameters` (
  `modelIds` STRING[],
  `replicates` UINT64,
  `qualityWeight` UINT64,
  `costWeight` UINT64,
  `speedWeight` UINT64,
  `minimumScore` UINT64,
  `temperature` UINT64,
  `randomSeed` INT64,
  `executeContent` BOOLEAN,
  `executionBounds` STRING,
  `maximumRetries` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `MonetaryGrant` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `sponsors` STRING,
  `amounts` DOUBLE,
  `funders` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Note` (
  `noteType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `NumberValidator` (
  `minimum` DOUBLE,
  `exclusiveMinimum` DOUBLE,
  `maximum` DOUBLE,
  `exclusiveMaximum` DOUBLE,
  `multipleOf` DOUBLE,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ObjectHint` (
  `length` INT64,
  `keys` STRING[],
  `values` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Organization` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `funders` STRING,
  `legalName` STRING,
  `members` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Paragraph` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Parameter` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `name` STRING,
  `label` STRING,
  `value` STRING,
  `default` STRING,
  `validator` STRING,
  `derivedFrom` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Periodical` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `dateStart` DATE,
  `dateEnd` DATE,
  `issns` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Person` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `emails` STRING[],
  `familyNames` STRING[],
  `funders` STRING,
  `givenNames` STRING[],
  `honorificPrefix` STRING,
  `honorificSuffix` STRING,
  `jobTitle` STRING,
  `telephoneNumbers` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `PostalAddress` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `emails` STRING[],
  `telephoneNumbers` STRING[],
  `availableLanguages` STRING[],
  `streetAddress` STRING,
  `postOfficeBoxNumber` STRING,
  `addressLocality` STRING,
  `addressRegion` STRING,
  `postalCode` STRING,
  `addressCountry` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Product` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `productID` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Prompt` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `frontmatter` STRING,
  `instructionTypes` STRING,
  `nodeTypes` STRING[],
  `queryPatterns` STRING[],
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `PromptBlock` (
  `executionMode` STRING,
  `executionCount` INT64,
  `executionRequired` STRING,
  `executionStatus` STRING,
  `executionInstance` STRING,
  `executionEnded` TIMESTAMP,
  `instructionType` STRING,
  `nodeTypes` STRING[],
  `relativePosition` STRING,
  `query` STRING,
  `target` STRING,
  `directory` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `PropertyValue` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `propertyID` STRING,
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ProvenanceCount` (
  `provenanceCategory` STRING,
  `characterCount` UINT64,
  `characterPercent` UINT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `PublicationIssue` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `pagination` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `PublicationVolume` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `pagination` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `QuoteBlock` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `QuoteInline` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `RawBlock` (
  `format` STRING,
  `content` STRING,
  `css` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Review` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `itemReviewed` STRING,
  `reviewAspect` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Section` (
  `sectionType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `SoftwareApplication` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `softwareVersion` STRING,
  `operatingSystem` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `SoftwareSourceCode` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `programmingLanguage` STRING,
  `codeRepository` STRING,
  `codeSampleType` STRING,
  `runtimePlatform` STRING[],
  `softwareRequirements` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Strikeout` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `StringHint` (
  `chars` INT64,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `StringValidator` (
  `minLength` INT64,
  `maxLength` INT64,
  `pattern` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Strong` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `StyledBlock` (
  `code` STRING,
  `styleLanguage` STRING,
  `css` STRING,
  `classList` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `StyledInline` (
  `code` STRING,
  `styleLanguage` STRING,
  `css` STRING,
  `classList` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Subscript` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `SuggestionBlock` (
  `suggestionStatus` STRING,
  `executionEnded` TIMESTAMP,
  `feedback` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `SuggestionInline` (
  `suggestionStatus` STRING,
  `executionEnded` TIMESTAMP,
  `feedback` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Superscript` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Table` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `label` STRING,
  `labelAutomatically` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `TableCell` (
  `cellType` STRING,
  `name` STRING,
  `columnSpan` INT64,
  `rowSpan` INT64,
  `horizontalAlignment` STRING,
  `horizontalAlignmentCharacter` STRING,
  `verticalAlignment` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `TableRow` (
  `rowType` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Text` (
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `ThematicBreak` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Thing` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Time` (
  `value` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `TimeValidator` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Timestamp` (
  `value` INT64,
  `timeUnit` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `TimestampValidator` (
  `timeUnits` STRING,
  `minimum` TIMESTAMP,
  `maximum` TIMESTAMP,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `TupleValidator` (
  `items` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Underline` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Unknown` (
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Variable` (
  `name` STRING,
  `programmingLanguage` STRING,
  `nativeType` STRING,
  `nodeType` STRING,
  `value` STRING,
  `hint` STRING,
  `nativeHint` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `VideoObject` (
  `alternateNames` STRING[],
  `description` STRING,
  `identifiers` STRING,
  `name` STRING,
  `url` STRING,
  `maintainers` STRING,
  `dateCreated` DATE,
  `dateReceived` DATE,
  `dateAccepted` DATE,
  `dateModified` DATE,
  `datePublished` DATE,
  `funders` STRING,
  `fundedBy` STRING,
  `genre` STRING[],
  `keywords` STRING[],
  `isPartOf` STRING,
  `licenses` STRING,
  `parts` STRING,
  `references` STRING,
  `bitrate` DOUBLE,
  `contentSize` DOUBLE,
  `contentUrl` STRING,
  `embedUrl` STRING,
  `mediaType` STRING,
  `transcript` STRING,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `Walkthrough` (
  `isCollapsed` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE NODE TABLE IF NOT EXISTS `WalkthroughStep` (
  `isCollapsed` BOOLEAN,
  `nodeId` STRING PRIMARY KEY
);

CREATE REL TABLE IF NOT EXISTS `codeLocation` (
  FROM `CompilationMessage` TO `CodeLocation`,
  FROM `ExecutionDependant` TO `CodeLocation`,
  FROM `ExecutionDependency` TO `CodeLocation`,
  FROM `ExecutionMessage` TO `CodeLocation`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `compilationDigest` (
  FROM `Article` TO `CompilationDigest`,
  FROM `Button` TO `CompilationDigest`,
  FROM `CallArgument` TO `CompilationDigest`,
  FROM `CallBlock` TO `CompilationDigest`,
  FROM `Chat` TO `CompilationDigest`,
  FROM `ChatMessage` TO `CompilationDigest`,
  FROM `CodeChunk` TO `CompilationDigest`,
  FROM `CodeExpression` TO `CompilationDigest`,
  FROM `ForBlock` TO `CompilationDigest`,
  FROM `Form` TO `CompilationDigest`,
  FROM `IfBlock` TO `CompilationDigest`,
  FROM `IfBlockClause` TO `CompilationDigest`,
  FROM `IncludeBlock` TO `CompilationDigest`,
  FROM `InstructionBlock` TO `CompilationDigest`,
  FROM `InstructionInline` TO `CompilationDigest`,
  FROM `MathBlock` TO `CompilationDigest`,
  FROM `MathInline` TO `CompilationDigest`,
  FROM `Parameter` TO `CompilationDigest`,
  FROM `Prompt` TO `CompilationDigest`,
  FROM `PromptBlock` TO `CompilationDigest`,
  FROM `RawBlock` TO `CompilationDigest`,
  FROM `StyledBlock` TO `CompilationDigest`,
  FROM `StyledInline` TO `CompilationDigest`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `executionDigest` (
  FROM `Article` TO `CompilationDigest`,
  FROM `Button` TO `CompilationDigest`,
  FROM `CallArgument` TO `CompilationDigest`,
  FROM `CallBlock` TO `CompilationDigest`,
  FROM `Chat` TO `CompilationDigest`,
  FROM `ChatMessage` TO `CompilationDigest`,
  FROM `CodeChunk` TO `CompilationDigest`,
  FROM `CodeExpression` TO `CompilationDigest`,
  FROM `ForBlock` TO `CompilationDigest`,
  FROM `Form` TO `CompilationDigest`,
  FROM `IfBlock` TO `CompilationDigest`,
  FROM `IfBlockClause` TO `CompilationDigest`,
  FROM `IncludeBlock` TO `CompilationDigest`,
  FROM `InstructionBlock` TO `CompilationDigest`,
  FROM `InstructionInline` TO `CompilationDigest`,
  FROM `Parameter` TO `CompilationDigest`,
  FROM `Prompt` TO `CompilationDigest`,
  FROM `PromptBlock` TO `CompilationDigest`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `executionDuration` (
  FROM `Article` TO `Duration`,
  FROM `Button` TO `Duration`,
  FROM `CallArgument` TO `Duration`,
  FROM `CallBlock` TO `Duration`,
  FROM `Chat` TO `Duration`,
  FROM `ChatMessage` TO `Duration`,
  FROM `CodeChunk` TO `Duration`,
  FROM `CodeExpression` TO `Duration`,
  FROM `ForBlock` TO `Duration`,
  FROM `Form` TO `Duration`,
  FROM `IfBlock` TO `Duration`,
  FROM `IfBlockClause` TO `Duration`,
  FROM `IncludeBlock` TO `Duration`,
  FROM `InstructionBlock` TO `Duration`,
  FROM `InstructionInline` TO `Duration`,
  FROM `Parameter` TO `Duration`,
  FROM `Prompt` TO `Duration`,
  FROM `PromptBlock` TO `Duration`,
  FROM `SuggestionBlock` TO `Duration`,
  FROM `SuggestionInline` TO `Duration`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `headings` (
  FROM `Article` TO `List`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `logo` (
  FROM `Brand` TO `ImageObject`,
  FROM `Organization` TO `ImageObject`,
  FROM `Product` TO `ImageObject`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `maximum` (
  FROM `DurationValidator` TO `Duration`,
  FROM `TimeValidator` TO `Time`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `message` (
  FROM `InstructionBlock` TO `InstructionMessage`,
  FROM `InstructionInline` TO `InstructionMessage`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `minimum` (
  FROM `DurationValidator` TO `Duration`,
  FROM `TimeValidator` TO `Time`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `modelParameters` (
  FROM `Chat` TO `ModelParameters`,
  FROM `InstructionBlock` TO `ModelParameters`,
  FROM `InstructionInline` TO `ModelParameters`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `parentItem` (
  FROM `Comment` TO `Comment`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `parentOrganization` (
  FROM `Organization` TO `Organization`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `prompt` (
  FROM `Chat` TO `PromptBlock`,
  FROM `InstructionBlock` TO `PromptBlock`,
  FROM `InstructionInline` TO `PromptBlock`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `text` (
  FROM `Article` TO `Text`,
  FROM `AudioObject` TO `Text`,
  FROM `Chat` TO `Text`,
  FROM `Claim` TO `Text`,
  FROM `Collection` TO `Text`,
  FROM `Comment` TO `Text`,
  FROM `CreativeWork` TO `Text`,
  FROM `Datatable` TO `Text`,
  FROM `Figure` TO `Text`,
  FROM `ImageObject` TO `Text`,
  FROM `MediaObject` TO `Text`,
  FROM `Periodical` TO `Text`,
  FROM `Prompt` TO `Text`,
  FROM `PublicationIssue` TO `Text`,
  FROM `PublicationVolume` TO `Text`,
  FROM `Review` TO `Text`,
  FROM `SoftwareApplication` TO `Text`,
  FROM `SoftwareSourceCode` TO `Text`,
  FROM `Table` TO `Text`,
  FROM `VideoObject` TO `Text`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `thumbnail` (
  FROM `ImageObject` TO `ImageObject`,
  FROM `VideoObject` TO `ImageObject`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `validator` (
  FROM `DatatableColumn` TO `ArrayValidator`,
  ONE_ONE
);

CREATE REL TABLE IF NOT EXISTS `abstract` (
  FROM `Article` TO `Admonition`,
  FROM `Article` TO `AudioObject`,
  FROM `Article` TO `CallBlock`,
  FROM `Article` TO `Chat`,
  FROM `Article` TO `ChatMessage`,
  FROM `Article` TO `ChatMessageGroup`,
  FROM `Article` TO `Claim`,
  FROM `Article` TO `CodeBlock`,
  FROM `Article` TO `CodeChunk`,
  FROM `Article` TO `Figure`,
  FROM `Article` TO `File`,
  FROM `Article` TO `ForBlock`,
  FROM `Article` TO `Form`,
  FROM `Article` TO `Heading`,
  FROM `Article` TO `IfBlock`,
  FROM `Article` TO `ImageObject`,
  FROM `Article` TO `IncludeBlock`,
  FROM `Article` TO `InstructionBlock`,
  FROM `Article` TO `List`,
  FROM `Article` TO `MathBlock`,
  FROM `Article` TO `Paragraph`,
  FROM `Article` TO `PromptBlock`,
  FROM `Article` TO `QuoteBlock`,
  FROM `Article` TO `RawBlock`,
  FROM `Article` TO `Section`,
  FROM `Article` TO `StyledBlock`,
  FROM `Article` TO `SuggestionBlock`,
  FROM `Article` TO `Table`,
  FROM `Article` TO `ThematicBreak`,
  FROM `Article` TO `VideoObject`,
  FROM `Article` TO `Walkthrough`,
  FROM `AudioObject` TO `Admonition`,
  FROM `AudioObject` TO `AudioObject`,
  FROM `AudioObject` TO `CallBlock`,
  FROM `AudioObject` TO `Chat`,
  FROM `AudioObject` TO `ChatMessage`,
  FROM `AudioObject` TO `ChatMessageGroup`,
  FROM `AudioObject` TO `Claim`,
  FROM `AudioObject` TO `CodeBlock`,
  FROM `AudioObject` TO `CodeChunk`,
  FROM `AudioObject` TO `Figure`,
  FROM `AudioObject` TO `File`,
  FROM `AudioObject` TO `ForBlock`,
  FROM `AudioObject` TO `Form`,
  FROM `AudioObject` TO `Heading`,
  FROM `AudioObject` TO `IfBlock`,
  FROM `AudioObject` TO `ImageObject`,
  FROM `AudioObject` TO `IncludeBlock`,
  FROM `AudioObject` TO `InstructionBlock`,
  FROM `AudioObject` TO `List`,
  FROM `AudioObject` TO `MathBlock`,
  FROM `AudioObject` TO `Paragraph`,
  FROM `AudioObject` TO `PromptBlock`,
  FROM `AudioObject` TO `QuoteBlock`,
  FROM `AudioObject` TO `RawBlock`,
  FROM `AudioObject` TO `Section`,
  FROM `AudioObject` TO `StyledBlock`,
  FROM `AudioObject` TO `SuggestionBlock`,
  FROM `AudioObject` TO `Table`,
  FROM `AudioObject` TO `ThematicBreak`,
  FROM `AudioObject` TO `VideoObject`,
  FROM `AudioObject` TO `Walkthrough`,
  FROM `Chat` TO `Admonition`,
  FROM `Chat` TO `AudioObject`,
  FROM `Chat` TO `CallBlock`,
  FROM `Chat` TO `Chat`,
  FROM `Chat` TO `ChatMessage`,
  FROM `Chat` TO `ChatMessageGroup`,
  FROM `Chat` TO `Claim`,
  FROM `Chat` TO `CodeBlock`,
  FROM `Chat` TO `CodeChunk`,
  FROM `Chat` TO `Figure`,
  FROM `Chat` TO `File`,
  FROM `Chat` TO `ForBlock`,
  FROM `Chat` TO `Form`,
  FROM `Chat` TO `Heading`,
  FROM `Chat` TO `IfBlock`,
  FROM `Chat` TO `ImageObject`,
  FROM `Chat` TO `IncludeBlock`,
  FROM `Chat` TO `InstructionBlock`,
  FROM `Chat` TO `List`,
  FROM `Chat` TO `MathBlock`,
  FROM `Chat` TO `Paragraph`,
  FROM `Chat` TO `PromptBlock`,
  FROM `Chat` TO `QuoteBlock`,
  FROM `Chat` TO `RawBlock`,
  FROM `Chat` TO `Section`,
  FROM `Chat` TO `StyledBlock`,
  FROM `Chat` TO `SuggestionBlock`,
  FROM `Chat` TO `Table`,
  FROM `Chat` TO `ThematicBreak`,
  FROM `Chat` TO `VideoObject`,
  FROM `Chat` TO `Walkthrough`,
  FROM `Claim` TO `Admonition`,
  FROM `Claim` TO `AudioObject`,
  FROM `Claim` TO `CallBlock`,
  FROM `Claim` TO `Chat`,
  FROM `Claim` TO `ChatMessage`,
  FROM `Claim` TO `ChatMessageGroup`,
  FROM `Claim` TO `Claim`,
  FROM `Claim` TO `CodeBlock`,
  FROM `Claim` TO `CodeChunk`,
  FROM `Claim` TO `Figure`,
  FROM `Claim` TO `File`,
  FROM `Claim` TO `ForBlock`,
  FROM `Claim` TO `Form`,
  FROM `Claim` TO `Heading`,
  FROM `Claim` TO `IfBlock`,
  FROM `Claim` TO `ImageObject`,
  FROM `Claim` TO `IncludeBlock`,
  FROM `Claim` TO `InstructionBlock`,
  FROM `Claim` TO `List`,
  FROM `Claim` TO `MathBlock`,
  FROM `Claim` TO `Paragraph`,
  FROM `Claim` TO `PromptBlock`,
  FROM `Claim` TO `QuoteBlock`,
  FROM `Claim` TO `RawBlock`,
  FROM `Claim` TO `Section`,
  FROM `Claim` TO `StyledBlock`,
  FROM `Claim` TO `SuggestionBlock`,
  FROM `Claim` TO `Table`,
  FROM `Claim` TO `ThematicBreak`,
  FROM `Claim` TO `VideoObject`,
  FROM `Claim` TO `Walkthrough`,
  FROM `Collection` TO `Admonition`,
  FROM `Collection` TO `AudioObject`,
  FROM `Collection` TO `CallBlock`,
  FROM `Collection` TO `Chat`,
  FROM `Collection` TO `ChatMessage`,
  FROM `Collection` TO `ChatMessageGroup`,
  FROM `Collection` TO `Claim`,
  FROM `Collection` TO `CodeBlock`,
  FROM `Collection` TO `CodeChunk`,
  FROM `Collection` TO `Figure`,
  FROM `Collection` TO `File`,
  FROM `Collection` TO `ForBlock`,
  FROM `Collection` TO `Form`,
  FROM `Collection` TO `Heading`,
  FROM `Collection` TO `IfBlock`,
  FROM `Collection` TO `ImageObject`,
  FROM `Collection` TO `IncludeBlock`,
  FROM `Collection` TO `InstructionBlock`,
  FROM `Collection` TO `List`,
  FROM `Collection` TO `MathBlock`,
  FROM `Collection` TO `Paragraph`,
  FROM `Collection` TO `PromptBlock`,
  FROM `Collection` TO `QuoteBlock`,
  FROM `Collection` TO `RawBlock`,
  FROM `Collection` TO `Section`,
  FROM `Collection` TO `StyledBlock`,
  FROM `Collection` TO `SuggestionBlock`,
  FROM `Collection` TO `Table`,
  FROM `Collection` TO `ThematicBreak`,
  FROM `Collection` TO `VideoObject`,
  FROM `Collection` TO `Walkthrough`,
  FROM `Comment` TO `Admonition`,
  FROM `Comment` TO `AudioObject`,
  FROM `Comment` TO `CallBlock`,
  FROM `Comment` TO `Chat`,
  FROM `Comment` TO `ChatMessage`,
  FROM `Comment` TO `ChatMessageGroup`,
  FROM `Comment` TO `Claim`,
  FROM `Comment` TO `CodeBlock`,
  FROM `Comment` TO `CodeChunk`,
  FROM `Comment` TO `Figure`,
  FROM `Comment` TO `File`,
  FROM `Comment` TO `ForBlock`,
  FROM `Comment` TO `Form`,
  FROM `Comment` TO `Heading`,
  FROM `Comment` TO `IfBlock`,
  FROM `Comment` TO `ImageObject`,
  FROM `Comment` TO `IncludeBlock`,
  FROM `Comment` TO `InstructionBlock`,
  FROM `Comment` TO `List`,
  FROM `Comment` TO `MathBlock`,
  FROM `Comment` TO `Paragraph`,
  FROM `Comment` TO `PromptBlock`,
  FROM `Comment` TO `QuoteBlock`,
  FROM `Comment` TO `RawBlock`,
  FROM `Comment` TO `Section`,
  FROM `Comment` TO `StyledBlock`,
  FROM `Comment` TO `SuggestionBlock`,
  FROM `Comment` TO `Table`,
  FROM `Comment` TO `ThematicBreak`,
  FROM `Comment` TO `VideoObject`,
  FROM `Comment` TO `Walkthrough`,
  FROM `CreativeWork` TO `Admonition`,
  FROM `CreativeWork` TO `AudioObject`,
  FROM `CreativeWork` TO `CallBlock`,
  FROM `CreativeWork` TO `Chat`,
  FROM `CreativeWork` TO `ChatMessage`,
  FROM `CreativeWork` TO `ChatMessageGroup`,
  FROM `CreativeWork` TO `Claim`,
  FROM `CreativeWork` TO `CodeBlock`,
  FROM `CreativeWork` TO `CodeChunk`,
  FROM `CreativeWork` TO `Figure`,
  FROM `CreativeWork` TO `File`,
  FROM `CreativeWork` TO `ForBlock`,
  FROM `CreativeWork` TO `Form`,
  FROM `CreativeWork` TO `Heading`,
  FROM `CreativeWork` TO `IfBlock`,
  FROM `CreativeWork` TO `ImageObject`,
  FROM `CreativeWork` TO `IncludeBlock`,
  FROM `CreativeWork` TO `InstructionBlock`,
  FROM `CreativeWork` TO `List`,
  FROM `CreativeWork` TO `MathBlock`,
  FROM `CreativeWork` TO `Paragraph`,
  FROM `CreativeWork` TO `PromptBlock`,
  FROM `CreativeWork` TO `QuoteBlock`,
  FROM `CreativeWork` TO `RawBlock`,
  FROM `CreativeWork` TO `Section`,
  FROM `CreativeWork` TO `StyledBlock`,
  FROM `CreativeWork` TO `SuggestionBlock`,
  FROM `CreativeWork` TO `Table`,
  FROM `CreativeWork` TO `ThematicBreak`,
  FROM `CreativeWork` TO `VideoObject`,
  FROM `CreativeWork` TO `Walkthrough`,
  FROM `Datatable` TO `Admonition`,
  FROM `Datatable` TO `AudioObject`,
  FROM `Datatable` TO `CallBlock`,
  FROM `Datatable` TO `Chat`,
  FROM `Datatable` TO `ChatMessage`,
  FROM `Datatable` TO `ChatMessageGroup`,
  FROM `Datatable` TO `Claim`,
  FROM `Datatable` TO `CodeBlock`,
  FROM `Datatable` TO `CodeChunk`,
  FROM `Datatable` TO `Figure`,
  FROM `Datatable` TO `File`,
  FROM `Datatable` TO `ForBlock`,
  FROM `Datatable` TO `Form`,
  FROM `Datatable` TO `Heading`,
  FROM `Datatable` TO `IfBlock`,
  FROM `Datatable` TO `ImageObject`,
  FROM `Datatable` TO `IncludeBlock`,
  FROM `Datatable` TO `InstructionBlock`,
  FROM `Datatable` TO `List`,
  FROM `Datatable` TO `MathBlock`,
  FROM `Datatable` TO `Paragraph`,
  FROM `Datatable` TO `PromptBlock`,
  FROM `Datatable` TO `QuoteBlock`,
  FROM `Datatable` TO `RawBlock`,
  FROM `Datatable` TO `Section`,
  FROM `Datatable` TO `StyledBlock`,
  FROM `Datatable` TO `SuggestionBlock`,
  FROM `Datatable` TO `Table`,
  FROM `Datatable` TO `ThematicBreak`,
  FROM `Datatable` TO `VideoObject`,
  FROM `Datatable` TO `Walkthrough`,
  FROM `Figure` TO `Admonition`,
  FROM `Figure` TO `AudioObject`,
  FROM `Figure` TO `CallBlock`,
  FROM `Figure` TO `Chat`,
  FROM `Figure` TO `ChatMessage`,
  FROM `Figure` TO `ChatMessageGroup`,
  FROM `Figure` TO `Claim`,
  FROM `Figure` TO `CodeBlock`,
  FROM `Figure` TO `CodeChunk`,
  FROM `Figure` TO `Figure`,
  FROM `Figure` TO `File`,
  FROM `Figure` TO `ForBlock`,
  FROM `Figure` TO `Form`,
  FROM `Figure` TO `Heading`,
  FROM `Figure` TO `IfBlock`,
  FROM `Figure` TO `ImageObject`,
  FROM `Figure` TO `IncludeBlock`,
  FROM `Figure` TO `InstructionBlock`,
  FROM `Figure` TO `List`,
  FROM `Figure` TO `MathBlock`,
  FROM `Figure` TO `Paragraph`,
  FROM `Figure` TO `PromptBlock`,
  FROM `Figure` TO `QuoteBlock`,
  FROM `Figure` TO `RawBlock`,
  FROM `Figure` TO `Section`,
  FROM `Figure` TO `StyledBlock`,
  FROM `Figure` TO `SuggestionBlock`,
  FROM `Figure` TO `Table`,
  FROM `Figure` TO `ThematicBreak`,
  FROM `Figure` TO `VideoObject`,
  FROM `Figure` TO `Walkthrough`,
  FROM `ImageObject` TO `Admonition`,
  FROM `ImageObject` TO `AudioObject`,
  FROM `ImageObject` TO `CallBlock`,
  FROM `ImageObject` TO `Chat`,
  FROM `ImageObject` TO `ChatMessage`,
  FROM `ImageObject` TO `ChatMessageGroup`,
  FROM `ImageObject` TO `Claim`,
  FROM `ImageObject` TO `CodeBlock`,
  FROM `ImageObject` TO `CodeChunk`,
  FROM `ImageObject` TO `Figure`,
  FROM `ImageObject` TO `File`,
  FROM `ImageObject` TO `ForBlock`,
  FROM `ImageObject` TO `Form`,
  FROM `ImageObject` TO `Heading`,
  FROM `ImageObject` TO `IfBlock`,
  FROM `ImageObject` TO `ImageObject`,
  FROM `ImageObject` TO `IncludeBlock`,
  FROM `ImageObject` TO `InstructionBlock`,
  FROM `ImageObject` TO `List`,
  FROM `ImageObject` TO `MathBlock`,
  FROM `ImageObject` TO `Paragraph`,
  FROM `ImageObject` TO `PromptBlock`,
  FROM `ImageObject` TO `QuoteBlock`,
  FROM `ImageObject` TO `RawBlock`,
  FROM `ImageObject` TO `Section`,
  FROM `ImageObject` TO `StyledBlock`,
  FROM `ImageObject` TO `SuggestionBlock`,
  FROM `ImageObject` TO `Table`,
  FROM `ImageObject` TO `ThematicBreak`,
  FROM `ImageObject` TO `VideoObject`,
  FROM `ImageObject` TO `Walkthrough`,
  FROM `MediaObject` TO `Admonition`,
  FROM `MediaObject` TO `AudioObject`,
  FROM `MediaObject` TO `CallBlock`,
  FROM `MediaObject` TO `Chat`,
  FROM `MediaObject` TO `ChatMessage`,
  FROM `MediaObject` TO `ChatMessageGroup`,
  FROM `MediaObject` TO `Claim`,
  FROM `MediaObject` TO `CodeBlock`,
  FROM `MediaObject` TO `CodeChunk`,
  FROM `MediaObject` TO `Figure`,
  FROM `MediaObject` TO `File`,
  FROM `MediaObject` TO `ForBlock`,
  FROM `MediaObject` TO `Form`,
  FROM `MediaObject` TO `Heading`,
  FROM `MediaObject` TO `IfBlock`,
  FROM `MediaObject` TO `ImageObject`,
  FROM `MediaObject` TO `IncludeBlock`,
  FROM `MediaObject` TO `InstructionBlock`,
  FROM `MediaObject` TO `List`,
  FROM `MediaObject` TO `MathBlock`,
  FROM `MediaObject` TO `Paragraph`,
  FROM `MediaObject` TO `PromptBlock`,
  FROM `MediaObject` TO `QuoteBlock`,
  FROM `MediaObject` TO `RawBlock`,
  FROM `MediaObject` TO `Section`,
  FROM `MediaObject` TO `StyledBlock`,
  FROM `MediaObject` TO `SuggestionBlock`,
  FROM `MediaObject` TO `Table`,
  FROM `MediaObject` TO `ThematicBreak`,
  FROM `MediaObject` TO `VideoObject`,
  FROM `MediaObject` TO `Walkthrough`,
  FROM `Periodical` TO `Admonition`,
  FROM `Periodical` TO `AudioObject`,
  FROM `Periodical` TO `CallBlock`,
  FROM `Periodical` TO `Chat`,
  FROM `Periodical` TO `ChatMessage`,
  FROM `Periodical` TO `ChatMessageGroup`,
  FROM `Periodical` TO `Claim`,
  FROM `Periodical` TO `CodeBlock`,
  FROM `Periodical` TO `CodeChunk`,
  FROM `Periodical` TO `Figure`,
  FROM `Periodical` TO `File`,
  FROM `Periodical` TO `ForBlock`,
  FROM `Periodical` TO `Form`,
  FROM `Periodical` TO `Heading`,
  FROM `Periodical` TO `IfBlock`,
  FROM `Periodical` TO `ImageObject`,
  FROM `Periodical` TO `IncludeBlock`,
  FROM `Periodical` TO `InstructionBlock`,
  FROM `Periodical` TO `List`,
  FROM `Periodical` TO `MathBlock`,
  FROM `Periodical` TO `Paragraph`,
  FROM `Periodical` TO `PromptBlock`,
  FROM `Periodical` TO `QuoteBlock`,
  FROM `Periodical` TO `RawBlock`,
  FROM `Periodical` TO `Section`,
  FROM `Periodical` TO `StyledBlock`,
  FROM `Periodical` TO `SuggestionBlock`,
  FROM `Periodical` TO `Table`,
  FROM `Periodical` TO `ThematicBreak`,
  FROM `Periodical` TO `VideoObject`,
  FROM `Periodical` TO `Walkthrough`,
  FROM `Prompt` TO `Admonition`,
  FROM `Prompt` TO `AudioObject`,
  FROM `Prompt` TO `CallBlock`,
  FROM `Prompt` TO `Chat`,
  FROM `Prompt` TO `ChatMessage`,
  FROM `Prompt` TO `ChatMessageGroup`,
  FROM `Prompt` TO `Claim`,
  FROM `Prompt` TO `CodeBlock`,
  FROM `Prompt` TO `CodeChunk`,
  FROM `Prompt` TO `Figure`,
  FROM `Prompt` TO `File`,
  FROM `Prompt` TO `ForBlock`,
  FROM `Prompt` TO `Form`,
  FROM `Prompt` TO `Heading`,
  FROM `Prompt` TO `IfBlock`,
  FROM `Prompt` TO `ImageObject`,
  FROM `Prompt` TO `IncludeBlock`,
  FROM `Prompt` TO `InstructionBlock`,
  FROM `Prompt` TO `List`,
  FROM `Prompt` TO `MathBlock`,
  FROM `Prompt` TO `Paragraph`,
  FROM `Prompt` TO `PromptBlock`,
  FROM `Prompt` TO `QuoteBlock`,
  FROM `Prompt` TO `RawBlock`,
  FROM `Prompt` TO `Section`,
  FROM `Prompt` TO `StyledBlock`,
  FROM `Prompt` TO `SuggestionBlock`,
  FROM `Prompt` TO `Table`,
  FROM `Prompt` TO `ThematicBreak`,
  FROM `Prompt` TO `VideoObject`,
  FROM `Prompt` TO `Walkthrough`,
  FROM `PublicationIssue` TO `Admonition`,
  FROM `PublicationIssue` TO `AudioObject`,
  FROM `PublicationIssue` TO `CallBlock`,
  FROM `PublicationIssue` TO `Chat`,
  FROM `PublicationIssue` TO `ChatMessage`,
  FROM `PublicationIssue` TO `ChatMessageGroup`,
  FROM `PublicationIssue` TO `Claim`,
  FROM `PublicationIssue` TO `CodeBlock`,
  FROM `PublicationIssue` TO `CodeChunk`,
  FROM `PublicationIssue` TO `Figure`,
  FROM `PublicationIssue` TO `File`,
  FROM `PublicationIssue` TO `ForBlock`,
  FROM `PublicationIssue` TO `Form`,
  FROM `PublicationIssue` TO `Heading`,
  FROM `PublicationIssue` TO `IfBlock`,
  FROM `PublicationIssue` TO `ImageObject`,
  FROM `PublicationIssue` TO `IncludeBlock`,
  FROM `PublicationIssue` TO `InstructionBlock`,
  FROM `PublicationIssue` TO `List`,
  FROM `PublicationIssue` TO `MathBlock`,
  FROM `PublicationIssue` TO `Paragraph`,
  FROM `PublicationIssue` TO `PromptBlock`,
  FROM `PublicationIssue` TO `QuoteBlock`,
  FROM `PublicationIssue` TO `RawBlock`,
  FROM `PublicationIssue` TO `Section`,
  FROM `PublicationIssue` TO `StyledBlock`,
  FROM `PublicationIssue` TO `SuggestionBlock`,
  FROM `PublicationIssue` TO `Table`,
  FROM `PublicationIssue` TO `ThematicBreak`,
  FROM `PublicationIssue` TO `VideoObject`,
  FROM `PublicationIssue` TO `Walkthrough`,
  FROM `PublicationVolume` TO `Admonition`,
  FROM `PublicationVolume` TO `AudioObject`,
  FROM `PublicationVolume` TO `CallBlock`,
  FROM `PublicationVolume` TO `Chat`,
  FROM `PublicationVolume` TO `ChatMessage`,
  FROM `PublicationVolume` TO `ChatMessageGroup`,
  FROM `PublicationVolume` TO `Claim`,
  FROM `PublicationVolume` TO `CodeBlock`,
  FROM `PublicationVolume` TO `CodeChunk`,
  FROM `PublicationVolume` TO `Figure`,
  FROM `PublicationVolume` TO `File`,
  FROM `PublicationVolume` TO `ForBlock`,
  FROM `PublicationVolume` TO `Form`,
  FROM `PublicationVolume` TO `Heading`,
  FROM `PublicationVolume` TO `IfBlock`,
  FROM `PublicationVolume` TO `ImageObject`,
  FROM `PublicationVolume` TO `IncludeBlock`,
  FROM `PublicationVolume` TO `InstructionBlock`,
  FROM `PublicationVolume` TO `List`,
  FROM `PublicationVolume` TO `MathBlock`,
  FROM `PublicationVolume` TO `Paragraph`,
  FROM `PublicationVolume` TO `PromptBlock`,
  FROM `PublicationVolume` TO `QuoteBlock`,
  FROM `PublicationVolume` TO `RawBlock`,
  FROM `PublicationVolume` TO `Section`,
  FROM `PublicationVolume` TO `StyledBlock`,
  FROM `PublicationVolume` TO `SuggestionBlock`,
  FROM `PublicationVolume` TO `Table`,
  FROM `PublicationVolume` TO `ThematicBreak`,
  FROM `PublicationVolume` TO `VideoObject`,
  FROM `PublicationVolume` TO `Walkthrough`,
  FROM `Review` TO `Admonition`,
  FROM `Review` TO `AudioObject`,
  FROM `Review` TO `CallBlock`,
  FROM `Review` TO `Chat`,
  FROM `Review` TO `ChatMessage`,
  FROM `Review` TO `ChatMessageGroup`,
  FROM `Review` TO `Claim`,
  FROM `Review` TO `CodeBlock`,
  FROM `Review` TO `CodeChunk`,
  FROM `Review` TO `Figure`,
  FROM `Review` TO `File`,
  FROM `Review` TO `ForBlock`,
  FROM `Review` TO `Form`,
  FROM `Review` TO `Heading`,
  FROM `Review` TO `IfBlock`,
  FROM `Review` TO `ImageObject`,
  FROM `Review` TO `IncludeBlock`,
  FROM `Review` TO `InstructionBlock`,
  FROM `Review` TO `List`,
  FROM `Review` TO `MathBlock`,
  FROM `Review` TO `Paragraph`,
  FROM `Review` TO `PromptBlock`,
  FROM `Review` TO `QuoteBlock`,
  FROM `Review` TO `RawBlock`,
  FROM `Review` TO `Section`,
  FROM `Review` TO `StyledBlock`,
  FROM `Review` TO `SuggestionBlock`,
  FROM `Review` TO `Table`,
  FROM `Review` TO `ThematicBreak`,
  FROM `Review` TO `VideoObject`,
  FROM `Review` TO `Walkthrough`,
  FROM `SoftwareApplication` TO `Admonition`,
  FROM `SoftwareApplication` TO `AudioObject`,
  FROM `SoftwareApplication` TO `CallBlock`,
  FROM `SoftwareApplication` TO `Chat`,
  FROM `SoftwareApplication` TO `ChatMessage`,
  FROM `SoftwareApplication` TO `ChatMessageGroup`,
  FROM `SoftwareApplication` TO `Claim`,
  FROM `SoftwareApplication` TO `CodeBlock`,
  FROM `SoftwareApplication` TO `CodeChunk`,
  FROM `SoftwareApplication` TO `Figure`,
  FROM `SoftwareApplication` TO `File`,
  FROM `SoftwareApplication` TO `ForBlock`,
  FROM `SoftwareApplication` TO `Form`,
  FROM `SoftwareApplication` TO `Heading`,
  FROM `SoftwareApplication` TO `IfBlock`,
  FROM `SoftwareApplication` TO `ImageObject`,
  FROM `SoftwareApplication` TO `IncludeBlock`,
  FROM `SoftwareApplication` TO `InstructionBlock`,
  FROM `SoftwareApplication` TO `List`,
  FROM `SoftwareApplication` TO `MathBlock`,
  FROM `SoftwareApplication` TO `Paragraph`,
  FROM `SoftwareApplication` TO `PromptBlock`,
  FROM `SoftwareApplication` TO `QuoteBlock`,
  FROM `SoftwareApplication` TO `RawBlock`,
  FROM `SoftwareApplication` TO `Section`,
  FROM `SoftwareApplication` TO `StyledBlock`,
  FROM `SoftwareApplication` TO `SuggestionBlock`,
  FROM `SoftwareApplication` TO `Table`,
  FROM `SoftwareApplication` TO `ThematicBreak`,
  FROM `SoftwareApplication` TO `VideoObject`,
  FROM `SoftwareApplication` TO `Walkthrough`,
  FROM `SoftwareSourceCode` TO `Admonition`,
  FROM `SoftwareSourceCode` TO `AudioObject`,
  FROM `SoftwareSourceCode` TO `CallBlock`,
  FROM `SoftwareSourceCode` TO `Chat`,
  FROM `SoftwareSourceCode` TO `ChatMessage`,
  FROM `SoftwareSourceCode` TO `ChatMessageGroup`,
  FROM `SoftwareSourceCode` TO `Claim`,
  FROM `SoftwareSourceCode` TO `CodeBlock`,
  FROM `SoftwareSourceCode` TO `CodeChunk`,
  FROM `SoftwareSourceCode` TO `Figure`,
  FROM `SoftwareSourceCode` TO `File`,
  FROM `SoftwareSourceCode` TO `ForBlock`,
  FROM `SoftwareSourceCode` TO `Form`,
  FROM `SoftwareSourceCode` TO `Heading`,
  FROM `SoftwareSourceCode` TO `IfBlock`,
  FROM `SoftwareSourceCode` TO `ImageObject`,
  FROM `SoftwareSourceCode` TO `IncludeBlock`,
  FROM `SoftwareSourceCode` TO `InstructionBlock`,
  FROM `SoftwareSourceCode` TO `List`,
  FROM `SoftwareSourceCode` TO `MathBlock`,
  FROM `SoftwareSourceCode` TO `Paragraph`,
  FROM `SoftwareSourceCode` TO `PromptBlock`,
  FROM `SoftwareSourceCode` TO `QuoteBlock`,
  FROM `SoftwareSourceCode` TO `RawBlock`,
  FROM `SoftwareSourceCode` TO `Section`,
  FROM `SoftwareSourceCode` TO `StyledBlock`,
  FROM `SoftwareSourceCode` TO `SuggestionBlock`,
  FROM `SoftwareSourceCode` TO `Table`,
  FROM `SoftwareSourceCode` TO `ThematicBreak`,
  FROM `SoftwareSourceCode` TO `VideoObject`,
  FROM `SoftwareSourceCode` TO `Walkthrough`,
  FROM `Table` TO `Admonition`,
  FROM `Table` TO `AudioObject`,
  FROM `Table` TO `CallBlock`,
  FROM `Table` TO `Chat`,
  FROM `Table` TO `ChatMessage`,
  FROM `Table` TO `ChatMessageGroup`,
  FROM `Table` TO `Claim`,
  FROM `Table` TO `CodeBlock`,
  FROM `Table` TO `CodeChunk`,
  FROM `Table` TO `Figure`,
  FROM `Table` TO `File`,
  FROM `Table` TO `ForBlock`,
  FROM `Table` TO `Form`,
  FROM `Table` TO `Heading`,
  FROM `Table` TO `IfBlock`,
  FROM `Table` TO `ImageObject`,
  FROM `Table` TO `IncludeBlock`,
  FROM `Table` TO `InstructionBlock`,
  FROM `Table` TO `List`,
  FROM `Table` TO `MathBlock`,
  FROM `Table` TO `Paragraph`,
  FROM `Table` TO `PromptBlock`,
  FROM `Table` TO `QuoteBlock`,
  FROM `Table` TO `RawBlock`,
  FROM `Table` TO `Section`,
  FROM `Table` TO `StyledBlock`,
  FROM `Table` TO `SuggestionBlock`,
  FROM `Table` TO `Table`,
  FROM `Table` TO `ThematicBreak`,
  FROM `Table` TO `VideoObject`,
  FROM `Table` TO `Walkthrough`,
  FROM `VideoObject` TO `Admonition`,
  FROM `VideoObject` TO `AudioObject`,
  FROM `VideoObject` TO `CallBlock`,
  FROM `VideoObject` TO `Chat`,
  FROM `VideoObject` TO `ChatMessage`,
  FROM `VideoObject` TO `ChatMessageGroup`,
  FROM `VideoObject` TO `Claim`,
  FROM `VideoObject` TO `CodeBlock`,
  FROM `VideoObject` TO `CodeChunk`,
  FROM `VideoObject` TO `Figure`,
  FROM `VideoObject` TO `File`,
  FROM `VideoObject` TO `ForBlock`,
  FROM `VideoObject` TO `Form`,
  FROM `VideoObject` TO `Heading`,
  FROM `VideoObject` TO `IfBlock`,
  FROM `VideoObject` TO `ImageObject`,
  FROM `VideoObject` TO `IncludeBlock`,
  FROM `VideoObject` TO `InstructionBlock`,
  FROM `VideoObject` TO `List`,
  FROM `VideoObject` TO `MathBlock`,
  FROM `VideoObject` TO `Paragraph`,
  FROM `VideoObject` TO `PromptBlock`,
  FROM `VideoObject` TO `QuoteBlock`,
  FROM `VideoObject` TO `RawBlock`,
  FROM `VideoObject` TO `Section`,
  FROM `VideoObject` TO `StyledBlock`,
  FROM `VideoObject` TO `SuggestionBlock`,
  FROM `VideoObject` TO `Table`,
  FROM `VideoObject` TO `ThematicBreak`,
  FROM `VideoObject` TO `VideoObject`,
  FROM `VideoObject` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `affiliations` (
  FROM `Person` TO `Organization`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `annotation` (
  FROM `Annotation` TO `Admonition`,
  FROM `Annotation` TO `AudioObject`,
  FROM `Annotation` TO `CallBlock`,
  FROM `Annotation` TO `Chat`,
  FROM `Annotation` TO `ChatMessage`,
  FROM `Annotation` TO `ChatMessageGroup`,
  FROM `Annotation` TO `Claim`,
  FROM `Annotation` TO `CodeBlock`,
  FROM `Annotation` TO `CodeChunk`,
  FROM `Annotation` TO `Figure`,
  FROM `Annotation` TO `File`,
  FROM `Annotation` TO `ForBlock`,
  FROM `Annotation` TO `Form`,
  FROM `Annotation` TO `Heading`,
  FROM `Annotation` TO `IfBlock`,
  FROM `Annotation` TO `ImageObject`,
  FROM `Annotation` TO `IncludeBlock`,
  FROM `Annotation` TO `InstructionBlock`,
  FROM `Annotation` TO `List`,
  FROM `Annotation` TO `MathBlock`,
  FROM `Annotation` TO `Paragraph`,
  FROM `Annotation` TO `PromptBlock`,
  FROM `Annotation` TO `QuoteBlock`,
  FROM `Annotation` TO `RawBlock`,
  FROM `Annotation` TO `Section`,
  FROM `Annotation` TO `StyledBlock`,
  FROM `Annotation` TO `SuggestionBlock`,
  FROM `Annotation` TO `Table`,
  FROM `Annotation` TO `ThematicBreak`,
  FROM `Annotation` TO `VideoObject`,
  FROM `Annotation` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `arguments` (
  FROM `CallBlock` TO `CallArgument`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `authors` (
  FROM `Admonition` TO `Person`,
  FROM `Admonition` TO `Organization`,
  FROM `Admonition` TO `SoftwareApplication`,
  FROM `Admonition` TO `AuthorRole`,
  FROM `Article` TO `Person`,
  FROM `Article` TO `Organization`,
  FROM `Article` TO `SoftwareApplication`,
  FROM `Article` TO `AuthorRole`,
  FROM `AudioObject` TO `Person`,
  FROM `AudioObject` TO `Organization`,
  FROM `AudioObject` TO `SoftwareApplication`,
  FROM `AudioObject` TO `AuthorRole`,
  FROM `Button` TO `Person`,
  FROM `Button` TO `Organization`,
  FROM `Button` TO `SoftwareApplication`,
  FROM `Button` TO `AuthorRole`,
  FROM `Chat` TO `Person`,
  FROM `Chat` TO `Organization`,
  FROM `Chat` TO `SoftwareApplication`,
  FROM `Chat` TO `AuthorRole`,
  FROM `Claim` TO `Person`,
  FROM `Claim` TO `Organization`,
  FROM `Claim` TO `SoftwareApplication`,
  FROM `Claim` TO `AuthorRole`,
  FROM `CodeBlock` TO `Person`,
  FROM `CodeBlock` TO `Organization`,
  FROM `CodeBlock` TO `SoftwareApplication`,
  FROM `CodeBlock` TO `AuthorRole`,
  FROM `CodeChunk` TO `Person`,
  FROM `CodeChunk` TO `Organization`,
  FROM `CodeChunk` TO `SoftwareApplication`,
  FROM `CodeChunk` TO `AuthorRole`,
  FROM `CodeExpression` TO `Person`,
  FROM `CodeExpression` TO `Organization`,
  FROM `CodeExpression` TO `SoftwareApplication`,
  FROM `CodeExpression` TO `AuthorRole`,
  FROM `CodeInline` TO `Person`,
  FROM `CodeInline` TO `Organization`,
  FROM `CodeInline` TO `SoftwareApplication`,
  FROM `CodeInline` TO `AuthorRole`,
  FROM `Collection` TO `Person`,
  FROM `Collection` TO `Organization`,
  FROM `Collection` TO `SoftwareApplication`,
  FROM `Collection` TO `AuthorRole`,
  FROM `Comment` TO `Person`,
  FROM `Comment` TO `Organization`,
  FROM `Comment` TO `SoftwareApplication`,
  FROM `Comment` TO `AuthorRole`,
  FROM `CreativeWork` TO `Person`,
  FROM `CreativeWork` TO `Organization`,
  FROM `CreativeWork` TO `SoftwareApplication`,
  FROM `CreativeWork` TO `AuthorRole`,
  FROM `Datatable` TO `Person`,
  FROM `Datatable` TO `Organization`,
  FROM `Datatable` TO `SoftwareApplication`,
  FROM `Datatable` TO `AuthorRole`,
  FROM `Figure` TO `Person`,
  FROM `Figure` TO `Organization`,
  FROM `Figure` TO `SoftwareApplication`,
  FROM `Figure` TO `AuthorRole`,
  FROM `ForBlock` TO `Person`,
  FROM `ForBlock` TO `Organization`,
  FROM `ForBlock` TO `SoftwareApplication`,
  FROM `ForBlock` TO `AuthorRole`,
  FROM `Heading` TO `Person`,
  FROM `Heading` TO `Organization`,
  FROM `Heading` TO `SoftwareApplication`,
  FROM `Heading` TO `AuthorRole`,
  FROM `IfBlockClause` TO `Person`,
  FROM `IfBlockClause` TO `Organization`,
  FROM `IfBlockClause` TO `SoftwareApplication`,
  FROM `IfBlockClause` TO `AuthorRole`,
  FROM `ImageObject` TO `Person`,
  FROM `ImageObject` TO `Organization`,
  FROM `ImageObject` TO `SoftwareApplication`,
  FROM `ImageObject` TO `AuthorRole`,
  FROM `InstructionMessage` TO `Person`,
  FROM `InstructionMessage` TO `Organization`,
  FROM `InstructionMessage` TO `SoftwareApplication`,
  FROM `InstructionMessage` TO `AuthorRole`,
  FROM `List` TO `Person`,
  FROM `List` TO `Organization`,
  FROM `List` TO `SoftwareApplication`,
  FROM `List` TO `AuthorRole`,
  FROM `MathBlock` TO `Person`,
  FROM `MathBlock` TO `Organization`,
  FROM `MathBlock` TO `SoftwareApplication`,
  FROM `MathBlock` TO `AuthorRole`,
  FROM `MathInline` TO `Person`,
  FROM `MathInline` TO `Organization`,
  FROM `MathInline` TO `SoftwareApplication`,
  FROM `MathInline` TO `AuthorRole`,
  FROM `MediaObject` TO `Person`,
  FROM `MediaObject` TO `Organization`,
  FROM `MediaObject` TO `SoftwareApplication`,
  FROM `MediaObject` TO `AuthorRole`,
  FROM `Paragraph` TO `Person`,
  FROM `Paragraph` TO `Organization`,
  FROM `Paragraph` TO `SoftwareApplication`,
  FROM `Paragraph` TO `AuthorRole`,
  FROM `Periodical` TO `Person`,
  FROM `Periodical` TO `Organization`,
  FROM `Periodical` TO `SoftwareApplication`,
  FROM `Periodical` TO `AuthorRole`,
  FROM `Prompt` TO `Person`,
  FROM `Prompt` TO `Organization`,
  FROM `Prompt` TO `SoftwareApplication`,
  FROM `Prompt` TO `AuthorRole`,
  FROM `PublicationIssue` TO `Person`,
  FROM `PublicationIssue` TO `Organization`,
  FROM `PublicationIssue` TO `SoftwareApplication`,
  FROM `PublicationIssue` TO `AuthorRole`,
  FROM `PublicationVolume` TO `Person`,
  FROM `PublicationVolume` TO `Organization`,
  FROM `PublicationVolume` TO `SoftwareApplication`,
  FROM `PublicationVolume` TO `AuthorRole`,
  FROM `QuoteBlock` TO `Person`,
  FROM `QuoteBlock` TO `Organization`,
  FROM `QuoteBlock` TO `SoftwareApplication`,
  FROM `QuoteBlock` TO `AuthorRole`,
  FROM `RawBlock` TO `Person`,
  FROM `RawBlock` TO `Organization`,
  FROM `RawBlock` TO `SoftwareApplication`,
  FROM `RawBlock` TO `AuthorRole`,
  FROM `Review` TO `Person`,
  FROM `Review` TO `Organization`,
  FROM `Review` TO `SoftwareApplication`,
  FROM `Review` TO `AuthorRole`,
  FROM `Section` TO `Person`,
  FROM `Section` TO `Organization`,
  FROM `Section` TO `SoftwareApplication`,
  FROM `Section` TO `AuthorRole`,
  FROM `SoftwareApplication` TO `Person`,
  FROM `SoftwareApplication` TO `Organization`,
  FROM `SoftwareApplication` TO `SoftwareApplication`,
  FROM `SoftwareApplication` TO `AuthorRole`,
  FROM `SoftwareSourceCode` TO `Person`,
  FROM `SoftwareSourceCode` TO `Organization`,
  FROM `SoftwareSourceCode` TO `SoftwareApplication`,
  FROM `SoftwareSourceCode` TO `AuthorRole`,
  FROM `StyledBlock` TO `Person`,
  FROM `StyledBlock` TO `Organization`,
  FROM `StyledBlock` TO `SoftwareApplication`,
  FROM `StyledBlock` TO `AuthorRole`,
  FROM `StyledInline` TO `Person`,
  FROM `StyledInline` TO `Organization`,
  FROM `StyledInline` TO `SoftwareApplication`,
  FROM `StyledInline` TO `AuthorRole`,
  FROM `SuggestionBlock` TO `Person`,
  FROM `SuggestionBlock` TO `Organization`,
  FROM `SuggestionBlock` TO `SoftwareApplication`,
  FROM `SuggestionBlock` TO `AuthorRole`,
  FROM `SuggestionInline` TO `Person`,
  FROM `SuggestionInline` TO `Organization`,
  FROM `SuggestionInline` TO `SoftwareApplication`,
  FROM `SuggestionInline` TO `AuthorRole`,
  FROM `Table` TO `Person`,
  FROM `Table` TO `Organization`,
  FROM `Table` TO `SoftwareApplication`,
  FROM `Table` TO `AuthorRole`,
  FROM `VideoObject` TO `Person`,
  FROM `VideoObject` TO `Organization`,
  FROM `VideoObject` TO `SoftwareApplication`,
  FROM `VideoObject` TO `AuthorRole`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `brands` (
  FROM `Organization` TO `Brand`,
  FROM `Product` TO `Brand`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `caption` (
  FROM `AudioObject` TO `Annotation`,
  FROM `AudioObject` TO `AudioObject`,
  FROM `AudioObject` TO `Button`,
  FROM `AudioObject` TO `Cite`,
  FROM `AudioObject` TO `CiteGroup`,
  FROM `AudioObject` TO `CodeExpression`,
  FROM `AudioObject` TO `CodeInline`,
  FROM `AudioObject` TO `Date`,
  FROM `AudioObject` TO `DateTime`,
  FROM `AudioObject` TO `Duration`,
  FROM `AudioObject` TO `Emphasis`,
  FROM `AudioObject` TO `ImageObject`,
  FROM `AudioObject` TO `InstructionInline`,
  FROM `AudioObject` TO `Link`,
  FROM `AudioObject` TO `MathInline`,
  FROM `AudioObject` TO `MediaObject`,
  FROM `AudioObject` TO `Note`,
  FROM `AudioObject` TO `Parameter`,
  FROM `AudioObject` TO `QuoteInline`,
  FROM `AudioObject` TO `StyledInline`,
  FROM `AudioObject` TO `Strikeout`,
  FROM `AudioObject` TO `Strong`,
  FROM `AudioObject` TO `Subscript`,
  FROM `AudioObject` TO `SuggestionInline`,
  FROM `AudioObject` TO `Superscript`,
  FROM `AudioObject` TO `Text`,
  FROM `AudioObject` TO `Time`,
  FROM `AudioObject` TO `Timestamp`,
  FROM `AudioObject` TO `Underline`,
  FROM `AudioObject` TO `VideoObject`,
  FROM `CodeChunk` TO `Admonition`,
  FROM `CodeChunk` TO `AudioObject`,
  FROM `CodeChunk` TO `CallBlock`,
  FROM `CodeChunk` TO `Chat`,
  FROM `CodeChunk` TO `ChatMessage`,
  FROM `CodeChunk` TO `ChatMessageGroup`,
  FROM `CodeChunk` TO `Claim`,
  FROM `CodeChunk` TO `CodeBlock`,
  FROM `CodeChunk` TO `CodeChunk`,
  FROM `CodeChunk` TO `Figure`,
  FROM `CodeChunk` TO `File`,
  FROM `CodeChunk` TO `ForBlock`,
  FROM `CodeChunk` TO `Form`,
  FROM `CodeChunk` TO `Heading`,
  FROM `CodeChunk` TO `IfBlock`,
  FROM `CodeChunk` TO `ImageObject`,
  FROM `CodeChunk` TO `IncludeBlock`,
  FROM `CodeChunk` TO `InstructionBlock`,
  FROM `CodeChunk` TO `List`,
  FROM `CodeChunk` TO `MathBlock`,
  FROM `CodeChunk` TO `Paragraph`,
  FROM `CodeChunk` TO `PromptBlock`,
  FROM `CodeChunk` TO `QuoteBlock`,
  FROM `CodeChunk` TO `RawBlock`,
  FROM `CodeChunk` TO `Section`,
  FROM `CodeChunk` TO `StyledBlock`,
  FROM `CodeChunk` TO `SuggestionBlock`,
  FROM `CodeChunk` TO `Table`,
  FROM `CodeChunk` TO `ThematicBreak`,
  FROM `CodeChunk` TO `VideoObject`,
  FROM `CodeChunk` TO `Walkthrough`,
  FROM `Figure` TO `Admonition`,
  FROM `Figure` TO `AudioObject`,
  FROM `Figure` TO `CallBlock`,
  FROM `Figure` TO `Chat`,
  FROM `Figure` TO `ChatMessage`,
  FROM `Figure` TO `ChatMessageGroup`,
  FROM `Figure` TO `Claim`,
  FROM `Figure` TO `CodeBlock`,
  FROM `Figure` TO `CodeChunk`,
  FROM `Figure` TO `Figure`,
  FROM `Figure` TO `File`,
  FROM `Figure` TO `ForBlock`,
  FROM `Figure` TO `Form`,
  FROM `Figure` TO `Heading`,
  FROM `Figure` TO `IfBlock`,
  FROM `Figure` TO `ImageObject`,
  FROM `Figure` TO `IncludeBlock`,
  FROM `Figure` TO `InstructionBlock`,
  FROM `Figure` TO `List`,
  FROM `Figure` TO `MathBlock`,
  FROM `Figure` TO `Paragraph`,
  FROM `Figure` TO `PromptBlock`,
  FROM `Figure` TO `QuoteBlock`,
  FROM `Figure` TO `RawBlock`,
  FROM `Figure` TO `Section`,
  FROM `Figure` TO `StyledBlock`,
  FROM `Figure` TO `SuggestionBlock`,
  FROM `Figure` TO `Table`,
  FROM `Figure` TO `ThematicBreak`,
  FROM `Figure` TO `VideoObject`,
  FROM `Figure` TO `Walkthrough`,
  FROM `ImageObject` TO `Annotation`,
  FROM `ImageObject` TO `AudioObject`,
  FROM `ImageObject` TO `Button`,
  FROM `ImageObject` TO `Cite`,
  FROM `ImageObject` TO `CiteGroup`,
  FROM `ImageObject` TO `CodeExpression`,
  FROM `ImageObject` TO `CodeInline`,
  FROM `ImageObject` TO `Date`,
  FROM `ImageObject` TO `DateTime`,
  FROM `ImageObject` TO `Duration`,
  FROM `ImageObject` TO `Emphasis`,
  FROM `ImageObject` TO `ImageObject`,
  FROM `ImageObject` TO `InstructionInline`,
  FROM `ImageObject` TO `Link`,
  FROM `ImageObject` TO `MathInline`,
  FROM `ImageObject` TO `MediaObject`,
  FROM `ImageObject` TO `Note`,
  FROM `ImageObject` TO `Parameter`,
  FROM `ImageObject` TO `QuoteInline`,
  FROM `ImageObject` TO `StyledInline`,
  FROM `ImageObject` TO `Strikeout`,
  FROM `ImageObject` TO `Strong`,
  FROM `ImageObject` TO `Subscript`,
  FROM `ImageObject` TO `SuggestionInline`,
  FROM `ImageObject` TO `Superscript`,
  FROM `ImageObject` TO `Text`,
  FROM `ImageObject` TO `Time`,
  FROM `ImageObject` TO `Timestamp`,
  FROM `ImageObject` TO `Underline`,
  FROM `ImageObject` TO `VideoObject`,
  FROM `Table` TO `Admonition`,
  FROM `Table` TO `AudioObject`,
  FROM `Table` TO `CallBlock`,
  FROM `Table` TO `Chat`,
  FROM `Table` TO `ChatMessage`,
  FROM `Table` TO `ChatMessageGroup`,
  FROM `Table` TO `Claim`,
  FROM `Table` TO `CodeBlock`,
  FROM `Table` TO `CodeChunk`,
  FROM `Table` TO `Figure`,
  FROM `Table` TO `File`,
  FROM `Table` TO `ForBlock`,
  FROM `Table` TO `Form`,
  FROM `Table` TO `Heading`,
  FROM `Table` TO `IfBlock`,
  FROM `Table` TO `ImageObject`,
  FROM `Table` TO `IncludeBlock`,
  FROM `Table` TO `InstructionBlock`,
  FROM `Table` TO `List`,
  FROM `Table` TO `MathBlock`,
  FROM `Table` TO `Paragraph`,
  FROM `Table` TO `PromptBlock`,
  FROM `Table` TO `QuoteBlock`,
  FROM `Table` TO `RawBlock`,
  FROM `Table` TO `Section`,
  FROM `Table` TO `StyledBlock`,
  FROM `Table` TO `SuggestionBlock`,
  FROM `Table` TO `Table`,
  FROM `Table` TO `ThematicBreak`,
  FROM `Table` TO `VideoObject`,
  FROM `Table` TO `Walkthrough`,
  FROM `VideoObject` TO `Annotation`,
  FROM `VideoObject` TO `AudioObject`,
  FROM `VideoObject` TO `Button`,
  FROM `VideoObject` TO `Cite`,
  FROM `VideoObject` TO `CiteGroup`,
  FROM `VideoObject` TO `CodeExpression`,
  FROM `VideoObject` TO `CodeInline`,
  FROM `VideoObject` TO `Date`,
  FROM `VideoObject` TO `DateTime`,
  FROM `VideoObject` TO `Duration`,
  FROM `VideoObject` TO `Emphasis`,
  FROM `VideoObject` TO `ImageObject`,
  FROM `VideoObject` TO `InstructionInline`,
  FROM `VideoObject` TO `Link`,
  FROM `VideoObject` TO `MathInline`,
  FROM `VideoObject` TO `MediaObject`,
  FROM `VideoObject` TO `Note`,
  FROM `VideoObject` TO `Parameter`,
  FROM `VideoObject` TO `QuoteInline`,
  FROM `VideoObject` TO `StyledInline`,
  FROM `VideoObject` TO `Strikeout`,
  FROM `VideoObject` TO `Strong`,
  FROM `VideoObject` TO `Subscript`,
  FROM `VideoObject` TO `SuggestionInline`,
  FROM `VideoObject` TO `Superscript`,
  FROM `VideoObject` TO `Text`,
  FROM `VideoObject` TO `Time`,
  FROM `VideoObject` TO `Timestamp`,
  FROM `VideoObject` TO `Underline`,
  FROM `VideoObject` TO `VideoObject`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `cells` (
  FROM `TableRow` TO `TableCell`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `clauses` (
  FROM `IfBlock` TO `IfBlockClause`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `columns` (
  FROM `Datatable` TO `DatatableColumn`,
  FROM `DatatableHint` TO `DatatableColumnHint`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `comments` (
  FROM `Article` TO `Comment`,
  FROM `AudioObject` TO `Comment`,
  FROM `Chat` TO `Comment`,
  FROM `Claim` TO `Comment`,
  FROM `Collection` TO `Comment`,
  FROM `Comment` TO `Comment`,
  FROM `CreativeWork` TO `Comment`,
  FROM `Datatable` TO `Comment`,
  FROM `Figure` TO `Comment`,
  FROM `ImageObject` TO `Comment`,
  FROM `MediaObject` TO `Comment`,
  FROM `Periodical` TO `Comment`,
  FROM `Prompt` TO `Comment`,
  FROM `PublicationIssue` TO `Comment`,
  FROM `PublicationVolume` TO `Comment`,
  FROM `Review` TO `Comment`,
  FROM `SoftwareApplication` TO `Comment`,
  FROM `SoftwareSourceCode` TO `Comment`,
  FROM `Table` TO `Comment`,
  FROM `VideoObject` TO `Comment`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `compilationMessages` (
  FROM `Article` TO `CompilationMessage`,
  FROM `Button` TO `CompilationMessage`,
  FROM `CallArgument` TO `CompilationMessage`,
  FROM `CallBlock` TO `CompilationMessage`,
  FROM `Chat` TO `CompilationMessage`,
  FROM `ChatMessage` TO `CompilationMessage`,
  FROM `CodeChunk` TO `CompilationMessage`,
  FROM `CodeExpression` TO `CompilationMessage`,
  FROM `ForBlock` TO `CompilationMessage`,
  FROM `Form` TO `CompilationMessage`,
  FROM `IfBlock` TO `CompilationMessage`,
  FROM `IfBlockClause` TO `CompilationMessage`,
  FROM `IncludeBlock` TO `CompilationMessage`,
  FROM `InstructionBlock` TO `CompilationMessage`,
  FROM `InstructionInline` TO `CompilationMessage`,
  FROM `MathBlock` TO `CompilationMessage`,
  FROM `MathInline` TO `CompilationMessage`,
  FROM `Parameter` TO `CompilationMessage`,
  FROM `Prompt` TO `CompilationMessage`,
  FROM `PromptBlock` TO `CompilationMessage`,
  FROM `RawBlock` TO `CompilationMessage`,
  FROM `StyledBlock` TO `CompilationMessage`,
  FROM `StyledInline` TO `CompilationMessage`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `contactPoints` (
  FROM `Organization` TO `ContactPoint`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `content` (
  FROM `Admonition` TO `Admonition`,
  FROM `Admonition` TO `AudioObject`,
  FROM `Admonition` TO `CallBlock`,
  FROM `Admonition` TO `Chat`,
  FROM `Admonition` TO `ChatMessage`,
  FROM `Admonition` TO `ChatMessageGroup`,
  FROM `Admonition` TO `Claim`,
  FROM `Admonition` TO `CodeBlock`,
  FROM `Admonition` TO `CodeChunk`,
  FROM `Admonition` TO `Figure`,
  FROM `Admonition` TO `File`,
  FROM `Admonition` TO `ForBlock`,
  FROM `Admonition` TO `Form`,
  FROM `Admonition` TO `Heading`,
  FROM `Admonition` TO `IfBlock`,
  FROM `Admonition` TO `ImageObject`,
  FROM `Admonition` TO `IncludeBlock`,
  FROM `Admonition` TO `InstructionBlock`,
  FROM `Admonition` TO `List`,
  FROM `Admonition` TO `MathBlock`,
  FROM `Admonition` TO `Paragraph`,
  FROM `Admonition` TO `PromptBlock`,
  FROM `Admonition` TO `QuoteBlock`,
  FROM `Admonition` TO `RawBlock`,
  FROM `Admonition` TO `Section`,
  FROM `Admonition` TO `StyledBlock`,
  FROM `Admonition` TO `SuggestionBlock`,
  FROM `Admonition` TO `Table`,
  FROM `Admonition` TO `ThematicBreak`,
  FROM `Admonition` TO `VideoObject`,
  FROM `Admonition` TO `Walkthrough`,
  FROM `Annotation` TO `Annotation`,
  FROM `Annotation` TO `AudioObject`,
  FROM `Annotation` TO `Button`,
  FROM `Annotation` TO `Cite`,
  FROM `Annotation` TO `CiteGroup`,
  FROM `Annotation` TO `CodeExpression`,
  FROM `Annotation` TO `CodeInline`,
  FROM `Annotation` TO `Date`,
  FROM `Annotation` TO `DateTime`,
  FROM `Annotation` TO `Duration`,
  FROM `Annotation` TO `Emphasis`,
  FROM `Annotation` TO `ImageObject`,
  FROM `Annotation` TO `InstructionInline`,
  FROM `Annotation` TO `Link`,
  FROM `Annotation` TO `MathInline`,
  FROM `Annotation` TO `MediaObject`,
  FROM `Annotation` TO `Note`,
  FROM `Annotation` TO `Parameter`,
  FROM `Annotation` TO `QuoteInline`,
  FROM `Annotation` TO `StyledInline`,
  FROM `Annotation` TO `Strikeout`,
  FROM `Annotation` TO `Strong`,
  FROM `Annotation` TO `Subscript`,
  FROM `Annotation` TO `SuggestionInline`,
  FROM `Annotation` TO `Superscript`,
  FROM `Annotation` TO `Text`,
  FROM `Annotation` TO `Time`,
  FROM `Annotation` TO `Timestamp`,
  FROM `Annotation` TO `Underline`,
  FROM `Annotation` TO `VideoObject`,
  FROM `Article` TO `Admonition`,
  FROM `Article` TO `AudioObject`,
  FROM `Article` TO `CallBlock`,
  FROM `Article` TO `Chat`,
  FROM `Article` TO `ChatMessage`,
  FROM `Article` TO `ChatMessageGroup`,
  FROM `Article` TO `Claim`,
  FROM `Article` TO `CodeBlock`,
  FROM `Article` TO `CodeChunk`,
  FROM `Article` TO `Figure`,
  FROM `Article` TO `File`,
  FROM `Article` TO `ForBlock`,
  FROM `Article` TO `Form`,
  FROM `Article` TO `Heading`,
  FROM `Article` TO `IfBlock`,
  FROM `Article` TO `ImageObject`,
  FROM `Article` TO `IncludeBlock`,
  FROM `Article` TO `InstructionBlock`,
  FROM `Article` TO `List`,
  FROM `Article` TO `MathBlock`,
  FROM `Article` TO `Paragraph`,
  FROM `Article` TO `PromptBlock`,
  FROM `Article` TO `QuoteBlock`,
  FROM `Article` TO `RawBlock`,
  FROM `Article` TO `Section`,
  FROM `Article` TO `StyledBlock`,
  FROM `Article` TO `SuggestionBlock`,
  FROM `Article` TO `Table`,
  FROM `Article` TO `ThematicBreak`,
  FROM `Article` TO `VideoObject`,
  FROM `Article` TO `Walkthrough`,
  FROM `CallBlock` TO `Admonition`,
  FROM `CallBlock` TO `AudioObject`,
  FROM `CallBlock` TO `CallBlock`,
  FROM `CallBlock` TO `Chat`,
  FROM `CallBlock` TO `ChatMessage`,
  FROM `CallBlock` TO `ChatMessageGroup`,
  FROM `CallBlock` TO `Claim`,
  FROM `CallBlock` TO `CodeBlock`,
  FROM `CallBlock` TO `CodeChunk`,
  FROM `CallBlock` TO `Figure`,
  FROM `CallBlock` TO `File`,
  FROM `CallBlock` TO `ForBlock`,
  FROM `CallBlock` TO `Form`,
  FROM `CallBlock` TO `Heading`,
  FROM `CallBlock` TO `IfBlock`,
  FROM `CallBlock` TO `ImageObject`,
  FROM `CallBlock` TO `IncludeBlock`,
  FROM `CallBlock` TO `InstructionBlock`,
  FROM `CallBlock` TO `List`,
  FROM `CallBlock` TO `MathBlock`,
  FROM `CallBlock` TO `Paragraph`,
  FROM `CallBlock` TO `PromptBlock`,
  FROM `CallBlock` TO `QuoteBlock`,
  FROM `CallBlock` TO `RawBlock`,
  FROM `CallBlock` TO `Section`,
  FROM `CallBlock` TO `StyledBlock`,
  FROM `CallBlock` TO `SuggestionBlock`,
  FROM `CallBlock` TO `Table`,
  FROM `CallBlock` TO `ThematicBreak`,
  FROM `CallBlock` TO `VideoObject`,
  FROM `CallBlock` TO `Walkthrough`,
  FROM `Chat` TO `Admonition`,
  FROM `Chat` TO `AudioObject`,
  FROM `Chat` TO `CallBlock`,
  FROM `Chat` TO `Chat`,
  FROM `Chat` TO `ChatMessage`,
  FROM `Chat` TO `ChatMessageGroup`,
  FROM `Chat` TO `Claim`,
  FROM `Chat` TO `CodeBlock`,
  FROM `Chat` TO `CodeChunk`,
  FROM `Chat` TO `Figure`,
  FROM `Chat` TO `File`,
  FROM `Chat` TO `ForBlock`,
  FROM `Chat` TO `Form`,
  FROM `Chat` TO `Heading`,
  FROM `Chat` TO `IfBlock`,
  FROM `Chat` TO `ImageObject`,
  FROM `Chat` TO `IncludeBlock`,
  FROM `Chat` TO `InstructionBlock`,
  FROM `Chat` TO `List`,
  FROM `Chat` TO `MathBlock`,
  FROM `Chat` TO `Paragraph`,
  FROM `Chat` TO `PromptBlock`,
  FROM `Chat` TO `QuoteBlock`,
  FROM `Chat` TO `RawBlock`,
  FROM `Chat` TO `Section`,
  FROM `Chat` TO `StyledBlock`,
  FROM `Chat` TO `SuggestionBlock`,
  FROM `Chat` TO `Table`,
  FROM `Chat` TO `ThematicBreak`,
  FROM `Chat` TO `VideoObject`,
  FROM `Chat` TO `Walkthrough`,
  FROM `ChatMessage` TO `Admonition`,
  FROM `ChatMessage` TO `AudioObject`,
  FROM `ChatMessage` TO `CallBlock`,
  FROM `ChatMessage` TO `Chat`,
  FROM `ChatMessage` TO `ChatMessage`,
  FROM `ChatMessage` TO `ChatMessageGroup`,
  FROM `ChatMessage` TO `Claim`,
  FROM `ChatMessage` TO `CodeBlock`,
  FROM `ChatMessage` TO `CodeChunk`,
  FROM `ChatMessage` TO `Figure`,
  FROM `ChatMessage` TO `File`,
  FROM `ChatMessage` TO `ForBlock`,
  FROM `ChatMessage` TO `Form`,
  FROM `ChatMessage` TO `Heading`,
  FROM `ChatMessage` TO `IfBlock`,
  FROM `ChatMessage` TO `ImageObject`,
  FROM `ChatMessage` TO `IncludeBlock`,
  FROM `ChatMessage` TO `InstructionBlock`,
  FROM `ChatMessage` TO `List`,
  FROM `ChatMessage` TO `MathBlock`,
  FROM `ChatMessage` TO `Paragraph`,
  FROM `ChatMessage` TO `PromptBlock`,
  FROM `ChatMessage` TO `QuoteBlock`,
  FROM `ChatMessage` TO `RawBlock`,
  FROM `ChatMessage` TO `Section`,
  FROM `ChatMessage` TO `StyledBlock`,
  FROM `ChatMessage` TO `SuggestionBlock`,
  FROM `ChatMessage` TO `Table`,
  FROM `ChatMessage` TO `ThematicBreak`,
  FROM `ChatMessage` TO `VideoObject`,
  FROM `ChatMessage` TO `Walkthrough`,
  FROM `Cite` TO `Annotation`,
  FROM `Cite` TO `AudioObject`,
  FROM `Cite` TO `Button`,
  FROM `Cite` TO `Cite`,
  FROM `Cite` TO `CiteGroup`,
  FROM `Cite` TO `CodeExpression`,
  FROM `Cite` TO `CodeInline`,
  FROM `Cite` TO `Date`,
  FROM `Cite` TO `DateTime`,
  FROM `Cite` TO `Duration`,
  FROM `Cite` TO `Emphasis`,
  FROM `Cite` TO `ImageObject`,
  FROM `Cite` TO `InstructionInline`,
  FROM `Cite` TO `Link`,
  FROM `Cite` TO `MathInline`,
  FROM `Cite` TO `MediaObject`,
  FROM `Cite` TO `Note`,
  FROM `Cite` TO `Parameter`,
  FROM `Cite` TO `QuoteInline`,
  FROM `Cite` TO `StyledInline`,
  FROM `Cite` TO `Strikeout`,
  FROM `Cite` TO `Strong`,
  FROM `Cite` TO `Subscript`,
  FROM `Cite` TO `SuggestionInline`,
  FROM `Cite` TO `Superscript`,
  FROM `Cite` TO `Text`,
  FROM `Cite` TO `Time`,
  FROM `Cite` TO `Timestamp`,
  FROM `Cite` TO `Underline`,
  FROM `Cite` TO `VideoObject`,
  FROM `Claim` TO `Admonition`,
  FROM `Claim` TO `AudioObject`,
  FROM `Claim` TO `CallBlock`,
  FROM `Claim` TO `Chat`,
  FROM `Claim` TO `ChatMessage`,
  FROM `Claim` TO `ChatMessageGroup`,
  FROM `Claim` TO `Claim`,
  FROM `Claim` TO `CodeBlock`,
  FROM `Claim` TO `CodeChunk`,
  FROM `Claim` TO `Figure`,
  FROM `Claim` TO `File`,
  FROM `Claim` TO `ForBlock`,
  FROM `Claim` TO `Form`,
  FROM `Claim` TO `Heading`,
  FROM `Claim` TO `IfBlock`,
  FROM `Claim` TO `ImageObject`,
  FROM `Claim` TO `IncludeBlock`,
  FROM `Claim` TO `InstructionBlock`,
  FROM `Claim` TO `List`,
  FROM `Claim` TO `MathBlock`,
  FROM `Claim` TO `Paragraph`,
  FROM `Claim` TO `PromptBlock`,
  FROM `Claim` TO `QuoteBlock`,
  FROM `Claim` TO `RawBlock`,
  FROM `Claim` TO `Section`,
  FROM `Claim` TO `StyledBlock`,
  FROM `Claim` TO `SuggestionBlock`,
  FROM `Claim` TO `Table`,
  FROM `Claim` TO `ThematicBreak`,
  FROM `Claim` TO `VideoObject`,
  FROM `Claim` TO `Walkthrough`,
  FROM `Comment` TO `Admonition`,
  FROM `Comment` TO `AudioObject`,
  FROM `Comment` TO `CallBlock`,
  FROM `Comment` TO `Chat`,
  FROM `Comment` TO `ChatMessage`,
  FROM `Comment` TO `ChatMessageGroup`,
  FROM `Comment` TO `Claim`,
  FROM `Comment` TO `CodeBlock`,
  FROM `Comment` TO `CodeChunk`,
  FROM `Comment` TO `Figure`,
  FROM `Comment` TO `File`,
  FROM `Comment` TO `ForBlock`,
  FROM `Comment` TO `Form`,
  FROM `Comment` TO `Heading`,
  FROM `Comment` TO `IfBlock`,
  FROM `Comment` TO `ImageObject`,
  FROM `Comment` TO `IncludeBlock`,
  FROM `Comment` TO `InstructionBlock`,
  FROM `Comment` TO `List`,
  FROM `Comment` TO `MathBlock`,
  FROM `Comment` TO `Paragraph`,
  FROM `Comment` TO `PromptBlock`,
  FROM `Comment` TO `QuoteBlock`,
  FROM `Comment` TO `RawBlock`,
  FROM `Comment` TO `Section`,
  FROM `Comment` TO `StyledBlock`,
  FROM `Comment` TO `SuggestionBlock`,
  FROM `Comment` TO `Table`,
  FROM `Comment` TO `ThematicBreak`,
  FROM `Comment` TO `VideoObject`,
  FROM `Comment` TO `Walkthrough`,
  FROM `Emphasis` TO `Annotation`,
  FROM `Emphasis` TO `AudioObject`,
  FROM `Emphasis` TO `Button`,
  FROM `Emphasis` TO `Cite`,
  FROM `Emphasis` TO `CiteGroup`,
  FROM `Emphasis` TO `CodeExpression`,
  FROM `Emphasis` TO `CodeInline`,
  FROM `Emphasis` TO `Date`,
  FROM `Emphasis` TO `DateTime`,
  FROM `Emphasis` TO `Duration`,
  FROM `Emphasis` TO `Emphasis`,
  FROM `Emphasis` TO `ImageObject`,
  FROM `Emphasis` TO `InstructionInline`,
  FROM `Emphasis` TO `Link`,
  FROM `Emphasis` TO `MathInline`,
  FROM `Emphasis` TO `MediaObject`,
  FROM `Emphasis` TO `Note`,
  FROM `Emphasis` TO `Parameter`,
  FROM `Emphasis` TO `QuoteInline`,
  FROM `Emphasis` TO `StyledInline`,
  FROM `Emphasis` TO `Strikeout`,
  FROM `Emphasis` TO `Strong`,
  FROM `Emphasis` TO `Subscript`,
  FROM `Emphasis` TO `SuggestionInline`,
  FROM `Emphasis` TO `Superscript`,
  FROM `Emphasis` TO `Text`,
  FROM `Emphasis` TO `Time`,
  FROM `Emphasis` TO `Timestamp`,
  FROM `Emphasis` TO `Underline`,
  FROM `Emphasis` TO `VideoObject`,
  FROM `Figure` TO `Admonition`,
  FROM `Figure` TO `AudioObject`,
  FROM `Figure` TO `CallBlock`,
  FROM `Figure` TO `Chat`,
  FROM `Figure` TO `ChatMessage`,
  FROM `Figure` TO `ChatMessageGroup`,
  FROM `Figure` TO `Claim`,
  FROM `Figure` TO `CodeBlock`,
  FROM `Figure` TO `CodeChunk`,
  FROM `Figure` TO `Figure`,
  FROM `Figure` TO `File`,
  FROM `Figure` TO `ForBlock`,
  FROM `Figure` TO `Form`,
  FROM `Figure` TO `Heading`,
  FROM `Figure` TO `IfBlock`,
  FROM `Figure` TO `ImageObject`,
  FROM `Figure` TO `IncludeBlock`,
  FROM `Figure` TO `InstructionBlock`,
  FROM `Figure` TO `List`,
  FROM `Figure` TO `MathBlock`,
  FROM `Figure` TO `Paragraph`,
  FROM `Figure` TO `PromptBlock`,
  FROM `Figure` TO `QuoteBlock`,
  FROM `Figure` TO `RawBlock`,
  FROM `Figure` TO `Section`,
  FROM `Figure` TO `StyledBlock`,
  FROM `Figure` TO `SuggestionBlock`,
  FROM `Figure` TO `Table`,
  FROM `Figure` TO `ThematicBreak`,
  FROM `Figure` TO `VideoObject`,
  FROM `Figure` TO `Walkthrough`,
  FROM `ForBlock` TO `Admonition`,
  FROM `ForBlock` TO `AudioObject`,
  FROM `ForBlock` TO `CallBlock`,
  FROM `ForBlock` TO `Chat`,
  FROM `ForBlock` TO `ChatMessage`,
  FROM `ForBlock` TO `ChatMessageGroup`,
  FROM `ForBlock` TO `Claim`,
  FROM `ForBlock` TO `CodeBlock`,
  FROM `ForBlock` TO `CodeChunk`,
  FROM `ForBlock` TO `Figure`,
  FROM `ForBlock` TO `File`,
  FROM `ForBlock` TO `ForBlock`,
  FROM `ForBlock` TO `Form`,
  FROM `ForBlock` TO `Heading`,
  FROM `ForBlock` TO `IfBlock`,
  FROM `ForBlock` TO `ImageObject`,
  FROM `ForBlock` TO `IncludeBlock`,
  FROM `ForBlock` TO `InstructionBlock`,
  FROM `ForBlock` TO `List`,
  FROM `ForBlock` TO `MathBlock`,
  FROM `ForBlock` TO `Paragraph`,
  FROM `ForBlock` TO `PromptBlock`,
  FROM `ForBlock` TO `QuoteBlock`,
  FROM `ForBlock` TO `RawBlock`,
  FROM `ForBlock` TO `Section`,
  FROM `ForBlock` TO `StyledBlock`,
  FROM `ForBlock` TO `SuggestionBlock`,
  FROM `ForBlock` TO `Table`,
  FROM `ForBlock` TO `ThematicBreak`,
  FROM `ForBlock` TO `VideoObject`,
  FROM `ForBlock` TO `Walkthrough`,
  FROM `Form` TO `Admonition`,
  FROM `Form` TO `AudioObject`,
  FROM `Form` TO `CallBlock`,
  FROM `Form` TO `Chat`,
  FROM `Form` TO `ChatMessage`,
  FROM `Form` TO `ChatMessageGroup`,
  FROM `Form` TO `Claim`,
  FROM `Form` TO `CodeBlock`,
  FROM `Form` TO `CodeChunk`,
  FROM `Form` TO `Figure`,
  FROM `Form` TO `File`,
  FROM `Form` TO `ForBlock`,
  FROM `Form` TO `Form`,
  FROM `Form` TO `Heading`,
  FROM `Form` TO `IfBlock`,
  FROM `Form` TO `ImageObject`,
  FROM `Form` TO `IncludeBlock`,
  FROM `Form` TO `InstructionBlock`,
  FROM `Form` TO `List`,
  FROM `Form` TO `MathBlock`,
  FROM `Form` TO `Paragraph`,
  FROM `Form` TO `PromptBlock`,
  FROM `Form` TO `QuoteBlock`,
  FROM `Form` TO `RawBlock`,
  FROM `Form` TO `Section`,
  FROM `Form` TO `StyledBlock`,
  FROM `Form` TO `SuggestionBlock`,
  FROM `Form` TO `Table`,
  FROM `Form` TO `ThematicBreak`,
  FROM `Form` TO `VideoObject`,
  FROM `Form` TO `Walkthrough`,
  FROM `Heading` TO `Annotation`,
  FROM `Heading` TO `AudioObject`,
  FROM `Heading` TO `Button`,
  FROM `Heading` TO `Cite`,
  FROM `Heading` TO `CiteGroup`,
  FROM `Heading` TO `CodeExpression`,
  FROM `Heading` TO `CodeInline`,
  FROM `Heading` TO `Date`,
  FROM `Heading` TO `DateTime`,
  FROM `Heading` TO `Duration`,
  FROM `Heading` TO `Emphasis`,
  FROM `Heading` TO `ImageObject`,
  FROM `Heading` TO `InstructionInline`,
  FROM `Heading` TO `Link`,
  FROM `Heading` TO `MathInline`,
  FROM `Heading` TO `MediaObject`,
  FROM `Heading` TO `Note`,
  FROM `Heading` TO `Parameter`,
  FROM `Heading` TO `QuoteInline`,
  FROM `Heading` TO `StyledInline`,
  FROM `Heading` TO `Strikeout`,
  FROM `Heading` TO `Strong`,
  FROM `Heading` TO `Subscript`,
  FROM `Heading` TO `SuggestionInline`,
  FROM `Heading` TO `Superscript`,
  FROM `Heading` TO `Text`,
  FROM `Heading` TO `Time`,
  FROM `Heading` TO `Timestamp`,
  FROM `Heading` TO `Underline`,
  FROM `Heading` TO `VideoObject`,
  FROM `IfBlockClause` TO `Admonition`,
  FROM `IfBlockClause` TO `AudioObject`,
  FROM `IfBlockClause` TO `CallBlock`,
  FROM `IfBlockClause` TO `Chat`,
  FROM `IfBlockClause` TO `ChatMessage`,
  FROM `IfBlockClause` TO `ChatMessageGroup`,
  FROM `IfBlockClause` TO `Claim`,
  FROM `IfBlockClause` TO `CodeBlock`,
  FROM `IfBlockClause` TO `CodeChunk`,
  FROM `IfBlockClause` TO `Figure`,
  FROM `IfBlockClause` TO `File`,
  FROM `IfBlockClause` TO `ForBlock`,
  FROM `IfBlockClause` TO `Form`,
  FROM `IfBlockClause` TO `Heading`,
  FROM `IfBlockClause` TO `IfBlock`,
  FROM `IfBlockClause` TO `ImageObject`,
  FROM `IfBlockClause` TO `IncludeBlock`,
  FROM `IfBlockClause` TO `InstructionBlock`,
  FROM `IfBlockClause` TO `List`,
  FROM `IfBlockClause` TO `MathBlock`,
  FROM `IfBlockClause` TO `Paragraph`,
  FROM `IfBlockClause` TO `PromptBlock`,
  FROM `IfBlockClause` TO `QuoteBlock`,
  FROM `IfBlockClause` TO `RawBlock`,
  FROM `IfBlockClause` TO `Section`,
  FROM `IfBlockClause` TO `StyledBlock`,
  FROM `IfBlockClause` TO `SuggestionBlock`,
  FROM `IfBlockClause` TO `Table`,
  FROM `IfBlockClause` TO `ThematicBreak`,
  FROM `IfBlockClause` TO `VideoObject`,
  FROM `IfBlockClause` TO `Walkthrough`,
  FROM `IncludeBlock` TO `Admonition`,
  FROM `IncludeBlock` TO `AudioObject`,
  FROM `IncludeBlock` TO `CallBlock`,
  FROM `IncludeBlock` TO `Chat`,
  FROM `IncludeBlock` TO `ChatMessage`,
  FROM `IncludeBlock` TO `ChatMessageGroup`,
  FROM `IncludeBlock` TO `Claim`,
  FROM `IncludeBlock` TO `CodeBlock`,
  FROM `IncludeBlock` TO `CodeChunk`,
  FROM `IncludeBlock` TO `Figure`,
  FROM `IncludeBlock` TO `File`,
  FROM `IncludeBlock` TO `ForBlock`,
  FROM `IncludeBlock` TO `Form`,
  FROM `IncludeBlock` TO `Heading`,
  FROM `IncludeBlock` TO `IfBlock`,
  FROM `IncludeBlock` TO `ImageObject`,
  FROM `IncludeBlock` TO `IncludeBlock`,
  FROM `IncludeBlock` TO `InstructionBlock`,
  FROM `IncludeBlock` TO `List`,
  FROM `IncludeBlock` TO `MathBlock`,
  FROM `IncludeBlock` TO `Paragraph`,
  FROM `IncludeBlock` TO `PromptBlock`,
  FROM `IncludeBlock` TO `QuoteBlock`,
  FROM `IncludeBlock` TO `RawBlock`,
  FROM `IncludeBlock` TO `Section`,
  FROM `IncludeBlock` TO `StyledBlock`,
  FROM `IncludeBlock` TO `SuggestionBlock`,
  FROM `IncludeBlock` TO `Table`,
  FROM `IncludeBlock` TO `ThematicBreak`,
  FROM `IncludeBlock` TO `VideoObject`,
  FROM `IncludeBlock` TO `Walkthrough`,
  FROM `InstructionBlock` TO `Admonition`,
  FROM `InstructionBlock` TO `AudioObject`,
  FROM `InstructionBlock` TO `CallBlock`,
  FROM `InstructionBlock` TO `Chat`,
  FROM `InstructionBlock` TO `ChatMessage`,
  FROM `InstructionBlock` TO `ChatMessageGroup`,
  FROM `InstructionBlock` TO `Claim`,
  FROM `InstructionBlock` TO `CodeBlock`,
  FROM `InstructionBlock` TO `CodeChunk`,
  FROM `InstructionBlock` TO `Figure`,
  FROM `InstructionBlock` TO `File`,
  FROM `InstructionBlock` TO `ForBlock`,
  FROM `InstructionBlock` TO `Form`,
  FROM `InstructionBlock` TO `Heading`,
  FROM `InstructionBlock` TO `IfBlock`,
  FROM `InstructionBlock` TO `ImageObject`,
  FROM `InstructionBlock` TO `IncludeBlock`,
  FROM `InstructionBlock` TO `InstructionBlock`,
  FROM `InstructionBlock` TO `List`,
  FROM `InstructionBlock` TO `MathBlock`,
  FROM `InstructionBlock` TO `Paragraph`,
  FROM `InstructionBlock` TO `PromptBlock`,
  FROM `InstructionBlock` TO `QuoteBlock`,
  FROM `InstructionBlock` TO `RawBlock`,
  FROM `InstructionBlock` TO `Section`,
  FROM `InstructionBlock` TO `StyledBlock`,
  FROM `InstructionBlock` TO `SuggestionBlock`,
  FROM `InstructionBlock` TO `Table`,
  FROM `InstructionBlock` TO `ThematicBreak`,
  FROM `InstructionBlock` TO `VideoObject`,
  FROM `InstructionBlock` TO `Walkthrough`,
  FROM `InstructionInline` TO `Annotation`,
  FROM `InstructionInline` TO `AudioObject`,
  FROM `InstructionInline` TO `Button`,
  FROM `InstructionInline` TO `Cite`,
  FROM `InstructionInline` TO `CiteGroup`,
  FROM `InstructionInline` TO `CodeExpression`,
  FROM `InstructionInline` TO `CodeInline`,
  FROM `InstructionInline` TO `Date`,
  FROM `InstructionInline` TO `DateTime`,
  FROM `InstructionInline` TO `Duration`,
  FROM `InstructionInline` TO `Emphasis`,
  FROM `InstructionInline` TO `ImageObject`,
  FROM `InstructionInline` TO `InstructionInline`,
  FROM `InstructionInline` TO `Link`,
  FROM `InstructionInline` TO `MathInline`,
  FROM `InstructionInline` TO `MediaObject`,
  FROM `InstructionInline` TO `Note`,
  FROM `InstructionInline` TO `Parameter`,
  FROM `InstructionInline` TO `QuoteInline`,
  FROM `InstructionInline` TO `StyledInline`,
  FROM `InstructionInline` TO `Strikeout`,
  FROM `InstructionInline` TO `Strong`,
  FROM `InstructionInline` TO `Subscript`,
  FROM `InstructionInline` TO `SuggestionInline`,
  FROM `InstructionInline` TO `Superscript`,
  FROM `InstructionInline` TO `Text`,
  FROM `InstructionInline` TO `Time`,
  FROM `InstructionInline` TO `Timestamp`,
  FROM `InstructionInline` TO `Underline`,
  FROM `InstructionInline` TO `VideoObject`,
  FROM `Link` TO `Annotation`,
  FROM `Link` TO `AudioObject`,
  FROM `Link` TO `Button`,
  FROM `Link` TO `Cite`,
  FROM `Link` TO `CiteGroup`,
  FROM `Link` TO `CodeExpression`,
  FROM `Link` TO `CodeInline`,
  FROM `Link` TO `Date`,
  FROM `Link` TO `DateTime`,
  FROM `Link` TO `Duration`,
  FROM `Link` TO `Emphasis`,
  FROM `Link` TO `ImageObject`,
  FROM `Link` TO `InstructionInline`,
  FROM `Link` TO `Link`,
  FROM `Link` TO `MathInline`,
  FROM `Link` TO `MediaObject`,
  FROM `Link` TO `Note`,
  FROM `Link` TO `Parameter`,
  FROM `Link` TO `QuoteInline`,
  FROM `Link` TO `StyledInline`,
  FROM `Link` TO `Strikeout`,
  FROM `Link` TO `Strong`,
  FROM `Link` TO `Subscript`,
  FROM `Link` TO `SuggestionInline`,
  FROM `Link` TO `Superscript`,
  FROM `Link` TO `Text`,
  FROM `Link` TO `Time`,
  FROM `Link` TO `Timestamp`,
  FROM `Link` TO `Underline`,
  FROM `Link` TO `VideoObject`,
  FROM `ListItem` TO `Admonition`,
  FROM `ListItem` TO `AudioObject`,
  FROM `ListItem` TO `CallBlock`,
  FROM `ListItem` TO `Chat`,
  FROM `ListItem` TO `ChatMessage`,
  FROM `ListItem` TO `ChatMessageGroup`,
  FROM `ListItem` TO `Claim`,
  FROM `ListItem` TO `CodeBlock`,
  FROM `ListItem` TO `CodeChunk`,
  FROM `ListItem` TO `Figure`,
  FROM `ListItem` TO `File`,
  FROM `ListItem` TO `ForBlock`,
  FROM `ListItem` TO `Form`,
  FROM `ListItem` TO `Heading`,
  FROM `ListItem` TO `IfBlock`,
  FROM `ListItem` TO `ImageObject`,
  FROM `ListItem` TO `IncludeBlock`,
  FROM `ListItem` TO `InstructionBlock`,
  FROM `ListItem` TO `List`,
  FROM `ListItem` TO `MathBlock`,
  FROM `ListItem` TO `Paragraph`,
  FROM `ListItem` TO `PromptBlock`,
  FROM `ListItem` TO `QuoteBlock`,
  FROM `ListItem` TO `RawBlock`,
  FROM `ListItem` TO `Section`,
  FROM `ListItem` TO `StyledBlock`,
  FROM `ListItem` TO `SuggestionBlock`,
  FROM `ListItem` TO `Table`,
  FROM `ListItem` TO `ThematicBreak`,
  FROM `ListItem` TO `VideoObject`,
  FROM `ListItem` TO `Walkthrough`,
  FROM `Note` TO `Admonition`,
  FROM `Note` TO `AudioObject`,
  FROM `Note` TO `CallBlock`,
  FROM `Note` TO `Chat`,
  FROM `Note` TO `ChatMessage`,
  FROM `Note` TO `ChatMessageGroup`,
  FROM `Note` TO `Claim`,
  FROM `Note` TO `CodeBlock`,
  FROM `Note` TO `CodeChunk`,
  FROM `Note` TO `Figure`,
  FROM `Note` TO `File`,
  FROM `Note` TO `ForBlock`,
  FROM `Note` TO `Form`,
  FROM `Note` TO `Heading`,
  FROM `Note` TO `IfBlock`,
  FROM `Note` TO `ImageObject`,
  FROM `Note` TO `IncludeBlock`,
  FROM `Note` TO `InstructionBlock`,
  FROM `Note` TO `List`,
  FROM `Note` TO `MathBlock`,
  FROM `Note` TO `Paragraph`,
  FROM `Note` TO `PromptBlock`,
  FROM `Note` TO `QuoteBlock`,
  FROM `Note` TO `RawBlock`,
  FROM `Note` TO `Section`,
  FROM `Note` TO `StyledBlock`,
  FROM `Note` TO `SuggestionBlock`,
  FROM `Note` TO `Table`,
  FROM `Note` TO `ThematicBreak`,
  FROM `Note` TO `VideoObject`,
  FROM `Note` TO `Walkthrough`,
  FROM `Paragraph` TO `Annotation`,
  FROM `Paragraph` TO `AudioObject`,
  FROM `Paragraph` TO `Button`,
  FROM `Paragraph` TO `Cite`,
  FROM `Paragraph` TO `CiteGroup`,
  FROM `Paragraph` TO `CodeExpression`,
  FROM `Paragraph` TO `CodeInline`,
  FROM `Paragraph` TO `Date`,
  FROM `Paragraph` TO `DateTime`,
  FROM `Paragraph` TO `Duration`,
  FROM `Paragraph` TO `Emphasis`,
  FROM `Paragraph` TO `ImageObject`,
  FROM `Paragraph` TO `InstructionInline`,
  FROM `Paragraph` TO `Link`,
  FROM `Paragraph` TO `MathInline`,
  FROM `Paragraph` TO `MediaObject`,
  FROM `Paragraph` TO `Note`,
  FROM `Paragraph` TO `Parameter`,
  FROM `Paragraph` TO `QuoteInline`,
  FROM `Paragraph` TO `StyledInline`,
  FROM `Paragraph` TO `Strikeout`,
  FROM `Paragraph` TO `Strong`,
  FROM `Paragraph` TO `Subscript`,
  FROM `Paragraph` TO `SuggestionInline`,
  FROM `Paragraph` TO `Superscript`,
  FROM `Paragraph` TO `Text`,
  FROM `Paragraph` TO `Time`,
  FROM `Paragraph` TO `Timestamp`,
  FROM `Paragraph` TO `Underline`,
  FROM `Paragraph` TO `VideoObject`,
  FROM `Prompt` TO `Admonition`,
  FROM `Prompt` TO `AudioObject`,
  FROM `Prompt` TO `CallBlock`,
  FROM `Prompt` TO `Chat`,
  FROM `Prompt` TO `ChatMessage`,
  FROM `Prompt` TO `ChatMessageGroup`,
  FROM `Prompt` TO `Claim`,
  FROM `Prompt` TO `CodeBlock`,
  FROM `Prompt` TO `CodeChunk`,
  FROM `Prompt` TO `Figure`,
  FROM `Prompt` TO `File`,
  FROM `Prompt` TO `ForBlock`,
  FROM `Prompt` TO `Form`,
  FROM `Prompt` TO `Heading`,
  FROM `Prompt` TO `IfBlock`,
  FROM `Prompt` TO `ImageObject`,
  FROM `Prompt` TO `IncludeBlock`,
  FROM `Prompt` TO `InstructionBlock`,
  FROM `Prompt` TO `List`,
  FROM `Prompt` TO `MathBlock`,
  FROM `Prompt` TO `Paragraph`,
  FROM `Prompt` TO `PromptBlock`,
  FROM `Prompt` TO `QuoteBlock`,
  FROM `Prompt` TO `RawBlock`,
  FROM `Prompt` TO `Section`,
  FROM `Prompt` TO `StyledBlock`,
  FROM `Prompt` TO `SuggestionBlock`,
  FROM `Prompt` TO `Table`,
  FROM `Prompt` TO `ThematicBreak`,
  FROM `Prompt` TO `VideoObject`,
  FROM `Prompt` TO `Walkthrough`,
  FROM `PromptBlock` TO `Admonition`,
  FROM `PromptBlock` TO `AudioObject`,
  FROM `PromptBlock` TO `CallBlock`,
  FROM `PromptBlock` TO `Chat`,
  FROM `PromptBlock` TO `ChatMessage`,
  FROM `PromptBlock` TO `ChatMessageGroup`,
  FROM `PromptBlock` TO `Claim`,
  FROM `PromptBlock` TO `CodeBlock`,
  FROM `PromptBlock` TO `CodeChunk`,
  FROM `PromptBlock` TO `Figure`,
  FROM `PromptBlock` TO `File`,
  FROM `PromptBlock` TO `ForBlock`,
  FROM `PromptBlock` TO `Form`,
  FROM `PromptBlock` TO `Heading`,
  FROM `PromptBlock` TO `IfBlock`,
  FROM `PromptBlock` TO `ImageObject`,
  FROM `PromptBlock` TO `IncludeBlock`,
  FROM `PromptBlock` TO `InstructionBlock`,
  FROM `PromptBlock` TO `List`,
  FROM `PromptBlock` TO `MathBlock`,
  FROM `PromptBlock` TO `Paragraph`,
  FROM `PromptBlock` TO `PromptBlock`,
  FROM `PromptBlock` TO `QuoteBlock`,
  FROM `PromptBlock` TO `RawBlock`,
  FROM `PromptBlock` TO `Section`,
  FROM `PromptBlock` TO `StyledBlock`,
  FROM `PromptBlock` TO `SuggestionBlock`,
  FROM `PromptBlock` TO `Table`,
  FROM `PromptBlock` TO `ThematicBreak`,
  FROM `PromptBlock` TO `VideoObject`,
  FROM `PromptBlock` TO `Walkthrough`,
  FROM `QuoteBlock` TO `Admonition`,
  FROM `QuoteBlock` TO `AudioObject`,
  FROM `QuoteBlock` TO `CallBlock`,
  FROM `QuoteBlock` TO `Chat`,
  FROM `QuoteBlock` TO `ChatMessage`,
  FROM `QuoteBlock` TO `ChatMessageGroup`,
  FROM `QuoteBlock` TO `Claim`,
  FROM `QuoteBlock` TO `CodeBlock`,
  FROM `QuoteBlock` TO `CodeChunk`,
  FROM `QuoteBlock` TO `Figure`,
  FROM `QuoteBlock` TO `File`,
  FROM `QuoteBlock` TO `ForBlock`,
  FROM `QuoteBlock` TO `Form`,
  FROM `QuoteBlock` TO `Heading`,
  FROM `QuoteBlock` TO `IfBlock`,
  FROM `QuoteBlock` TO `ImageObject`,
  FROM `QuoteBlock` TO `IncludeBlock`,
  FROM `QuoteBlock` TO `InstructionBlock`,
  FROM `QuoteBlock` TO `List`,
  FROM `QuoteBlock` TO `MathBlock`,
  FROM `QuoteBlock` TO `Paragraph`,
  FROM `QuoteBlock` TO `PromptBlock`,
  FROM `QuoteBlock` TO `QuoteBlock`,
  FROM `QuoteBlock` TO `RawBlock`,
  FROM `QuoteBlock` TO `Section`,
  FROM `QuoteBlock` TO `StyledBlock`,
  FROM `QuoteBlock` TO `SuggestionBlock`,
  FROM `QuoteBlock` TO `Table`,
  FROM `QuoteBlock` TO `ThematicBreak`,
  FROM `QuoteBlock` TO `VideoObject`,
  FROM `QuoteBlock` TO `Walkthrough`,
  FROM `QuoteInline` TO `Annotation`,
  FROM `QuoteInline` TO `AudioObject`,
  FROM `QuoteInline` TO `Button`,
  FROM `QuoteInline` TO `Cite`,
  FROM `QuoteInline` TO `CiteGroup`,
  FROM `QuoteInline` TO `CodeExpression`,
  FROM `QuoteInline` TO `CodeInline`,
  FROM `QuoteInline` TO `Date`,
  FROM `QuoteInline` TO `DateTime`,
  FROM `QuoteInline` TO `Duration`,
  FROM `QuoteInline` TO `Emphasis`,
  FROM `QuoteInline` TO `ImageObject`,
  FROM `QuoteInline` TO `InstructionInline`,
  FROM `QuoteInline` TO `Link`,
  FROM `QuoteInline` TO `MathInline`,
  FROM `QuoteInline` TO `MediaObject`,
  FROM `QuoteInline` TO `Note`,
  FROM `QuoteInline` TO `Parameter`,
  FROM `QuoteInline` TO `QuoteInline`,
  FROM `QuoteInline` TO `StyledInline`,
  FROM `QuoteInline` TO `Strikeout`,
  FROM `QuoteInline` TO `Strong`,
  FROM `QuoteInline` TO `Subscript`,
  FROM `QuoteInline` TO `SuggestionInline`,
  FROM `QuoteInline` TO `Superscript`,
  FROM `QuoteInline` TO `Text`,
  FROM `QuoteInline` TO `Time`,
  FROM `QuoteInline` TO `Timestamp`,
  FROM `QuoteInline` TO `Underline`,
  FROM `QuoteInline` TO `VideoObject`,
  FROM `Section` TO `Admonition`,
  FROM `Section` TO `AudioObject`,
  FROM `Section` TO `CallBlock`,
  FROM `Section` TO `Chat`,
  FROM `Section` TO `ChatMessage`,
  FROM `Section` TO `ChatMessageGroup`,
  FROM `Section` TO `Claim`,
  FROM `Section` TO `CodeBlock`,
  FROM `Section` TO `CodeChunk`,
  FROM `Section` TO `Figure`,
  FROM `Section` TO `File`,
  FROM `Section` TO `ForBlock`,
  FROM `Section` TO `Form`,
  FROM `Section` TO `Heading`,
  FROM `Section` TO `IfBlock`,
  FROM `Section` TO `ImageObject`,
  FROM `Section` TO `IncludeBlock`,
  FROM `Section` TO `InstructionBlock`,
  FROM `Section` TO `List`,
  FROM `Section` TO `MathBlock`,
  FROM `Section` TO `Paragraph`,
  FROM `Section` TO `PromptBlock`,
  FROM `Section` TO `QuoteBlock`,
  FROM `Section` TO `RawBlock`,
  FROM `Section` TO `Section`,
  FROM `Section` TO `StyledBlock`,
  FROM `Section` TO `SuggestionBlock`,
  FROM `Section` TO `Table`,
  FROM `Section` TO `ThematicBreak`,
  FROM `Section` TO `VideoObject`,
  FROM `Section` TO `Walkthrough`,
  FROM `Strikeout` TO `Annotation`,
  FROM `Strikeout` TO `AudioObject`,
  FROM `Strikeout` TO `Button`,
  FROM `Strikeout` TO `Cite`,
  FROM `Strikeout` TO `CiteGroup`,
  FROM `Strikeout` TO `CodeExpression`,
  FROM `Strikeout` TO `CodeInline`,
  FROM `Strikeout` TO `Date`,
  FROM `Strikeout` TO `DateTime`,
  FROM `Strikeout` TO `Duration`,
  FROM `Strikeout` TO `Emphasis`,
  FROM `Strikeout` TO `ImageObject`,
  FROM `Strikeout` TO `InstructionInline`,
  FROM `Strikeout` TO `Link`,
  FROM `Strikeout` TO `MathInline`,
  FROM `Strikeout` TO `MediaObject`,
  FROM `Strikeout` TO `Note`,
  FROM `Strikeout` TO `Parameter`,
  FROM `Strikeout` TO `QuoteInline`,
  FROM `Strikeout` TO `StyledInline`,
  FROM `Strikeout` TO `Strikeout`,
  FROM `Strikeout` TO `Strong`,
  FROM `Strikeout` TO `Subscript`,
  FROM `Strikeout` TO `SuggestionInline`,
  FROM `Strikeout` TO `Superscript`,
  FROM `Strikeout` TO `Text`,
  FROM `Strikeout` TO `Time`,
  FROM `Strikeout` TO `Timestamp`,
  FROM `Strikeout` TO `Underline`,
  FROM `Strikeout` TO `VideoObject`,
  FROM `Strong` TO `Annotation`,
  FROM `Strong` TO `AudioObject`,
  FROM `Strong` TO `Button`,
  FROM `Strong` TO `Cite`,
  FROM `Strong` TO `CiteGroup`,
  FROM `Strong` TO `CodeExpression`,
  FROM `Strong` TO `CodeInline`,
  FROM `Strong` TO `Date`,
  FROM `Strong` TO `DateTime`,
  FROM `Strong` TO `Duration`,
  FROM `Strong` TO `Emphasis`,
  FROM `Strong` TO `ImageObject`,
  FROM `Strong` TO `InstructionInline`,
  FROM `Strong` TO `Link`,
  FROM `Strong` TO `MathInline`,
  FROM `Strong` TO `MediaObject`,
  FROM `Strong` TO `Note`,
  FROM `Strong` TO `Parameter`,
  FROM `Strong` TO `QuoteInline`,
  FROM `Strong` TO `StyledInline`,
  FROM `Strong` TO `Strikeout`,
  FROM `Strong` TO `Strong`,
  FROM `Strong` TO `Subscript`,
  FROM `Strong` TO `SuggestionInline`,
  FROM `Strong` TO `Superscript`,
  FROM `Strong` TO `Text`,
  FROM `Strong` TO `Time`,
  FROM `Strong` TO `Timestamp`,
  FROM `Strong` TO `Underline`,
  FROM `Strong` TO `VideoObject`,
  FROM `StyledBlock` TO `Admonition`,
  FROM `StyledBlock` TO `AudioObject`,
  FROM `StyledBlock` TO `CallBlock`,
  FROM `StyledBlock` TO `Chat`,
  FROM `StyledBlock` TO `ChatMessage`,
  FROM `StyledBlock` TO `ChatMessageGroup`,
  FROM `StyledBlock` TO `Claim`,
  FROM `StyledBlock` TO `CodeBlock`,
  FROM `StyledBlock` TO `CodeChunk`,
  FROM `StyledBlock` TO `Figure`,
  FROM `StyledBlock` TO `File`,
  FROM `StyledBlock` TO `ForBlock`,
  FROM `StyledBlock` TO `Form`,
  FROM `StyledBlock` TO `Heading`,
  FROM `StyledBlock` TO `IfBlock`,
  FROM `StyledBlock` TO `ImageObject`,
  FROM `StyledBlock` TO `IncludeBlock`,
  FROM `StyledBlock` TO `InstructionBlock`,
  FROM `StyledBlock` TO `List`,
  FROM `StyledBlock` TO `MathBlock`,
  FROM `StyledBlock` TO `Paragraph`,
  FROM `StyledBlock` TO `PromptBlock`,
  FROM `StyledBlock` TO `QuoteBlock`,
  FROM `StyledBlock` TO `RawBlock`,
  FROM `StyledBlock` TO `Section`,
  FROM `StyledBlock` TO `StyledBlock`,
  FROM `StyledBlock` TO `SuggestionBlock`,
  FROM `StyledBlock` TO `Table`,
  FROM `StyledBlock` TO `ThematicBreak`,
  FROM `StyledBlock` TO `VideoObject`,
  FROM `StyledBlock` TO `Walkthrough`,
  FROM `StyledInline` TO `Annotation`,
  FROM `StyledInline` TO `AudioObject`,
  FROM `StyledInline` TO `Button`,
  FROM `StyledInline` TO `Cite`,
  FROM `StyledInline` TO `CiteGroup`,
  FROM `StyledInline` TO `CodeExpression`,
  FROM `StyledInline` TO `CodeInline`,
  FROM `StyledInline` TO `Date`,
  FROM `StyledInline` TO `DateTime`,
  FROM `StyledInline` TO `Duration`,
  FROM `StyledInline` TO `Emphasis`,
  FROM `StyledInline` TO `ImageObject`,
  FROM `StyledInline` TO `InstructionInline`,
  FROM `StyledInline` TO `Link`,
  FROM `StyledInline` TO `MathInline`,
  FROM `StyledInline` TO `MediaObject`,
  FROM `StyledInline` TO `Note`,
  FROM `StyledInline` TO `Parameter`,
  FROM `StyledInline` TO `QuoteInline`,
  FROM `StyledInline` TO `StyledInline`,
  FROM `StyledInline` TO `Strikeout`,
  FROM `StyledInline` TO `Strong`,
  FROM `StyledInline` TO `Subscript`,
  FROM `StyledInline` TO `SuggestionInline`,
  FROM `StyledInline` TO `Superscript`,
  FROM `StyledInline` TO `Text`,
  FROM `StyledInline` TO `Time`,
  FROM `StyledInline` TO `Timestamp`,
  FROM `StyledInline` TO `Underline`,
  FROM `StyledInline` TO `VideoObject`,
  FROM `Subscript` TO `Annotation`,
  FROM `Subscript` TO `AudioObject`,
  FROM `Subscript` TO `Button`,
  FROM `Subscript` TO `Cite`,
  FROM `Subscript` TO `CiteGroup`,
  FROM `Subscript` TO `CodeExpression`,
  FROM `Subscript` TO `CodeInline`,
  FROM `Subscript` TO `Date`,
  FROM `Subscript` TO `DateTime`,
  FROM `Subscript` TO `Duration`,
  FROM `Subscript` TO `Emphasis`,
  FROM `Subscript` TO `ImageObject`,
  FROM `Subscript` TO `InstructionInline`,
  FROM `Subscript` TO `Link`,
  FROM `Subscript` TO `MathInline`,
  FROM `Subscript` TO `MediaObject`,
  FROM `Subscript` TO `Note`,
  FROM `Subscript` TO `Parameter`,
  FROM `Subscript` TO `QuoteInline`,
  FROM `Subscript` TO `StyledInline`,
  FROM `Subscript` TO `Strikeout`,
  FROM `Subscript` TO `Strong`,
  FROM `Subscript` TO `Subscript`,
  FROM `Subscript` TO `SuggestionInline`,
  FROM `Subscript` TO `Superscript`,
  FROM `Subscript` TO `Text`,
  FROM `Subscript` TO `Time`,
  FROM `Subscript` TO `Timestamp`,
  FROM `Subscript` TO `Underline`,
  FROM `Subscript` TO `VideoObject`,
  FROM `SuggestionBlock` TO `Admonition`,
  FROM `SuggestionBlock` TO `AudioObject`,
  FROM `SuggestionBlock` TO `CallBlock`,
  FROM `SuggestionBlock` TO `Chat`,
  FROM `SuggestionBlock` TO `ChatMessage`,
  FROM `SuggestionBlock` TO `ChatMessageGroup`,
  FROM `SuggestionBlock` TO `Claim`,
  FROM `SuggestionBlock` TO `CodeBlock`,
  FROM `SuggestionBlock` TO `CodeChunk`,
  FROM `SuggestionBlock` TO `Figure`,
  FROM `SuggestionBlock` TO `File`,
  FROM `SuggestionBlock` TO `ForBlock`,
  FROM `SuggestionBlock` TO `Form`,
  FROM `SuggestionBlock` TO `Heading`,
  FROM `SuggestionBlock` TO `IfBlock`,
  FROM `SuggestionBlock` TO `ImageObject`,
  FROM `SuggestionBlock` TO `IncludeBlock`,
  FROM `SuggestionBlock` TO `InstructionBlock`,
  FROM `SuggestionBlock` TO `List`,
  FROM `SuggestionBlock` TO `MathBlock`,
  FROM `SuggestionBlock` TO `Paragraph`,
  FROM `SuggestionBlock` TO `PromptBlock`,
  FROM `SuggestionBlock` TO `QuoteBlock`,
  FROM `SuggestionBlock` TO `RawBlock`,
  FROM `SuggestionBlock` TO `Section`,
  FROM `SuggestionBlock` TO `StyledBlock`,
  FROM `SuggestionBlock` TO `SuggestionBlock`,
  FROM `SuggestionBlock` TO `Table`,
  FROM `SuggestionBlock` TO `ThematicBreak`,
  FROM `SuggestionBlock` TO `VideoObject`,
  FROM `SuggestionBlock` TO `Walkthrough`,
  FROM `SuggestionInline` TO `Annotation`,
  FROM `SuggestionInline` TO `AudioObject`,
  FROM `SuggestionInline` TO `Button`,
  FROM `SuggestionInline` TO `Cite`,
  FROM `SuggestionInline` TO `CiteGroup`,
  FROM `SuggestionInline` TO `CodeExpression`,
  FROM `SuggestionInline` TO `CodeInline`,
  FROM `SuggestionInline` TO `Date`,
  FROM `SuggestionInline` TO `DateTime`,
  FROM `SuggestionInline` TO `Duration`,
  FROM `SuggestionInline` TO `Emphasis`,
  FROM `SuggestionInline` TO `ImageObject`,
  FROM `SuggestionInline` TO `InstructionInline`,
  FROM `SuggestionInline` TO `Link`,
  FROM `SuggestionInline` TO `MathInline`,
  FROM `SuggestionInline` TO `MediaObject`,
  FROM `SuggestionInline` TO `Note`,
  FROM `SuggestionInline` TO `Parameter`,
  FROM `SuggestionInline` TO `QuoteInline`,
  FROM `SuggestionInline` TO `StyledInline`,
  FROM `SuggestionInline` TO `Strikeout`,
  FROM `SuggestionInline` TO `Strong`,
  FROM `SuggestionInline` TO `Subscript`,
  FROM `SuggestionInline` TO `SuggestionInline`,
  FROM `SuggestionInline` TO `Superscript`,
  FROM `SuggestionInline` TO `Text`,
  FROM `SuggestionInline` TO `Time`,
  FROM `SuggestionInline` TO `Timestamp`,
  FROM `SuggestionInline` TO `Underline`,
  FROM `SuggestionInline` TO `VideoObject`,
  FROM `Superscript` TO `Annotation`,
  FROM `Superscript` TO `AudioObject`,
  FROM `Superscript` TO `Button`,
  FROM `Superscript` TO `Cite`,
  FROM `Superscript` TO `CiteGroup`,
  FROM `Superscript` TO `CodeExpression`,
  FROM `Superscript` TO `CodeInline`,
  FROM `Superscript` TO `Date`,
  FROM `Superscript` TO `DateTime`,
  FROM `Superscript` TO `Duration`,
  FROM `Superscript` TO `Emphasis`,
  FROM `Superscript` TO `ImageObject`,
  FROM `Superscript` TO `InstructionInline`,
  FROM `Superscript` TO `Link`,
  FROM `Superscript` TO `MathInline`,
  FROM `Superscript` TO `MediaObject`,
  FROM `Superscript` TO `Note`,
  FROM `Superscript` TO `Parameter`,
  FROM `Superscript` TO `QuoteInline`,
  FROM `Superscript` TO `StyledInline`,
  FROM `Superscript` TO `Strikeout`,
  FROM `Superscript` TO `Strong`,
  FROM `Superscript` TO `Subscript`,
  FROM `Superscript` TO `SuggestionInline`,
  FROM `Superscript` TO `Superscript`,
  FROM `Superscript` TO `Text`,
  FROM `Superscript` TO `Time`,
  FROM `Superscript` TO `Timestamp`,
  FROM `Superscript` TO `Underline`,
  FROM `Superscript` TO `VideoObject`,
  FROM `TableCell` TO `Admonition`,
  FROM `TableCell` TO `AudioObject`,
  FROM `TableCell` TO `CallBlock`,
  FROM `TableCell` TO `Chat`,
  FROM `TableCell` TO `ChatMessage`,
  FROM `TableCell` TO `ChatMessageGroup`,
  FROM `TableCell` TO `Claim`,
  FROM `TableCell` TO `CodeBlock`,
  FROM `TableCell` TO `CodeChunk`,
  FROM `TableCell` TO `Figure`,
  FROM `TableCell` TO `File`,
  FROM `TableCell` TO `ForBlock`,
  FROM `TableCell` TO `Form`,
  FROM `TableCell` TO `Heading`,
  FROM `TableCell` TO `IfBlock`,
  FROM `TableCell` TO `ImageObject`,
  FROM `TableCell` TO `IncludeBlock`,
  FROM `TableCell` TO `InstructionBlock`,
  FROM `TableCell` TO `List`,
  FROM `TableCell` TO `MathBlock`,
  FROM `TableCell` TO `Paragraph`,
  FROM `TableCell` TO `PromptBlock`,
  FROM `TableCell` TO `QuoteBlock`,
  FROM `TableCell` TO `RawBlock`,
  FROM `TableCell` TO `Section`,
  FROM `TableCell` TO `StyledBlock`,
  FROM `TableCell` TO `SuggestionBlock`,
  FROM `TableCell` TO `Table`,
  FROM `TableCell` TO `ThematicBreak`,
  FROM `TableCell` TO `VideoObject`,
  FROM `TableCell` TO `Walkthrough`,
  FROM `Underline` TO `Annotation`,
  FROM `Underline` TO `AudioObject`,
  FROM `Underline` TO `Button`,
  FROM `Underline` TO `Cite`,
  FROM `Underline` TO `CiteGroup`,
  FROM `Underline` TO `CodeExpression`,
  FROM `Underline` TO `CodeInline`,
  FROM `Underline` TO `Date`,
  FROM `Underline` TO `DateTime`,
  FROM `Underline` TO `Duration`,
  FROM `Underline` TO `Emphasis`,
  FROM `Underline` TO `ImageObject`,
  FROM `Underline` TO `InstructionInline`,
  FROM `Underline` TO `Link`,
  FROM `Underline` TO `MathInline`,
  FROM `Underline` TO `MediaObject`,
  FROM `Underline` TO `Note`,
  FROM `Underline` TO `Parameter`,
  FROM `Underline` TO `QuoteInline`,
  FROM `Underline` TO `StyledInline`,
  FROM `Underline` TO `Strikeout`,
  FROM `Underline` TO `Strong`,
  FROM `Underline` TO `Subscript`,
  FROM `Underline` TO `SuggestionInline`,
  FROM `Underline` TO `Superscript`,
  FROM `Underline` TO `Text`,
  FROM `Underline` TO `Time`,
  FROM `Underline` TO `Timestamp`,
  FROM `Underline` TO `Underline`,
  FROM `Underline` TO `VideoObject`,
  FROM `WalkthroughStep` TO `Admonition`,
  FROM `WalkthroughStep` TO `AudioObject`,
  FROM `WalkthroughStep` TO `CallBlock`,
  FROM `WalkthroughStep` TO `Chat`,
  FROM `WalkthroughStep` TO `ChatMessage`,
  FROM `WalkthroughStep` TO `ChatMessageGroup`,
  FROM `WalkthroughStep` TO `Claim`,
  FROM `WalkthroughStep` TO `CodeBlock`,
  FROM `WalkthroughStep` TO `CodeChunk`,
  FROM `WalkthroughStep` TO `Figure`,
  FROM `WalkthroughStep` TO `File`,
  FROM `WalkthroughStep` TO `ForBlock`,
  FROM `WalkthroughStep` TO `Form`,
  FROM `WalkthroughStep` TO `Heading`,
  FROM `WalkthroughStep` TO `IfBlock`,
  FROM `WalkthroughStep` TO `ImageObject`,
  FROM `WalkthroughStep` TO `IncludeBlock`,
  FROM `WalkthroughStep` TO `InstructionBlock`,
  FROM `WalkthroughStep` TO `List`,
  FROM `WalkthroughStep` TO `MathBlock`,
  FROM `WalkthroughStep` TO `Paragraph`,
  FROM `WalkthroughStep` TO `PromptBlock`,
  FROM `WalkthroughStep` TO `QuoteBlock`,
  FROM `WalkthroughStep` TO `RawBlock`,
  FROM `WalkthroughStep` TO `Section`,
  FROM `WalkthroughStep` TO `StyledBlock`,
  FROM `WalkthroughStep` TO `SuggestionBlock`,
  FROM `WalkthroughStep` TO `Table`,
  FROM `WalkthroughStep` TO `ThematicBreak`,
  FROM `WalkthroughStep` TO `VideoObject`,
  FROM `WalkthroughStep` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `contributors` (
  FROM `Article` TO `Person`,
  FROM `Article` TO `Organization`,
  FROM `Article` TO `SoftwareApplication`,
  FROM `Article` TO `AuthorRole`,
  FROM `AudioObject` TO `Person`,
  FROM `AudioObject` TO `Organization`,
  FROM `AudioObject` TO `SoftwareApplication`,
  FROM `AudioObject` TO `AuthorRole`,
  FROM `Chat` TO `Person`,
  FROM `Chat` TO `Organization`,
  FROM `Chat` TO `SoftwareApplication`,
  FROM `Chat` TO `AuthorRole`,
  FROM `Claim` TO `Person`,
  FROM `Claim` TO `Organization`,
  FROM `Claim` TO `SoftwareApplication`,
  FROM `Claim` TO `AuthorRole`,
  FROM `Collection` TO `Person`,
  FROM `Collection` TO `Organization`,
  FROM `Collection` TO `SoftwareApplication`,
  FROM `Collection` TO `AuthorRole`,
  FROM `Comment` TO `Person`,
  FROM `Comment` TO `Organization`,
  FROM `Comment` TO `SoftwareApplication`,
  FROM `Comment` TO `AuthorRole`,
  FROM `CreativeWork` TO `Person`,
  FROM `CreativeWork` TO `Organization`,
  FROM `CreativeWork` TO `SoftwareApplication`,
  FROM `CreativeWork` TO `AuthorRole`,
  FROM `Datatable` TO `Person`,
  FROM `Datatable` TO `Organization`,
  FROM `Datatable` TO `SoftwareApplication`,
  FROM `Datatable` TO `AuthorRole`,
  FROM `Figure` TO `Person`,
  FROM `Figure` TO `Organization`,
  FROM `Figure` TO `SoftwareApplication`,
  FROM `Figure` TO `AuthorRole`,
  FROM `ImageObject` TO `Person`,
  FROM `ImageObject` TO `Organization`,
  FROM `ImageObject` TO `SoftwareApplication`,
  FROM `ImageObject` TO `AuthorRole`,
  FROM `MediaObject` TO `Person`,
  FROM `MediaObject` TO `Organization`,
  FROM `MediaObject` TO `SoftwareApplication`,
  FROM `MediaObject` TO `AuthorRole`,
  FROM `Periodical` TO `Person`,
  FROM `Periodical` TO `Organization`,
  FROM `Periodical` TO `SoftwareApplication`,
  FROM `Periodical` TO `AuthorRole`,
  FROM `Prompt` TO `Person`,
  FROM `Prompt` TO `Organization`,
  FROM `Prompt` TO `SoftwareApplication`,
  FROM `Prompt` TO `AuthorRole`,
  FROM `PublicationIssue` TO `Person`,
  FROM `PublicationIssue` TO `Organization`,
  FROM `PublicationIssue` TO `SoftwareApplication`,
  FROM `PublicationIssue` TO `AuthorRole`,
  FROM `PublicationVolume` TO `Person`,
  FROM `PublicationVolume` TO `Organization`,
  FROM `PublicationVolume` TO `SoftwareApplication`,
  FROM `PublicationVolume` TO `AuthorRole`,
  FROM `Review` TO `Person`,
  FROM `Review` TO `Organization`,
  FROM `Review` TO `SoftwareApplication`,
  FROM `Review` TO `AuthorRole`,
  FROM `SoftwareApplication` TO `Person`,
  FROM `SoftwareApplication` TO `Organization`,
  FROM `SoftwareApplication` TO `SoftwareApplication`,
  FROM `SoftwareApplication` TO `AuthorRole`,
  FROM `SoftwareSourceCode` TO `Person`,
  FROM `SoftwareSourceCode` TO `Organization`,
  FROM `SoftwareSourceCode` TO `SoftwareApplication`,
  FROM `SoftwareSourceCode` TO `AuthorRole`,
  FROM `Table` TO `Person`,
  FROM `Table` TO `Organization`,
  FROM `Table` TO `SoftwareApplication`,
  FROM `Table` TO `AuthorRole`,
  FROM `VideoObject` TO `Person`,
  FROM `VideoObject` TO `Organization`,
  FROM `VideoObject` TO `SoftwareApplication`,
  FROM `VideoObject` TO `AuthorRole`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `departments` (
  FROM `Organization` TO `Organization`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `editors` (
  FROM `Article` TO `Person`,
  FROM `AudioObject` TO `Person`,
  FROM `Chat` TO `Person`,
  FROM `Claim` TO `Person`,
  FROM `Collection` TO `Person`,
  FROM `Comment` TO `Person`,
  FROM `CreativeWork` TO `Person`,
  FROM `Datatable` TO `Person`,
  FROM `Figure` TO `Person`,
  FROM `ImageObject` TO `Person`,
  FROM `MediaObject` TO `Person`,
  FROM `Periodical` TO `Person`,
  FROM `Prompt` TO `Person`,
  FROM `PublicationIssue` TO `Person`,
  FROM `PublicationVolume` TO `Person`,
  FROM `Review` TO `Person`,
  FROM `SoftwareApplication` TO `Person`,
  FROM `SoftwareSourceCode` TO `Person`,
  FROM `Table` TO `Person`,
  FROM `VideoObject` TO `Person`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `executionDependants` (
  FROM `Article` TO `ExecutionDependant`,
  FROM `Button` TO `ExecutionDependant`,
  FROM `CallArgument` TO `ExecutionDependant`,
  FROM `CallBlock` TO `ExecutionDependant`,
  FROM `Chat` TO `ExecutionDependant`,
  FROM `ChatMessage` TO `ExecutionDependant`,
  FROM `CodeChunk` TO `ExecutionDependant`,
  FROM `CodeExpression` TO `ExecutionDependant`,
  FROM `ForBlock` TO `ExecutionDependant`,
  FROM `Form` TO `ExecutionDependant`,
  FROM `IfBlock` TO `ExecutionDependant`,
  FROM `IfBlockClause` TO `ExecutionDependant`,
  FROM `IncludeBlock` TO `ExecutionDependant`,
  FROM `InstructionBlock` TO `ExecutionDependant`,
  FROM `InstructionInline` TO `ExecutionDependant`,
  FROM `Parameter` TO `ExecutionDependant`,
  FROM `Prompt` TO `ExecutionDependant`,
  FROM `PromptBlock` TO `ExecutionDependant`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `executionDependencies` (
  FROM `Article` TO `ExecutionDependency`,
  FROM `Button` TO `ExecutionDependency`,
  FROM `CallArgument` TO `ExecutionDependency`,
  FROM `CallBlock` TO `ExecutionDependency`,
  FROM `Chat` TO `ExecutionDependency`,
  FROM `ChatMessage` TO `ExecutionDependency`,
  FROM `CodeChunk` TO `ExecutionDependency`,
  FROM `CodeExpression` TO `ExecutionDependency`,
  FROM `ForBlock` TO `ExecutionDependency`,
  FROM `Form` TO `ExecutionDependency`,
  FROM `IfBlock` TO `ExecutionDependency`,
  FROM `IfBlockClause` TO `ExecutionDependency`,
  FROM `IncludeBlock` TO `ExecutionDependency`,
  FROM `InstructionBlock` TO `ExecutionDependency`,
  FROM `InstructionInline` TO `ExecutionDependency`,
  FROM `Parameter` TO `ExecutionDependency`,
  FROM `Prompt` TO `ExecutionDependency`,
  FROM `PromptBlock` TO `ExecutionDependency`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `executionMessages` (
  FROM `Article` TO `ExecutionMessage`,
  FROM `Button` TO `ExecutionMessage`,
  FROM `CallArgument` TO `ExecutionMessage`,
  FROM `CallBlock` TO `ExecutionMessage`,
  FROM `Chat` TO `ExecutionMessage`,
  FROM `ChatMessage` TO `ExecutionMessage`,
  FROM `CodeChunk` TO `ExecutionMessage`,
  FROM `CodeExpression` TO `ExecutionMessage`,
  FROM `ForBlock` TO `ExecutionMessage`,
  FROM `Form` TO `ExecutionMessage`,
  FROM `IfBlock` TO `ExecutionMessage`,
  FROM `IfBlockClause` TO `ExecutionMessage`,
  FROM `IncludeBlock` TO `ExecutionMessage`,
  FROM `InstructionBlock` TO `ExecutionMessage`,
  FROM `InstructionInline` TO `ExecutionMessage`,
  FROM `Parameter` TO `ExecutionMessage`,
  FROM `Prompt` TO `ExecutionMessage`,
  FROM `PromptBlock` TO `ExecutionMessage`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `executionTags` (
  FROM `Article` TO `ExecutionTag`,
  FROM `Button` TO `ExecutionTag`,
  FROM `CallArgument` TO `ExecutionTag`,
  FROM `CallBlock` TO `ExecutionTag`,
  FROM `Chat` TO `ExecutionTag`,
  FROM `ChatMessage` TO `ExecutionTag`,
  FROM `CodeChunk` TO `ExecutionTag`,
  FROM `CodeExpression` TO `ExecutionTag`,
  FROM `ForBlock` TO `ExecutionTag`,
  FROM `Form` TO `ExecutionTag`,
  FROM `IfBlock` TO `ExecutionTag`,
  FROM `IfBlockClause` TO `ExecutionTag`,
  FROM `IncludeBlock` TO `ExecutionTag`,
  FROM `InstructionBlock` TO `ExecutionTag`,
  FROM `InstructionInline` TO `ExecutionTag`,
  FROM `Parameter` TO `ExecutionTag`,
  FROM `Prompt` TO `ExecutionTag`,
  FROM `PromptBlock` TO `ExecutionTag`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `files` (
  FROM `ChatMessage` TO `File`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `images` (
  FROM `Article` TO `ImageObject`,
  FROM `AudioObject` TO `ImageObject`,
  FROM `Brand` TO `ImageObject`,
  FROM `Chat` TO `ImageObject`,
  FROM `Claim` TO `ImageObject`,
  FROM `Collection` TO `ImageObject`,
  FROM `Comment` TO `ImageObject`,
  FROM `ContactPoint` TO `ImageObject`,
  FROM `CreativeWork` TO `ImageObject`,
  FROM `Datatable` TO `ImageObject`,
  FROM `DefinedTerm` TO `ImageObject`,
  FROM `Enumeration` TO `ImageObject`,
  FROM `Figure` TO `ImageObject`,
  FROM `Grant` TO `ImageObject`,
  FROM `ImageObject` TO `ImageObject`,
  FROM `ListItem` TO `ImageObject`,
  FROM `MediaObject` TO `ImageObject`,
  FROM `MonetaryGrant` TO `ImageObject`,
  FROM `Organization` TO `ImageObject`,
  FROM `Periodical` TO `ImageObject`,
  FROM `Person` TO `ImageObject`,
  FROM `PostalAddress` TO `ImageObject`,
  FROM `Product` TO `ImageObject`,
  FROM `Prompt` TO `ImageObject`,
  FROM `PropertyValue` TO `ImageObject`,
  FROM `PublicationIssue` TO `ImageObject`,
  FROM `PublicationVolume` TO `ImageObject`,
  FROM `Review` TO `ImageObject`,
  FROM `SoftwareApplication` TO `ImageObject`,
  FROM `SoftwareSourceCode` TO `ImageObject`,
  FROM `Table` TO `ImageObject`,
  FROM `Thing` TO `ImageObject`,
  FROM `VideoObject` TO `ImageObject`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `items` (
  FROM `CiteGroup` TO `Cite`,
  FROM `List` TO `ListItem`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `iterations` (
  FROM `ForBlock` TO `Admonition`,
  FROM `ForBlock` TO `AudioObject`,
  FROM `ForBlock` TO `CallBlock`,
  FROM `ForBlock` TO `Chat`,
  FROM `ForBlock` TO `ChatMessage`,
  FROM `ForBlock` TO `ChatMessageGroup`,
  FROM `ForBlock` TO `Claim`,
  FROM `ForBlock` TO `CodeBlock`,
  FROM `ForBlock` TO `CodeChunk`,
  FROM `ForBlock` TO `Figure`,
  FROM `ForBlock` TO `File`,
  FROM `ForBlock` TO `ForBlock`,
  FROM `ForBlock` TO `Form`,
  FROM `ForBlock` TO `Heading`,
  FROM `ForBlock` TO `IfBlock`,
  FROM `ForBlock` TO `ImageObject`,
  FROM `ForBlock` TO `IncludeBlock`,
  FROM `ForBlock` TO `InstructionBlock`,
  FROM `ForBlock` TO `List`,
  FROM `ForBlock` TO `MathBlock`,
  FROM `ForBlock` TO `Paragraph`,
  FROM `ForBlock` TO `PromptBlock`,
  FROM `ForBlock` TO `QuoteBlock`,
  FROM `ForBlock` TO `RawBlock`,
  FROM `ForBlock` TO `Section`,
  FROM `ForBlock` TO `StyledBlock`,
  FROM `ForBlock` TO `SuggestionBlock`,
  FROM `ForBlock` TO `Table`,
  FROM `ForBlock` TO `ThematicBreak`,
  FROM `ForBlock` TO `VideoObject`,
  FROM `ForBlock` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `memberOf` (
  FROM `Person` TO `Organization`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `messages` (
  FROM `ChatMessageGroup` TO `ChatMessage`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `notes` (
  FROM `Table` TO `Admonition`,
  FROM `Table` TO `AudioObject`,
  FROM `Table` TO `CallBlock`,
  FROM `Table` TO `Chat`,
  FROM `Table` TO `ChatMessage`,
  FROM `Table` TO `ChatMessageGroup`,
  FROM `Table` TO `Claim`,
  FROM `Table` TO `CodeBlock`,
  FROM `Table` TO `CodeChunk`,
  FROM `Table` TO `Figure`,
  FROM `Table` TO `File`,
  FROM `Table` TO `ForBlock`,
  FROM `Table` TO `Form`,
  FROM `Table` TO `Heading`,
  FROM `Table` TO `IfBlock`,
  FROM `Table` TO `ImageObject`,
  FROM `Table` TO `IncludeBlock`,
  FROM `Table` TO `InstructionBlock`,
  FROM `Table` TO `List`,
  FROM `Table` TO `MathBlock`,
  FROM `Table` TO `Paragraph`,
  FROM `Table` TO `PromptBlock`,
  FROM `Table` TO `QuoteBlock`,
  FROM `Table` TO `RawBlock`,
  FROM `Table` TO `Section`,
  FROM `Table` TO `StyledBlock`,
  FROM `Table` TO `SuggestionBlock`,
  FROM `Table` TO `Table`,
  FROM `Table` TO `ThematicBreak`,
  FROM `Table` TO `VideoObject`,
  FROM `Table` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `otherwise` (
  FROM `ForBlock` TO `Admonition`,
  FROM `ForBlock` TO `AudioObject`,
  FROM `ForBlock` TO `CallBlock`,
  FROM `ForBlock` TO `Chat`,
  FROM `ForBlock` TO `ChatMessage`,
  FROM `ForBlock` TO `ChatMessageGroup`,
  FROM `ForBlock` TO `Claim`,
  FROM `ForBlock` TO `CodeBlock`,
  FROM `ForBlock` TO `CodeChunk`,
  FROM `ForBlock` TO `Figure`,
  FROM `ForBlock` TO `File`,
  FROM `ForBlock` TO `ForBlock`,
  FROM `ForBlock` TO `Form`,
  FROM `ForBlock` TO `Heading`,
  FROM `ForBlock` TO `IfBlock`,
  FROM `ForBlock` TO `ImageObject`,
  FROM `ForBlock` TO `IncludeBlock`,
  FROM `ForBlock` TO `InstructionBlock`,
  FROM `ForBlock` TO `List`,
  FROM `ForBlock` TO `MathBlock`,
  FROM `ForBlock` TO `Paragraph`,
  FROM `ForBlock` TO `PromptBlock`,
  FROM `ForBlock` TO `QuoteBlock`,
  FROM `ForBlock` TO `RawBlock`,
  FROM `ForBlock` TO `Section`,
  FROM `ForBlock` TO `StyledBlock`,
  FROM `ForBlock` TO `SuggestionBlock`,
  FROM `ForBlock` TO `Table`,
  FROM `ForBlock` TO `ThematicBreak`,
  FROM `ForBlock` TO `VideoObject`,
  FROM `ForBlock` TO `Walkthrough`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `parameters` (
  FROM `Function` TO `Parameter`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `provenance` (
  FROM `Admonition` TO `ProvenanceCount`,
  FROM `Article` TO `ProvenanceCount`,
  FROM `AudioObject` TO `ProvenanceCount`,
  FROM `Button` TO `ProvenanceCount`,
  FROM `Chat` TO `ProvenanceCount`,
  FROM `Claim` TO `ProvenanceCount`,
  FROM `CodeBlock` TO `ProvenanceCount`,
  FROM `CodeChunk` TO `ProvenanceCount`,
  FROM `CodeExpression` TO `ProvenanceCount`,
  FROM `CodeInline` TO `ProvenanceCount`,
  FROM `Collection` TO `ProvenanceCount`,
  FROM `Comment` TO `ProvenanceCount`,
  FROM `CreativeWork` TO `ProvenanceCount`,
  FROM `Datatable` TO `ProvenanceCount`,
  FROM `Figure` TO `ProvenanceCount`,
  FROM `ForBlock` TO `ProvenanceCount`,
  FROM `Heading` TO `ProvenanceCount`,
  FROM `IfBlockClause` TO `ProvenanceCount`,
  FROM `ImageObject` TO `ProvenanceCount`,
  FROM `InstructionMessage` TO `ProvenanceCount`,
  FROM `List` TO `ProvenanceCount`,
  FROM `MathBlock` TO `ProvenanceCount`,
  FROM `MathInline` TO `ProvenanceCount`,
  FROM `MediaObject` TO `ProvenanceCount`,
  FROM `Paragraph` TO `ProvenanceCount`,
  FROM `Periodical` TO `ProvenanceCount`,
  FROM `Prompt` TO `ProvenanceCount`,
  FROM `PublicationIssue` TO `ProvenanceCount`,
  FROM `PublicationVolume` TO `ProvenanceCount`,
  FROM `QuoteBlock` TO `ProvenanceCount`,
  FROM `RawBlock` TO `ProvenanceCount`,
  FROM `Review` TO `ProvenanceCount`,
  FROM `Section` TO `ProvenanceCount`,
  FROM `SoftwareApplication` TO `ProvenanceCount`,
  FROM `SoftwareSourceCode` TO `ProvenanceCount`,
  FROM `StyledBlock` TO `ProvenanceCount`,
  FROM `StyledInline` TO `ProvenanceCount`,
  FROM `SuggestionBlock` TO `ProvenanceCount`,
  FROM `SuggestionInline` TO `ProvenanceCount`,
  FROM `Table` TO `ProvenanceCount`,
  FROM `VideoObject` TO `ProvenanceCount`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `rows` (
  FROM `Table` TO `TableRow`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `softwareRequirements` (
  FROM `SoftwareApplication` TO `SoftwareApplication`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `steps` (
  FROM `Walkthrough` TO `WalkthroughStep`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `suggestions` (
  FROM `InstructionBlock` TO `SuggestionBlock`,
  FROM `InstructionInline` TO `SuggestionInline`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `targetProducts` (
  FROM `SoftwareSourceCode` TO `SoftwareApplication`,
  `index` UINT32,
  ONE_MANY
);

CREATE REL TABLE IF NOT EXISTS `title` (
  FROM `Admonition` TO `Annotation`,
  FROM `Admonition` TO `AudioObject`,
  FROM `Admonition` TO `Button`,
  FROM `Admonition` TO `Cite`,
  FROM `Admonition` TO `CiteGroup`,
  FROM `Admonition` TO `CodeExpression`,
  FROM `Admonition` TO `CodeInline`,
  FROM `Admonition` TO `Date`,
  FROM `Admonition` TO `DateTime`,
  FROM `Admonition` TO `Duration`,
  FROM `Admonition` TO `Emphasis`,
  FROM `Admonition` TO `ImageObject`,
  FROM `Admonition` TO `InstructionInline`,
  FROM `Admonition` TO `Link`,
  FROM `Admonition` TO `MathInline`,
  FROM `Admonition` TO `MediaObject`,
  FROM `Admonition` TO `Note`,
  FROM `Admonition` TO `Parameter`,
  FROM `Admonition` TO `QuoteInline`,
  FROM `Admonition` TO `StyledInline`,
  FROM `Admonition` TO `Strikeout`,
  FROM `Admonition` TO `Strong`,
  FROM `Admonition` TO `Subscript`,
  FROM `Admonition` TO `SuggestionInline`,
  FROM `Admonition` TO `Superscript`,
  FROM `Admonition` TO `Text`,
  FROM `Admonition` TO `Time`,
  FROM `Admonition` TO `Timestamp`,
  FROM `Admonition` TO `Underline`,
  FROM `Admonition` TO `VideoObject`,
  FROM `Article` TO `Annotation`,
  FROM `Article` TO `AudioObject`,
  FROM `Article` TO `Button`,
  FROM `Article` TO `Cite`,
  FROM `Article` TO `CiteGroup`,
  FROM `Article` TO `CodeExpression`,
  FROM `Article` TO `CodeInline`,
  FROM `Article` TO `Date`,
  FROM `Article` TO `DateTime`,
  FROM `Article` TO `Duration`,
  FROM `Article` TO `Emphasis`,
  FROM `Article` TO `ImageObject`,
  FROM `Article` TO `InstructionInline`,
  FROM `Article` TO `Link`,
  FROM `Article` TO `MathInline`,
  FROM `Article` TO `MediaObject`,
  FROM `Article` TO `Note`,
  FROM `Article` TO `Parameter`,
  FROM `Article` TO `QuoteInline`,
  FROM `Article` TO `StyledInline`,
  FROM `Article` TO `Strikeout`,
  FROM `Article` TO `Strong`,
  FROM `Article` TO `Subscript`,
  FROM `Article` TO `SuggestionInline`,
  FROM `Article` TO `Superscript`,
  FROM `Article` TO `Text`,
  FROM `Article` TO `Time`,
  FROM `Article` TO `Timestamp`,
  FROM `Article` TO `Underline`,
  FROM `Article` TO `VideoObject`,
  FROM `AudioObject` TO `Annotation`,
  FROM `AudioObject` TO `AudioObject`,
  FROM `AudioObject` TO `Button`,
  FROM `AudioObject` TO `Cite`,
  FROM `AudioObject` TO `CiteGroup`,
  FROM `AudioObject` TO `CodeExpression`,
  FROM `AudioObject` TO `CodeInline`,
  FROM `AudioObject` TO `Date`,
  FROM `AudioObject` TO `DateTime`,
  FROM `AudioObject` TO `Duration`,
  FROM `AudioObject` TO `Emphasis`,
  FROM `AudioObject` TO `ImageObject`,
  FROM `AudioObject` TO `InstructionInline`,
  FROM `AudioObject` TO `Link`,
  FROM `AudioObject` TO `MathInline`,
  FROM `AudioObject` TO `MediaObject`,
  FROM `AudioObject` TO `Note`,
  FROM `AudioObject` TO `Parameter`,
  FROM `AudioObject` TO `QuoteInline`,
  FROM `AudioObject` TO `StyledInline`,
  FROM `AudioObject` TO `Strikeout`,
  FROM `AudioObject` TO `Strong`,
  FROM `AudioObject` TO `Subscript`,
  FROM `AudioObject` TO `SuggestionInline`,
  FROM `AudioObject` TO `Superscript`,
  FROM `AudioObject` TO `Text`,
  FROM `AudioObject` TO `Time`,
  FROM `AudioObject` TO `Timestamp`,
  FROM `AudioObject` TO `Underline`,
  FROM `AudioObject` TO `VideoObject`,
  FROM `Chat` TO `Annotation`,
  FROM `Chat` TO `AudioObject`,
  FROM `Chat` TO `Button`,
  FROM `Chat` TO `Cite`,
  FROM `Chat` TO `CiteGroup`,
  FROM `Chat` TO `CodeExpression`,
  FROM `Chat` TO `CodeInline`,
  FROM `Chat` TO `Date`,
  FROM `Chat` TO `DateTime`,
  FROM `Chat` TO `Duration`,
  FROM `Chat` TO `Emphasis`,
  FROM `Chat` TO `ImageObject`,
  FROM `Chat` TO `InstructionInline`,
  FROM `Chat` TO `Link`,
  FROM `Chat` TO `MathInline`,
  FROM `Chat` TO `MediaObject`,
  FROM `Chat` TO `Note`,
  FROM `Chat` TO `Parameter`,
  FROM `Chat` TO `QuoteInline`,
  FROM `Chat` TO `StyledInline`,
  FROM `Chat` TO `Strikeout`,
  FROM `Chat` TO `Strong`,
  FROM `Chat` TO `Subscript`,
  FROM `Chat` TO `SuggestionInline`,
  FROM `Chat` TO `Superscript`,
  FROM `Chat` TO `Text`,
  FROM `Chat` TO `Time`,
  FROM `Chat` TO `Timestamp`,
  FROM `Chat` TO `Underline`,
  FROM `Chat` TO `VideoObject`,
  FROM `Claim` TO `Annotation`,
  FROM `Claim` TO `AudioObject`,
  FROM `Claim` TO `Button`,
  FROM `Claim` TO `Cite`,
  FROM `Claim` TO `CiteGroup`,
  FROM `Claim` TO `CodeExpression`,
  FROM `Claim` TO `CodeInline`,
  FROM `Claim` TO `Date`,
  FROM `Claim` TO `DateTime`,
  FROM `Claim` TO `Duration`,
  FROM `Claim` TO `Emphasis`,
  FROM `Claim` TO `ImageObject`,
  FROM `Claim` TO `InstructionInline`,
  FROM `Claim` TO `Link`,
  FROM `Claim` TO `MathInline`,
  FROM `Claim` TO `MediaObject`,
  FROM `Claim` TO `Note`,
  FROM `Claim` TO `Parameter`,
  FROM `Claim` TO `QuoteInline`,
  FROM `Claim` TO `StyledInline`,
  FROM `Claim` TO `Strikeout`,
  FROM `Claim` TO `Strong`,
  FROM `Claim` TO `Subscript`,
  FROM `Claim` TO `SuggestionInline`,
  FROM `Claim` TO `Superscript`,
  FROM `Claim` TO `Text`,
  FROM `Claim` TO `Time`,
  FROM `Claim` TO `Timestamp`,
  FROM `Claim` TO `Underline`,
  FROM `Claim` TO `VideoObject`,
  FROM `Collection` TO `Annotation`,
  FROM `Collection` TO `AudioObject`,
  FROM `Collection` TO `Button`,
  FROM `Collection` TO `Cite`,
  FROM `Collection` TO `CiteGroup`,
  FROM `Collection` TO `CodeExpression`,
  FROM `Collection` TO `CodeInline`,
  FROM `Collection` TO `Date`,
  FROM `Collection` TO `DateTime`,
  FROM `Collection` TO `Duration`,
  FROM `Collection` TO `Emphasis`,
  FROM `Collection` TO `ImageObject`,
  FROM `Collection` TO `InstructionInline`,
  FROM `Collection` TO `Link`,
  FROM `Collection` TO `MathInline`,
  FROM `Collection` TO `MediaObject`,
  FROM `Collection` TO `Note`,
  FROM `Collection` TO `Parameter`,
  FROM `Collection` TO `QuoteInline`,
  FROM `Collection` TO `StyledInline`,
  FROM `Collection` TO `Strikeout`,
  FROM `Collection` TO `Strong`,
  FROM `Collection` TO `Subscript`,
  FROM `Collection` TO `SuggestionInline`,
  FROM `Collection` TO `Superscript`,
  FROM `Collection` TO `Text`,
  FROM `Collection` TO `Time`,
  FROM `Collection` TO `Timestamp`,
  FROM `Collection` TO `Underline`,
  FROM `Collection` TO `VideoObject`,
  FROM `Comment` TO `Annotation`,
  FROM `Comment` TO `AudioObject`,
  FROM `Comment` TO `Button`,
  FROM `Comment` TO `Cite`,
  FROM `Comment` TO `CiteGroup`,
  FROM `Comment` TO `CodeExpression`,
  FROM `Comment` TO `CodeInline`,
  FROM `Comment` TO `Date`,
  FROM `Comment` TO `DateTime`,
  FROM `Comment` TO `Duration`,
  FROM `Comment` TO `Emphasis`,
  FROM `Comment` TO `ImageObject`,
  FROM `Comment` TO `InstructionInline`,
  FROM `Comment` TO `Link`,
  FROM `Comment` TO `MathInline`,
  FROM `Comment` TO `MediaObject`,
  FROM `Comment` TO `Note`,
  FROM `Comment` TO `Parameter`,
  FROM `Comment` TO `QuoteInline`,
  FROM `Comment` TO `StyledInline`,
  FROM `Comment` TO `Strikeout`,
  FROM `Comment` TO `Strong`,
  FROM `Comment` TO `Subscript`,
  FROM `Comment` TO `SuggestionInline`,
  FROM `Comment` TO `Superscript`,
  FROM `Comment` TO `Text`,
  FROM `Comment` TO `Time`,
  FROM `Comment` TO `Timestamp`,
  FROM `Comment` TO `Underline`,
  FROM `Comment` TO `VideoObject`,
  FROM `CreativeWork` TO `Annotation`,
  FROM `CreativeWork` TO `AudioObject`,
  FROM `CreativeWork` TO `Button`,
  FROM `CreativeWork` TO `Cite`,
  FROM `CreativeWork` TO `CiteGroup`,
  FROM `CreativeWork` TO `CodeExpression`,
  FROM `CreativeWork` TO `CodeInline`,
  FROM `CreativeWork` TO `Date`,
  FROM `CreativeWork` TO `DateTime`,
  FROM `CreativeWork` TO `Duration`,
  FROM `CreativeWork` TO `Emphasis`,
  FROM `CreativeWork` TO `ImageObject`,
  FROM `CreativeWork` TO `InstructionInline`,
  FROM `CreativeWork` TO `Link`,
  FROM `CreativeWork` TO `MathInline`,
  FROM `CreativeWork` TO `MediaObject`,
  FROM `CreativeWork` TO `Note`,
  FROM `CreativeWork` TO `Parameter`,
  FROM `CreativeWork` TO `QuoteInline`,
  FROM `CreativeWork` TO `StyledInline`,
  FROM `CreativeWork` TO `Strikeout`,
  FROM `CreativeWork` TO `Strong`,
  FROM `CreativeWork` TO `Subscript`,
  FROM `CreativeWork` TO `SuggestionInline`,
  FROM `CreativeWork` TO `Superscript`,
  FROM `CreativeWork` TO `Text`,
  FROM `CreativeWork` TO `Time`,
  FROM `CreativeWork` TO `Timestamp`,
  FROM `CreativeWork` TO `Underline`,
  FROM `CreativeWork` TO `VideoObject`,
  FROM `Datatable` TO `Annotation`,
  FROM `Datatable` TO `AudioObject`,
  FROM `Datatable` TO `Button`,
  FROM `Datatable` TO `Cite`,
  FROM `Datatable` TO `CiteGroup`,
  FROM `Datatable` TO `CodeExpression`,
  FROM `Datatable` TO `CodeInline`,
  FROM `Datatable` TO `Date`,
  FROM `Datatable` TO `DateTime`,
  FROM `Datatable` TO `Duration`,
  FROM `Datatable` TO `Emphasis`,
  FROM `Datatable` TO `ImageObject`,
  FROM `Datatable` TO `InstructionInline`,
  FROM `Datatable` TO `Link`,
  FROM `Datatable` TO `MathInline`,
  FROM `Datatable` TO `MediaObject`,
  FROM `Datatable` TO `Note`,
  FROM `Datatable` TO `Parameter`,
  FROM `Datatable` TO `QuoteInline`,
  FROM `Datatable` TO `StyledInline`,
  FROM `Datatable` TO `Strikeout`,
  FROM `Datatable` TO `Strong`,
  FROM `Datatable` TO `Subscript`,
  FROM `Datatable` TO `SuggestionInline`,
  FROM `Datatable` TO `Superscript`,
  FROM `Datatable` TO `Text`,
  FROM `Datatable` TO `Time`,
  FROM `Datatable` TO `Timestamp`,
  FROM `Datatable` TO `Underline`,
  FROM `Datatable` TO `VideoObject`,
  FROM `Figure` TO `Annotation`,
  FROM `Figure` TO `AudioObject`,
  FROM `Figure` TO `Button`,
  FROM `Figure` TO `Cite`,
  FROM `Figure` TO `CiteGroup`,
  FROM `Figure` TO `CodeExpression`,
  FROM `Figure` TO `CodeInline`,
  FROM `Figure` TO `Date`,
  FROM `Figure` TO `DateTime`,
  FROM `Figure` TO `Duration`,
  FROM `Figure` TO `Emphasis`,
  FROM `Figure` TO `ImageObject`,
  FROM `Figure` TO `InstructionInline`,
  FROM `Figure` TO `Link`,
  FROM `Figure` TO `MathInline`,
  FROM `Figure` TO `MediaObject`,
  FROM `Figure` TO `Note`,
  FROM `Figure` TO `Parameter`,
  FROM `Figure` TO `QuoteInline`,
  FROM `Figure` TO `StyledInline`,
  FROM `Figure` TO `Strikeout`,
  FROM `Figure` TO `Strong`,
  FROM `Figure` TO `Subscript`,
  FROM `Figure` TO `SuggestionInline`,
  FROM `Figure` TO `Superscript`,
  FROM `Figure` TO `Text`,
  FROM `Figure` TO `Time`,
  FROM `Figure` TO `Timestamp`,
  FROM `Figure` TO `Underline`,
  FROM `Figure` TO `VideoObject`,
  FROM `ImageObject` TO `Annotation`,
  FROM `ImageObject` TO `AudioObject`,
  FROM `ImageObject` TO `Button`,
  FROM `ImageObject` TO `Cite`,
  FROM `ImageObject` TO `CiteGroup`,
  FROM `ImageObject` TO `CodeExpression`,
  FROM `ImageObject` TO `CodeInline`,
  FROM `ImageObject` TO `Date`,
  FROM `ImageObject` TO `DateTime`,
  FROM `ImageObject` TO `Duration`,
  FROM `ImageObject` TO `Emphasis`,
  FROM `ImageObject` TO `ImageObject`,
  FROM `ImageObject` TO `InstructionInline`,
  FROM `ImageObject` TO `Link`,
  FROM `ImageObject` TO `MathInline`,
  FROM `ImageObject` TO `MediaObject`,
  FROM `ImageObject` TO `Note`,
  FROM `ImageObject` TO `Parameter`,
  FROM `ImageObject` TO `QuoteInline`,
  FROM `ImageObject` TO `StyledInline`,
  FROM `ImageObject` TO `Strikeout`,
  FROM `ImageObject` TO `Strong`,
  FROM `ImageObject` TO `Subscript`,
  FROM `ImageObject` TO `SuggestionInline`,
  FROM `ImageObject` TO `Superscript`,
  FROM `ImageObject` TO `Text`,
  FROM `ImageObject` TO `Time`,
  FROM `ImageObject` TO `Timestamp`,
  FROM `ImageObject` TO `Underline`,
  FROM `ImageObject` TO `VideoObject`,
  FROM `MediaObject` TO `Annotation`,
  FROM `MediaObject` TO `AudioObject`,
  FROM `MediaObject` TO `Button`,
  FROM `MediaObject` TO `Cite`,
  FROM `MediaObject` TO `CiteGroup`,
  FROM `MediaObject` TO `CodeExpression`,
  FROM `MediaObject` TO `CodeInline`,
  FROM `MediaObject` TO `Date`,
  FROM `MediaObject` TO `DateTime`,
  FROM `MediaObject` TO `Duration`,
  FROM `MediaObject` TO `Emphasis`,
  FROM `MediaObject` TO `ImageObject`,
  FROM `MediaObject` TO `InstructionInline`,
  FROM `MediaObject` TO `Link`,
  FROM `MediaObject` TO `MathInline`,
  FROM `MediaObject` TO `MediaObject`,
  FROM `MediaObject` TO `Note`,
  FROM `MediaObject` TO `Parameter`,
  FROM `MediaObject` TO `QuoteInline`,
  FROM `MediaObject` TO `StyledInline`,
  FROM `MediaObject` TO `Strikeout`,
  FROM `MediaObject` TO `Strong`,
  FROM `MediaObject` TO `Subscript`,
  FROM `MediaObject` TO `SuggestionInline`,
  FROM `MediaObject` TO `Superscript`,
  FROM `MediaObject` TO `Text`,
  FROM `MediaObject` TO `Time`,
  FROM `MediaObject` TO `Timestamp`,
  FROM `MediaObject` TO `Underline`,
  FROM `MediaObject` TO `VideoObject`,
  FROM `Periodical` TO `Annotation`,
  FROM `Periodical` TO `AudioObject`,
  FROM `Periodical` TO `Button`,
  FROM `Periodical` TO `Cite`,
  FROM `Periodical` TO `CiteGroup`,
  FROM `Periodical` TO `CodeExpression`,
  FROM `Periodical` TO `CodeInline`,
  FROM `Periodical` TO `Date`,
  FROM `Periodical` TO `DateTime`,
  FROM `Periodical` TO `Duration`,
  FROM `Periodical` TO `Emphasis`,
  FROM `Periodical` TO `ImageObject`,
  FROM `Periodical` TO `InstructionInline`,
  FROM `Periodical` TO `Link`,
  FROM `Periodical` TO `MathInline`,
  FROM `Periodical` TO `MediaObject`,
  FROM `Periodical` TO `Note`,
  FROM `Periodical` TO `Parameter`,
  FROM `Periodical` TO `QuoteInline`,
  FROM `Periodical` TO `StyledInline`,
  FROM `Periodical` TO `Strikeout`,
  FROM `Periodical` TO `Strong`,
  FROM `Periodical` TO `Subscript`,
  FROM `Periodical` TO `SuggestionInline`,
  FROM `Periodical` TO `Superscript`,
  FROM `Periodical` TO `Text`,
  FROM `Periodical` TO `Time`,
  FROM `Periodical` TO `Timestamp`,
  FROM `Periodical` TO `Underline`,
  FROM `Periodical` TO `VideoObject`,
  FROM `Prompt` TO `Annotation`,
  FROM `Prompt` TO `AudioObject`,
  FROM `Prompt` TO `Button`,
  FROM `Prompt` TO `Cite`,
  FROM `Prompt` TO `CiteGroup`,
  FROM `Prompt` TO `CodeExpression`,
  FROM `Prompt` TO `CodeInline`,
  FROM `Prompt` TO `Date`,
  FROM `Prompt` TO `DateTime`,
  FROM `Prompt` TO `Duration`,
  FROM `Prompt` TO `Emphasis`,
  FROM `Prompt` TO `ImageObject`,
  FROM `Prompt` TO `InstructionInline`,
  FROM `Prompt` TO `Link`,
  FROM `Prompt` TO `MathInline`,
  FROM `Prompt` TO `MediaObject`,
  FROM `Prompt` TO `Note`,
  FROM `Prompt` TO `Parameter`,
  FROM `Prompt` TO `QuoteInline`,
  FROM `Prompt` TO `StyledInline`,
  FROM `Prompt` TO `Strikeout`,
  FROM `Prompt` TO `Strong`,
  FROM `Prompt` TO `Subscript`,
  FROM `Prompt` TO `SuggestionInline`,
  FROM `Prompt` TO `Superscript`,
  FROM `Prompt` TO `Text`,
  FROM `Prompt` TO `Time`,
  FROM `Prompt` TO `Timestamp`,
  FROM `Prompt` TO `Underline`,
  FROM `Prompt` TO `VideoObject`,
  FROM `PublicationIssue` TO `Annotation`,
  FROM `PublicationIssue` TO `AudioObject`,
  FROM `PublicationIssue` TO `Button`,
  FROM `PublicationIssue` TO `Cite`,
  FROM `PublicationIssue` TO `CiteGroup`,
  FROM `PublicationIssue` TO `CodeExpression`,
  FROM `PublicationIssue` TO `CodeInline`,
  FROM `PublicationIssue` TO `Date`,
  FROM `PublicationIssue` TO `DateTime`,
  FROM `PublicationIssue` TO `Duration`,
  FROM `PublicationIssue` TO `Emphasis`,
  FROM `PublicationIssue` TO `ImageObject`,
  FROM `PublicationIssue` TO `InstructionInline`,
  FROM `PublicationIssue` TO `Link`,
  FROM `PublicationIssue` TO `MathInline`,
  FROM `PublicationIssue` TO `MediaObject`,
  FROM `PublicationIssue` TO `Note`,
  FROM `PublicationIssue` TO `Parameter`,
  FROM `PublicationIssue` TO `QuoteInline`,
  FROM `PublicationIssue` TO `StyledInline`,
  FROM `PublicationIssue` TO `Strikeout`,
  FROM `PublicationIssue` TO `Strong`,
  FROM `PublicationIssue` TO `Subscript`,
  FROM `PublicationIssue` TO `SuggestionInline`,
  FROM `PublicationIssue` TO `Superscript`,
  FROM `PublicationIssue` TO `Text`,
  FROM `PublicationIssue` TO `Time`,
  FROM `PublicationIssue` TO `Timestamp`,
  FROM `PublicationIssue` TO `Underline`,
  FROM `PublicationIssue` TO `VideoObject`,
  FROM `PublicationVolume` TO `Annotation`,
  FROM `PublicationVolume` TO `AudioObject`,
  FROM `PublicationVolume` TO `Button`,
  FROM `PublicationVolume` TO `Cite`,
  FROM `PublicationVolume` TO `CiteGroup`,
  FROM `PublicationVolume` TO `CodeExpression`,
  FROM `PublicationVolume` TO `CodeInline`,
  FROM `PublicationVolume` TO `Date`,
  FROM `PublicationVolume` TO `DateTime`,
  FROM `PublicationVolume` TO `Duration`,
  FROM `PublicationVolume` TO `Emphasis`,
  FROM `PublicationVolume` TO `ImageObject`,
  FROM `PublicationVolume` TO `InstructionInline`,
  FROM `PublicationVolume` TO `Link`,
  FROM `PublicationVolume` TO `MathInline`,
  FROM `PublicationVolume` TO `MediaObject`,
  FROM `PublicationVolume` TO `Note`,
  FROM `PublicationVolume` TO `Parameter`,
  FROM `PublicationVolume` TO `QuoteInline`,
  FROM `PublicationVolume` TO `StyledInline`,
  FROM `PublicationVolume` TO `Strikeout`,
  FROM `PublicationVolume` TO `Strong`,
  FROM `PublicationVolume` TO `Subscript`,
  FROM `PublicationVolume` TO `SuggestionInline`,
  FROM `PublicationVolume` TO `Superscript`,
  FROM `PublicationVolume` TO `Text`,
  FROM `PublicationVolume` TO `Time`,
  FROM `PublicationVolume` TO `Timestamp`,
  FROM `PublicationVolume` TO `Underline`,
  FROM `PublicationVolume` TO `VideoObject`,
  FROM `Review` TO `Annotation`,
  FROM `Review` TO `AudioObject`,
  FROM `Review` TO `Button`,
  FROM `Review` TO `Cite`,
  FROM `Review` TO `CiteGroup`,
  FROM `Review` TO `CodeExpression`,
  FROM `Review` TO `CodeInline`,
  FROM `Review` TO `Date`,
  FROM `Review` TO `DateTime`,
  FROM `Review` TO `Duration`,
  FROM `Review` TO `Emphasis`,
  FROM `Review` TO `ImageObject`,
  FROM `Review` TO `InstructionInline`,
  FROM `Review` TO `Link`,
  FROM `Review` TO `MathInline`,
  FROM `Review` TO `MediaObject`,
  FROM `Review` TO `Note`,
  FROM `Review` TO `Parameter`,
  FROM `Review` TO `QuoteInline`,
  FROM `Review` TO `StyledInline`,
  FROM `Review` TO `Strikeout`,
  FROM `Review` TO `Strong`,
  FROM `Review` TO `Subscript`,
  FROM `Review` TO `SuggestionInline`,
  FROM `Review` TO `Superscript`,
  FROM `Review` TO `Text`,
  FROM `Review` TO `Time`,
  FROM `Review` TO `Timestamp`,
  FROM `Review` TO `Underline`,
  FROM `Review` TO `VideoObject`,
  FROM `SoftwareApplication` TO `Annotation`,
  FROM `SoftwareApplication` TO `AudioObject`,
  FROM `SoftwareApplication` TO `Button`,
  FROM `SoftwareApplication` TO `Cite`,
  FROM `SoftwareApplication` TO `CiteGroup`,
  FROM `SoftwareApplication` TO `CodeExpression`,
  FROM `SoftwareApplication` TO `CodeInline`,
  FROM `SoftwareApplication` TO `Date`,
  FROM `SoftwareApplication` TO `DateTime`,
  FROM `SoftwareApplication` TO `Duration`,
  FROM `SoftwareApplication` TO `Emphasis`,
  FROM `SoftwareApplication` TO `ImageObject`,
  FROM `SoftwareApplication` TO `InstructionInline`,
  FROM `SoftwareApplication` TO `Link`,
  FROM `SoftwareApplication` TO `MathInline`,
  FROM `SoftwareApplication` TO `MediaObject`,
  FROM `SoftwareApplication` TO `Note`,
  FROM `SoftwareApplication` TO `Parameter`,
  FROM `SoftwareApplication` TO `QuoteInline`,
  FROM `SoftwareApplication` TO `StyledInline`,
  FROM `SoftwareApplication` TO `Strikeout`,
  FROM `SoftwareApplication` TO `Strong`,
  FROM `SoftwareApplication` TO `Subscript`,
  FROM `SoftwareApplication` TO `SuggestionInline`,
  FROM `SoftwareApplication` TO `Superscript`,
  FROM `SoftwareApplication` TO `Text`,
  FROM `SoftwareApplication` TO `Time`,
  FROM `SoftwareApplication` TO `Timestamp`,
  FROM `SoftwareApplication` TO `Underline`,
  FROM `SoftwareApplication` TO `VideoObject`,
  FROM `SoftwareSourceCode` TO `Annotation`,
  FROM `SoftwareSourceCode` TO `AudioObject`,
  FROM `SoftwareSourceCode` TO `Button`,
  FROM `SoftwareSourceCode` TO `Cite`,
  FROM `SoftwareSourceCode` TO `CiteGroup`,
  FROM `SoftwareSourceCode` TO `CodeExpression`,
  FROM `SoftwareSourceCode` TO `CodeInline`,
  FROM `SoftwareSourceCode` TO `Date`,
  FROM `SoftwareSourceCode` TO `DateTime`,
  FROM `SoftwareSourceCode` TO `Duration`,
  FROM `SoftwareSourceCode` TO `Emphasis`,
  FROM `SoftwareSourceCode` TO `ImageObject`,
  FROM `SoftwareSourceCode` TO `InstructionInline`,
  FROM `SoftwareSourceCode` TO `Link`,
  FROM `SoftwareSourceCode` TO `MathInline`,
  FROM `SoftwareSourceCode` TO `MediaObject`,
  FROM `SoftwareSourceCode` TO `Note`,
  FROM `SoftwareSourceCode` TO `Parameter`,
  FROM `SoftwareSourceCode` TO `QuoteInline`,
  FROM `SoftwareSourceCode` TO `StyledInline`,
  FROM `SoftwareSourceCode` TO `Strikeout`,
  FROM `SoftwareSourceCode` TO `Strong`,
  FROM `SoftwareSourceCode` TO `Subscript`,
  FROM `SoftwareSourceCode` TO `SuggestionInline`,
  FROM `SoftwareSourceCode` TO `Superscript`,
  FROM `SoftwareSourceCode` TO `Text`,
  FROM `SoftwareSourceCode` TO `Time`,
  FROM `SoftwareSourceCode` TO `Timestamp`,
  FROM `SoftwareSourceCode` TO `Underline`,
  FROM `SoftwareSourceCode` TO `VideoObject`,
  FROM `Table` TO `Annotation`,
  FROM `Table` TO `AudioObject`,
  FROM `Table` TO `Button`,
  FROM `Table` TO `Cite`,
  FROM `Table` TO `CiteGroup`,
  FROM `Table` TO `CodeExpression`,
  FROM `Table` TO `CodeInline`,
  FROM `Table` TO `Date`,
  FROM `Table` TO `DateTime`,
  FROM `Table` TO `Duration`,
  FROM `Table` TO `Emphasis`,
  FROM `Table` TO `ImageObject`,
  FROM `Table` TO `InstructionInline`,
  FROM `Table` TO `Link`,
  FROM `Table` TO `MathInline`,
  FROM `Table` TO `MediaObject`,
  FROM `Table` TO `Note`,
  FROM `Table` TO `Parameter`,
  FROM `Table` TO `QuoteInline`,
  FROM `Table` TO `StyledInline`,
  FROM `Table` TO `Strikeout`,
  FROM `Table` TO `Strong`,
  FROM `Table` TO `Subscript`,
  FROM `Table` TO `SuggestionInline`,
  FROM `Table` TO `Superscript`,
  FROM `Table` TO `Text`,
  FROM `Table` TO `Time`,
  FROM `Table` TO `Timestamp`,
  FROM `Table` TO `Underline`,
  FROM `Table` TO `VideoObject`,
  FROM `VideoObject` TO `Annotation`,
  FROM `VideoObject` TO `AudioObject`,
  FROM `VideoObject` TO `Button`,
  FROM `VideoObject` TO `Cite`,
  FROM `VideoObject` TO `CiteGroup`,
  FROM `VideoObject` TO `CodeExpression`,
  FROM `VideoObject` TO `CodeInline`,
  FROM `VideoObject` TO `Date`,
  FROM `VideoObject` TO `DateTime`,
  FROM `VideoObject` TO `Duration`,
  FROM `VideoObject` TO `Emphasis`,
  FROM `VideoObject` TO `ImageObject`,
  FROM `VideoObject` TO `InstructionInline`,
  FROM `VideoObject` TO `Link`,
  FROM `VideoObject` TO `MathInline`,
  FROM `VideoObject` TO `MediaObject`,
  FROM `VideoObject` TO `Note`,
  FROM `VideoObject` TO `Parameter`,
  FROM `VideoObject` TO `QuoteInline`,
  FROM `VideoObject` TO `StyledInline`,
  FROM `VideoObject` TO `Strikeout`,
  FROM `VideoObject` TO `Strong`,
  FROM `VideoObject` TO `Subscript`,
  FROM `VideoObject` TO `SuggestionInline`,
  FROM `VideoObject` TO `Superscript`,
  FROM `VideoObject` TO `Text`,
  FROM `VideoObject` TO `Time`,
  FROM `VideoObject` TO `Timestamp`,
  FROM `VideoObject` TO `Underline`,
  FROM `VideoObject` TO `VideoObject`,
  `index` UINT32,
  ONE_MANY
);
