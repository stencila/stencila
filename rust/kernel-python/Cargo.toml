[package]
name = "kernel-python"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel-micro = { path = "../kernel-micro" }
which = { workspace = true }

[dev-dependencies]
common-dev = { path = "../common-dev" }
test-log = { workspace = true }

[lints]
workspace = true
