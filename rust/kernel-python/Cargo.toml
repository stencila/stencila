[package]
name = "kernel-python"
description = "A microkernel for Python"
version = "0.0.0"
edition = "2021"

[dependencies]
binaries = { path = "../binaries", features = ["binary-python"] }
kernel = { path = "../kernel" }
kernel-micro = { path = "../kernel-micro" }

[dev-dependencies]
test-utils = { path = "../test-utils" }
