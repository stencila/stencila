[package]
name = "kernel-python"
description = "A microkernel for Python"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel", version = "0.0.0" }
kernel-micro = { path = "../kernel-micro", version = "0.0.0" }

[dev-dependencies]
tokio = { version = "1.13.0", features = ["macros"] }
test-utils = { path = "../test-utils", version = "0.0.0" }
