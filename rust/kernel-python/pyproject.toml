[project]
name = "kernel-python-tests"
version = "0.1.0"
description = "Dependencies required for testing the Stencila kernel-python crate"
requires-python = ">=3.12"
dependencies = [
    "altair>=5.5.0",
    "matplotlib>=3.10.0",
    "numpy>=2.2.2",
    "pandas>=2.2.3",
    "plotly>=6.0.0",
]
