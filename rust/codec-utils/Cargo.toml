[package]
name = "codec-utils"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
which = { workspace = true }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
