[package]
name = "assistant-openai"
version = "0.0.0"
edition = "2021"

[dependencies]
assistant = { path = "../assistant" }
async-openai = "0.17.1"
cached = { version = "0.46.1", features = ["async"] }
