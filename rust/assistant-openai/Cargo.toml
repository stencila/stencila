[package]
name = "assistant-openai"
version = "0.0.0"
edition = "2021"

[dependencies]
assistant = { path = "../assistant" }
async-openai = "0.19.1"
cached = { version = "0.49.2", features = ["async"] }
