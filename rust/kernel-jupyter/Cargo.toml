[package]
name = "kernel-jupyter"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }
runtimelib = { version = "0.23.0", features = ["tokio-runtime"] }

[dev-dependencies]
common-dev = { path = "../common-dev" }
