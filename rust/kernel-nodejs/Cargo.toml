[package]
name = "kernel-nodejs"
version = "0.0.0"
edition = "2024"

[dependencies]
kernel-micro = { path = "../kernel-micro" }

[dev-dependencies]
common-dev = { path = "../common-dev" }
test-log = { workspace = true }

[lints]
workspace = true
