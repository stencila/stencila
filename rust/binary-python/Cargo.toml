[package]
name = "binary-python"
description = "A binary specification for Python"
version = "0.0.0"
edition = "2021"

[dependencies]
binary = { path = "../binary", version = "0.0.0", features = ["download-zip"] }
