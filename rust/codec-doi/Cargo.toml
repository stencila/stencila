[package]
name = "codec-doi"
version = "0.0.0"
edition = "2024"

[dependencies]
codec = { path = "../codec" }
url = { workspace = true }
version = { path = "../version" }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
