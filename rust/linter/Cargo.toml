[package]
name = "stencila-linter"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
format = { path = "../format" }
node-type = { path = "../node-type" }
schema = { path = "../schema" }
serde = { workspace = true }
strum = { workspace = true }

[lints]
workspace = true
