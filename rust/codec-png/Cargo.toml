[package]
name = "codec-png"
description = "A codec for PNG images"
version = "0.0.0"
edition = "2021"

[dependencies]
binaries = { path = "../binaries" }
chromiumoxide = { version = "0.3.4", features = ["tokio-runtime"] }
codec-html = { path = "../codec-html" }
codec = { path = "../codec" }

[dev-dependencies]
test-utils = { path = "../test-utils" }
