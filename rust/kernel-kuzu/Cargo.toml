[package]
name = "kernel-kuzu"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }
kernel-jinja = { path = "../kernel-jinja" }
kuzu = "0.10.1"

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
