[package]
name = "kernel-kuzu"
version = "0.0.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
kernel-jinja = { path = "../kernel-jinja" }
kuzu = "0.11.2"
serde = { workspace = true }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
