[package]
name = "kernel-kuzu"
version = "0.0.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
kuzu = "0.8.2"

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
