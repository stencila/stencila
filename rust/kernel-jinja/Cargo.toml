[package]
name = "kernel-jinja"
version = "0.0.0"
edition = "2024"

[dependencies]
kernel = { path = "../kernel" }
minijinja = { version = "2.11.0", features = ["loader", "json", "debug"] }

[dev-dependencies]
common-dev = { path = "../common-dev" }

[lints]
workspace = true
