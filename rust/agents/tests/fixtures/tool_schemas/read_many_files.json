{
  "name": "read_many_files",
  "description": "Read multiple files in a single call. Returns contents of each file with headers. Errors for individual files are reported inline without aborting the batch.",
  "parameters": {
    "type": "object",
    "properties": {
      "paths": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Array of absolute file paths to read."
      }
    },
    "required": ["paths"],
    "additionalProperties": false
  }
}
