[package]
name = "knit"
version = "0.0.0"
edition = "2024"

[dependencies]
codec-latex-trait = { path = "../codec-latex-trait" }
common = { path = "../common" }
format = { path = "../format" }
images = { path = "../images" }
document = { path = "../document" }
rand = { workspace = true }
schema = { path = "../schema" }

[lints]
workspace = true
