[package]
name = "kernel"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
format = { path = "../format" }
schema = { path = "../schema" }
serde = { workspace = true }
common-dev = { path = "../common-dev" }

[lints]
workspace = true
