[package]
name = "kernel"
version = "0.0.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
common-dev = { path = "../common-dev" }
format = { path = "../format" }
schema = { path = "../schema" }
serde = { workspace = true }
strum = { workspace = true }

[lints]
workspace = true
