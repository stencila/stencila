[package]
name = "node-diagnostics"
version = "0.0.0"
edition = "2021"

[dependencies]
ariadne = "0.5.0"
codec-info = { path = "../codec-info" }
common = { path = "../common" }
format = { path = "../format" }
schema = { path = "../schema" }

[lints]
workspace = true
