[package]
name = "node-diagnostics"
version = "0.0.0"
edition = "2024"

[dependencies]
ariadne = "0.5.1"
codec-info = { path = "../codec-info" }
common = { path = "../common" }
format = { path = "../format" }
schema = { path = "../schema" }

[lints]
workspace = true
