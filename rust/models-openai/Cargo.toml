[package]
name = "models-openai"
version = "0.0.0"
edition = "2021"

[dependencies]
model = { path = "../model" }
async-openai = { version = "0.29.0", features = ["rustls"] }
cached = { workspace = true }

[lints]
workspace = true
