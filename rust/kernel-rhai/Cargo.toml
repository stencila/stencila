[package]
name = "kernel-rhai"
version = "0.0.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }
rhai = { version = "1.19.0", features = ["sync", "serde"] }

[dev-dependencies]
common-dev = { path = "../common-dev" }
test-log = { version = "0.2.15", default-features = false, features = ["trace"] }
