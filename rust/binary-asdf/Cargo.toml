[package]
name = "binary-asdf"
description = "A binary specification for `asdf` (https://asdf-vm.com/)"
version = "0.0.0"
edition = "2021"

[dependencies]
binary = { path = "../binary", version = "0.0.0", features = [
    "download-tar-gz",
] }
