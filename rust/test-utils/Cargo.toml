[package]
name = "test-utils"
description = "Utilities for testing"
version = "0.0.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
insta = { version = "1.19.1", features = ["json", "glob", "redactions"] }
node-validate = { path = "../node-validate" }
pretty_assertions = "1.0.0"
proptest = "1.0.0"
stencila-schema = { path = "../schema" }
tracing-subscriber = "0.3.15"
