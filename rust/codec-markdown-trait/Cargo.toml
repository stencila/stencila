[package]
name = "stencila-codec-markdown-trait"
version = "0.0.0"
edition = "2024"

[dependencies]
Inflector = { workspace = true }
stencila-codec-info = { path = "../codec-info" }
stencila-codec-markdown-derive = { path = "../codec-markdown-derive" }
stencila-format = { path = "../format" }
tracing = { workspace = true }

[lints]
workspace = true
