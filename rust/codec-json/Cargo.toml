[package]
name = "codec-json"
version = "0.0.0"
edition = "2021"

[dependencies]
codec = { path = "../codec" }

[dev-dependencies]
common-dev = { path = "../common-dev" }
schema = { path = "../schema" }
