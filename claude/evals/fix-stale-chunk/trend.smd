---
title: Trend
description: Eval fixture with a locked chunk that will not re-run.
---

# Data

```python exec
values = [3, 5, 4, 6, 8, 7, 9, 12]
```

# Figure

```python exec lock
import matplotlib

matplotlib.use("Agg")
import matplotlib.pyplot as plt

plt.plot(values)
plt.title("Trend")
plt.gcf()
```
