import { css } from 'lit'

const outputCSS = css`
  img {
    height: auto;
    width: 100%;
  }
  table {
    border-spacing: 0px;
  }
  th {
    padding: 1rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }
  td {
    padding: 0.5rem 0.5rem 0.5rem 1rem;
  }
`

export { outputCSS }
