/**
 * Browser bundle for user mode `Alter`
 *
 * Set the mode to `Alter` so that web components allow editing
 * of code, changing of document calls etc.
 */

// @ts-ignore
import('./inspect-imports')
