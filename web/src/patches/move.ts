import { DomOperationMove } from '@stencila/stencila'
import { panic } from './utils'

/**
 * Apply a move operation
 */
export function applyMove(_op: DomOperationMove): void {
  throw panic('TODO')
}
