'use strict';

/**
 * Imports/exports a document from/to JSON
 *
 * This is only here to provide naming consistency with
 * `DocumentHTMLImporter` and `DocumentHTMLImporter` (i.e. to have
 * a `JSONCoverter` that is explicitly for Stencila Documents)
 *
 * @class      DocumentJSONConverter (name)
 */

module.exports = require('substance/model/JSONConverter');
