[project]
name = "stencila-workspace-defaults"
version = "0.1.0"
description = "Default Python packages for Stencila workspaces. Used when a workspace does not specify a pyproject.toml or requirements.txt file"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.3",
    "polars>=1.21.0",
    "matplotlib>=3.10.0",
    "seaborn>=0.13.2",
    "plotly>=6.0.0",
    "altair[all]>=5.5.0",
    "plotnine>=0.14.5"
]
