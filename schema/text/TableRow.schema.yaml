title: TableRow
'@id': stencila:TableRow
role: secondary
status: experimental
description: |
  A row within a `Table`.
properties:
  cells:
    '@id': stencila:cells
    description: |
      An array of cells in the table.
    type: array
    items:
      $ref: TableCell.schema.yaml
required:
  - value
