title: TableRow
'@id': stencila:TableRow
extends: Thing
role: secondary
status: experimental
description: A row within a Table.
properties:
  cells:
    '@id': stencila:cells
    description: An array of cells in the row.
    type: array
    items:
      $ref: TableCell
required:
  - cells
