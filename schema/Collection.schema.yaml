title: Collection
'@id': schema:Collection
$extends: CreativeWork.schema.yaml
properties:
  parts:
    '@id': schema:hasParts
    type: array
    items:
      $ref: CreativeWork.schema.yaml
required:
  - parts
