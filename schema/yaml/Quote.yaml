title: Quote
"@id": stencila:Quote
extends: Mark

status: stable
category: text
description: Inline, quoted content.
properties:
  cite:
    "@id": stencila:cite
    description: The source of the quote.
    anyOf:
      - $ref: Cite
      - type: string
        format: uri
