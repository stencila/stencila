title: Division
'@id': stencila:Division

description: Styled block content

status: unstable
extends: Styled
properties:
  content:
    '@id': 'stencila:content'
    description: The content within the division
    type: array
    items:
      $ref: BlockContent
required:
  - content
