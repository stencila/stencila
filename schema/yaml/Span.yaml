title: Span
"@id": stencila:Span
category: text
description: Styled inline content

status: unstable
extends: Styled
properties:
  content:
    "@id": "stencila:content"
    description: The content within the span
    type: array
    items:
      $ref: InlineContent
required:
  - content
