title: Span
'@id': stencila:Span
extends: Styled
category: style
description: Styled inline content
status: under-development
required:
  - content
properties:
  content:
    '@id': 'stencila:content'
    description: The content within the span
    type: array
    items:
      $ref: Inline
html:
  elem: span
  custom: true
markdown:
  format: '[{content}]{{{code}}}'
