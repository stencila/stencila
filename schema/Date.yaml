title: Date
'@id': schema:Date
description: A calendar date encoded as a ISO 8601 string.
parser: date
properties:
  value:
    '@id': 'schema:value'
    description: The date as an ISO 8601 string.
    type: string
    format: date
required:
  - value
