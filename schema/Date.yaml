title: Date
'@id': schema:Date
extends: Entity
description: A calendar date encoded as a ISO 8601 string.
required:
  - value
properties:
  value:
    '@id': 'schema:value'
    description: The date as an ISO 8601 string.
    type: string
    format: date
