title: ConfigPublishGhostState
extends: Enumeration
category: config
description: The state of Ghost resource
default: Draft 
anyOf:
  - const: Draft 
    description: a draft page or post.
    aliases: [Draft]
    serde:
      rename: draft
  - const: Publish
    description: Publish pagor or post.
    aliases: [Publish]
    serde:
      rename: publish
