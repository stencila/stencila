{
    "$id": "https://stencila.github.com/schema/01-draft/file.schema.json",
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "file",
    "description": "File system path to an instance (should resolve to content that can be loaded as the expected type).",
    "type": "string",
    "pattern": "^[.]\/$",
    "$comment": "A string starting with a lowercase letter and only including lowercase letters, digits and hyphens."
}
