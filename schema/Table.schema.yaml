title: Table
'@id': schema:Table
extends: CreativeWork
role: primary
status: unstable
category: prose
description: A table.
properties:
  rows:
    '@id': stencila:rows
    description: |
      Rows of cells in the table.
    type: array
    items:
      $ref: TableRow
required:
  - rows
