title: Datatable
'@id': stencila:Datatable
extends: CreativeWork
role: primary
status: experimental
description: A table of data.
properties:
  columns:
    '@id': stencila:columns
    description: The columns of data.
    type: array
    items:
      $ref: DatatableColumn
required:
  - columns
