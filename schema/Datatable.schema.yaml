$schema: http://json-schema.org/draft-07/schema#
$id: https://stencila.github.com/schema#Datatable
title: Datatable
description: |
  TODO
type: object
properties:
  type:
    description: The type of node; always "Datatable".
    type: string
    const: Datatable
  value:
    description: TODO
    type: array
    items:
      $ref: "#/definitions/column" 
definitions:
  column:
    title: DatatableColumn
    type: object
    properties:
      name:
        description: Name of the column.
        type: string
      meta:
        description: metadata
      schema:
        description: The schema to use to validate data in the column.
        type: object
        allOf:
          - $ref: http://json-schema.org/draft-07/schema#
      value:
        type: array
