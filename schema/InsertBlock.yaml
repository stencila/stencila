title: InsertBlock
'@id': stencila:InsertBlock
extends: SuggestionBlock
category: edits
description: A suggestion to insert some block content.
jats:
  special: true
markdown:
  # Consistent with the CriticMarkup for inline "Additions" (see `InsertInline)
  template: '++\n\n{content}++\n\n'
proptest: {}
