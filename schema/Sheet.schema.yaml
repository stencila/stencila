title: Sheet
'@id': stencila:Sheet
$extends: Thing.schema.yaml
status: experimental
description: |
  A sheet within an executable document.
properties:
  value:
    '@id': schema:value
    description: The content of the sheet e.g. some text or a table.
    oneOf:
      - $ref: Include.schema.yaml
      - $ref: data/Datatable.schema.yaml
      - $ref: text/Table.schema.yaml
