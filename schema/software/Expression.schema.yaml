title: Expression
'@id': Expression
$extends: SoftwareSourceCode.schema.yaml
role: secondary
status: experimental
description: An expression.
properties:
  value:
    '@id': schema:value
    allOf:
      - $ref: ../Node.schema.yaml
