[project]
name = "pyproject-toml"
version = "0.1.0"
description = "An example of a workspace that uses a pyproject.toml file to specify Python dependencies"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.21.0",
]
