Examples of the parameter type can be inferred from options when using Markdown.

No options and no type, so no validator: .

Options only found on string parameters: , , .

Option `vals` is only on "enum" parameters: .

Options specified as bool: true.

Options specified as integer: 1, , .

Options specified as number: 1.1, , , .

Options specified as date: .

Options specified as time: .

Options specified as datetime: .
