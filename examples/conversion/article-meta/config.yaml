smd:
  canonical: true

compact.jats.xml:
  decode:
    # TODO: Enable authors and other metadata encoding/decoding
    strip-scopes: [authors, metadata]

pandoc:
  decode:
    strip-props:
      # TODO: Implement (some) of these
      - Article.authors
      - Article.date_created
      - Article.date_accepted
      - ArticleOptions.funders

myst:
  decode:
    #TODO:removed when abstracts are implemented
    skip: true

qmd:
  decode:
    #TODO:removed when abstracts are implemented
    skip: true
