myst:
  decode:
    strip-props:
      # MyST does not currently support table notes
      - Table.notes

pandoc:
  decode:
    strip-props:
      # Pandoc does not support table notes
      - Table.notes
