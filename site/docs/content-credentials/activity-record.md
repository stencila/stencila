---
title: "Activity Record"
description: "Activity that generated, exported, or signed the asset."
---

# Activity Record

Activity that generated, exported, or signed the asset.

This is the provenance relation hub. It intentionally uses IDs to refer to
agents, inputs, and outputs so the assertion can remain compact while still
expressing the important "activity used/generated/was associated with"
relationships.

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| [`id`](#id) | `string` | No | Optional activity identifier. |
| [`kind`](#kind) | `string` | Yes | Activity kind. |
| [`name`](#name) | `string` | No | Human-readable activity name. |
| [`startedAt`](#started-at) | `string` | No | Activity start time in RFC 3339 format. |
| [`endedAt`](#ended-at) | `string` | No | Activity end time in RFC 3339 format. |
| [`durationMs`](#duration-ms) | `integer` | No | Activity duration in milliseconds. |
| [`associatedAttributionIds`](#associated-attribution-ids) | array<`string`> | No | Attribution IDs associated with the activity. |
| [`usedInputIds`](#used-input-ids) | array<`string`> | No | Input IDs used by the activity. |
| [`generatedOutputIds`](#generated-output-ids) | array<`string`> | No | Output IDs generated by the activity. |

### `id`

Optional activity identifier.

IDs make it possible for workflow logs, verification reports, or later
assertions to refer to this operation. They are optional because manual
signing often has no durable external run ID.

**Type:** `string` | **Required:** No | **Nullable:** Yes

### `kind`

Activity kind.

The initial vocabulary is `asset-signing`, `document-export`,
`code-execution`, and `workflow-run`; reverse-DNS extension values are
allowed. This field is required so consumers do not have to infer the
operation from whichever optional detail records happen to be present.

**Type:** `string` | **Required:** Yes

### `name`

Human-readable activity name.

A name is useful for workflows and UI summaries, but not stable enough to
serve as the activity kind or identifier.

**Type:** `string` | **Required:** No | **Nullable:** Yes

### `startedAt`

Activity start time in RFC 3339 format.

Start time is optional because some integrations only know when execution
ended or when the C2PA claim was signed.

**Type:** `string` | **Required:** No | **Nullable:** Yes

### `endedAt`

Activity end time in RFC 3339 format.

End time is often the most useful reproducibility timestamp because it is
when generated outputs became available for signing.

**Type:** `string` | **Required:** No | **Nullable:** Yes

### `durationMs`

Activity duration in milliseconds.

Duration is stored as an integer instead of a formatted string so
verifiers can compare timings across runs without parsing display units.

**Type:** `integer` | **Required:** No | **Nullable:** Yes

### `associatedAttributionIds`

Attribution IDs associated with the activity.

These IDs point at entries in `attributions`. The vector records the PROV
style association without duplicating full attribution records in the
activity.

**Type:** array<`string`> | **Required:** No

### `usedInputIds`

Input IDs used by the activity.

These IDs point at entries in `inputs`. They express use relationships
while allowing inputs to be redacted or summarized independently.

**Type:** array<`string`> | **Required:** No

### `generatedOutputIds`

Output IDs generated by the activity.

These IDs point at entries in `outputs` or the signed `asset.id`. They
express generation relationships without assuming the signed asset is the
only output of the operation.

**Type:** array<`string`> | **Required:** No


---

This documentation was generated from [`schema.rs`](https://github.com/stencila/stencila/blob/main/rust/content-credentials/src/schema.rs) by [`generate.rs`](https://github.com/stencila/stencila/blob/main/rust/content-credentials/src/bin/generate.rs).
