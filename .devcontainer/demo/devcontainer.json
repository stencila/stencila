// A Devcontainer for demoing the Stencila VSCode extension
//
// For format details see https://containers.dev/implementors/json_reference/.
{
  "build": { "dockerfile": "Dockerfile" },
  "workspaceMount": "source=${localWorkspaceFolder}/.devcontainer/demo/workspace,target=/demo,type=bind,consistency=cached",
  "workspaceFolder": "/demo",
  "secrets": {
    "STENCILA_API_TOKEN": {
      "description": "Add your Stencila Cloud API token",
      "documentationUrl": "https://stencila.cloud/access-tokens"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["stencila.stencila"],
      "settings": {
        // See here for VSCode settings and their defaults
        // https://code.visualstudio.com/docs/getstarted/settings#_default-settings
        "workbench.colorTheme": "Stencila Light",
        "[smd]": {
          "editor.wordWrap": "on"
        },
        "[myst]": {
          "editor.wordWrap": "on"
        }
      }
    },
    "codespaces": {
      "openFiles": ["welcome.smd"]
    }
  }
}
