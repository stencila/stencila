[package]
name = "stencila-python"
version = "0.111.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
stencila = { path = "../rust" }
pyo3 = { version = "=0.14.3", features = ["extension-module"] }
