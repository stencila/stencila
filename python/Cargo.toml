[package]
name = "stencila-python"
version = "0.127.1"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
stencila = { path = "../rust" }
pyo3 = { version = "=0.15.0", features = ["extension-module"] }
