[package]
name = "stencila-python"
version = "0.110.1"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
stencila = { path = "../rust" }
pyo3 = { version = "=0.14.3", features = ["extension-module"] }
