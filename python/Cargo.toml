[package]
name = "stencila-python"
version = "0.131.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
stencila = { path = "../rust" }
pyo3 = { version = "=0.15.1", features = ["extension-module"] }
