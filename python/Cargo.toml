[package]
name = "stencila-python"
version = "0.111.1"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
stencila = { path = "../rust" }
pyo3 = { version = "=0.14.4", features = ["extension-module"] }
