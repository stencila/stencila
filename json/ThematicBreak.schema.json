{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/ThematicBreak.schema.json",
  "@id": "stencila:ThematicBreak",
  "title": "ThematicBreak",
  "nick": "thb",
  "extends": [
    "Entity"
  ],
  "category": "prose",
  "description": "A thematic break.",
  "$comment": "This is a block type used in Stencila Schema for thematic or scene breaks, analogous\nto HTML `<hr>` and Markdown thematic breaks.\n\nIt exists to preserve this structural break as an explicit node within the\ndocument model rather than as a formatting artifact.\n\nThis type is mainly useful as a block separator within prose-oriented content\nmodels such as [`Block`](./block.md).\n",
  "analogues": [
    {
      "id": "html:hr"
    },
    {
      "id": "jats:hr"
    },
    {
      "id": "pandoc:HorizontalRule"
    },
    {
      "id": "mdast:ThematicBreak"
    }
  ],
  "dom": {
    "elem": "hr"
  },
  "html": {
    "elem": "hr"
  },
  "jats": {
    "elem": "hr"
  },
  "markdown": {
    "derive": false
  },
  "proptest": {},
  "required": [
    "type"
  ],
  "core": [
    "id"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    }
  }
}