{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/Strong.schema.json",
  "@id": "stencila:Strong",
  "title": "Strong",
  "nick": "stg",
  "extends": [
    "Mark"
  ],
  "category": "prose",
  "description": "Strongly emphasized content.",
  "$comment": "This is an inline mark type used in Stencila Schema for strong emphasis.\n\nIt extends [`Mark`](./mark.md), with analogues in HTML `<strong>` and Markdown\nstrong emphasis, to preserve semantic emphasis in a structured inline model.\n\nKey properties are inherited from [`Mark`](./mark.md), especially the wrapped\ninline `content`.\n",
  "analogues": [
    {
      "id": "html:strong"
    },
    {
      "id": "jats:bold",
      "notes": "Close JATS rendering analogue for strong emphasis."
    },
    {
      "id": "pandoc:Strong"
    },
    {
      "id": "mdast:Strong"
    }
  ],
  "dom": {
    "elem": "strong"
  },
  "html": {
    "elem": "strong"
  },
  "jats": {
    "elem": "bold"
  },
  "latex": {
    "command": "textbf"
  },
  "markdown": {
    "template": "**{{content}}**",
    "escape": "*"
  },
  "proptest": {},
  "required": [
    "type",
    "content"
  ],
  "core": [
    "id"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "content": {
      "@id": "stencila:content",
      "description": "The content that is marked.",
      "dom": {
        "elem": "none"
      },
      "proptest": {
        "min": {
          "description": "Generate a single fixed text value.",
          "value": "vec![t(\"text\")]"
        },
        "low": {
          "description": "Generate a single arbitrary, non-recursive, inline node",
          "strategy": "vec_inlines_non_recursive(1)"
        },
        "high": {
          "description": "Generate up to two arbitrary, non-recursive, inline nodes",
          "strategy": "vec_inlines_non_recursive(2)"
        },
        "max": {
          "description": "Generate up to four arbitrary, non-recursive, inline nodes",
          "strategy": "vec_inlines_non_recursive(4)"
        }
      },
      "type": "array",
      "items": {
        "$ref": "Inline.schema.json"
      }
    }
  }
}