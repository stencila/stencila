{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/Function.schema.json",
  "@id": "stencila:Function",
  "title": "Function",
  "extends": [
    "Entity"
  ],
  "category": "flow",
  "description": "A function signature.",
  "$comment": "This is a type used in Stencila Schema for describing callable functions in\ndocuments and execution contexts.\n\nIt exists to represent function signatures structurally, including parameters\nand return types, so functions can be documented, exposed to tools, and\ninvoked from executable nodes. This helps bridge document content with\ncomputational behavior.\n\nKey properties include the function `name`, `parameters`, and return-value\ndescription.\n",
  "analogues": [
    {
      "name": "OpenAPI operation signature",
      "url": "https://spec.openapis.org/oas/latest.html",
      "notes": "Approximate analogue for structured callable signatures with parameters and return-like schemas, though Stencila `Function` is language-agnostic and document-oriented."
    }
  ],
  "status": "experimental",
  "required": [
    "type",
    "name",
    "parameters"
  ],
  "core": [
    "id",
    "returns"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "name": {
      "@id": "schema:name",
      "description": "The name of the function.",
      "$comment": "The name property is not required; this allows for anonymous functions\n(although these are not yet implemented in Stencila interpreters).\nThe regex allows for snake_case and camelCase names but excludes\nPascalCase for parameter names.\n",
      "type": "string",
      "pattern": "[a-z_][A-z0-9_]*"
    },
    "parameters": {
      "@id": "stencila:parameters",
      "description": "The parameters of the function.",
      "aliases": [
        "parameter"
      ],
      "type": "array",
      "items": {
        "$ref": "Parameter.schema.json"
      }
    },
    "returns": {
      "@id": "stencila:returns",
      "description": "The return type of the function.",
      "$ref": "Validator.schema.json"
    }
  }
}