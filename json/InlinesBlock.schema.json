{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/InlinesBlock.schema.json",
  "@id": "stencila:InlinesBlock",
  "title": "InlinesBlock",
  "nick": "inb",
  "extends": [
    "Entity"
  ],
  "category": "prose",
  "description": "A block containing inlines with no other semantics.",
  "$comment": "This is a block type used in Stencila Schema for inline content without paragraph\nsemantics.\n\nIt exists to wrap inline content as a block when a container requires block\nstructure but a full paragraph would introduce misleading semantics or\nspacing. It is especially useful during coarse decoding and similar workflows\nthat need a minimal block wrapper around executable inline content.\n\nKey properties include the inline `content`.\n",
  "analogues": [
    {
      "id": "html:div",
      "notes": "Closest HTML container analogue for grouping inline content without paragraph semantics."
    },
    {
      "id": "pandoc:Plain",
      "notes": "Closest Pandoc block analogue for inline content that is block-positioned but not paragraph-like."
    },
    {
      "id": "mdast:Paragraph",
      "notes": "Only an approximate MDAST analogue because Markdown commonly uses paragraphs for inline block content and has no dedicated semantics-free inline block wrapper."
    }
  ],
  "required": [
    "type",
    "content"
  ],
  "core": [
    "id"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "content": {
      "@id": "stencila:content",
      "description": "The contents of the block.",
      "dom": {
        "elem": "div"
      },
      "type": "array",
      "items": {
        "$ref": "Inline.schema.json"
      }
    }
  }
}