{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/CitationGroup.schema.json",
  "@id": "stencila:CitationGroup",
  "title": "CitationGroup",
  "nick": "ctg",
  "extends": [
    "Entity"
  ],
  "category": "prose",
  "description": "A group of `Citation` nodes.",
  "$comment": "This is a type used in Stencila Schema for grouped citations.\n\nIt exists to represent multiple related [`Citation`](./citation.md) nodes as a\nsingle inline unit when the surrounding text cites several references at once.\nThis allows citation processors and renderers to generate a combined rendered\nform, including ranges and grouping punctuation, while preserving the\nindividual citation items structurally.\n\nKey properties include `items` and the generated `content` rendering.\n",
  "analogues": [
    {
      "id": "pandoc:Cite",
      "notes": "Close Pandoc analogue because grouped citations are represented as a single cite node containing multiple citation items."
    },
    {
      "id": "myst:role:cite",
      "notes": "Close MyST authoring analogue for grouped citations, though Stencila stores individual citation items explicitly."
    },
    {
      "name": "CSL citation cluster",
      "url": "https://docs.citationstyles.org/en/stable/specification.html",
      "notes": "Conceptually analogous to a rendered citation cluster in Citation Style Language processors."
    }
  ],
  "jats": {
    "special": true
  },
  "latex": {
    "derive": false
  },
  "markdown": {
    "derive": false
  },
  "required": [
    "type",
    "items"
  ],
  "core": [
    "id",
    "content"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "items": {
      "@id": "schema:itemListElement",
      "description": "One or more `Citation`s to be referenced in the same surrounding text.",
      "aliases": [
        "item"
      ],
      "patch": {
        "formats": [
          "all"
        ]
      },
      "walk": true,
      "type": "array",
      "items": {
        "$ref": "Citation.schema.json"
      }
    },
    "content": {
      "@id": "stencila:content",
      "description": "A rendering of the citation group using the citation style of the document.",
      "$comment": "Generated from the citation items during compilation and includes range characters\nand parentheses e.g. [1-4,7]\n",
      "walk": false,
      "dom": {
        "elem": "span"
      },
      "type": "array",
      "items": {
        "$ref": "Inline.schema.json"
      }
    }
  }
}