{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/Text.schema.json",
  "@id": "schema:Text",
  "title": "Text",
  "nick": "txt",
  "extends": [
    "Entity"
  ],
  "category": "prose",
  "description": "A text node.",
  "$comment": "This is an implementation of schema.org [`Text`](https://schema.org/Text),\nadapted in Stencila Schema as a node type rather than a primitive string.\n\nIt exists so textual content can participate in the document tree with its own\nidentity and metadata, which is useful for diffing, editing, and collaborative\nworkflows. This differs from the primitive [`String`](./string.md) type, which\nrepresents plain scalar string values outside the node model.\n\nThe main property is `value`.\n",
  "analogues": [
    {
      "id": "schema:Text",
      "notes": "Direct schema.org source type, adapted in Stencila from a scalar data type into a first-class node."
    },
    {
      "name": "HTML text node",
      "url": "https://dom.spec.whatwg.org/#text",
      "notes": "Closest DOM analogue for literal text content, though Stencila text nodes can carry identity and metadata through `Entity`."
    },
    {
      "id": "pandoc:Str",
      "notes": "Close Pandoc analogue for plain inline text, though Pandoc splits spaces and line breaks into separate inline constructors."
    },
    {
      "id": "mdast:Text",
      "notes": "Closest MDAST analogue for text leaf nodes."
    }
  ],
  "dom": {
    "derive": false
  },
  "html": {
    "elem": "span"
  },
  "jats": {
    "special": true
  },
  "latex": {
    "derive": false
  },
  "markdown": {
    "derive": false
  },
  "proptest": {},
  "required": [
    "type",
    "value"
  ],
  "core": [
    "id",
    "compilationMessages"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "value": {
      "@id": "schema:value",
      "description": "The value of the text content",
      "patch": {
        "formats": [
          "all"
        ]
      },
      "dom": {
        "elem": "none"
      },
      "html": {
        "content": true
      },
      "proptest": {
        "min": {
          "description": "Generate a fixed string of text.",
          "value": "Cord::from(\"text\")"
        },
        "low": {
          "description": "Generate a random string of up to 10 alphanumeric characters.",
          "strategy": "r\"[a-zA-Z0-9]{1,10}\".prop_map(Cord::from)"
        },
        "high": {
          "description": "Generate a random string of up to 100 alphanumeric characters, some special characters commonly used in prose, and whitespace.",
          "strategy": "r\"[a-zA-Z0-9 \\t\\-_.!?*+-/()'<>=]{1,100}\".prop_map(Cord::from)"
        },
        "max": {
          "description": "Generate an arbitrary string.",
          "strategy": "String::arbitrary().prop_map(Cord::from)"
        }
      },
      "$ref": "Cord.schema.json"
    },
    "compilationMessages": {
      "@id": "stencila:compilationMessages",
      "description": "Messages generated while compiling the text.",
      "aliases": [
        "compilation-messages",
        "compilation_messages",
        "compilationMessage",
        "compilation-message",
        "compilation_message"
      ],
      "strip": [
        "compilation"
      ],
      "type": "array",
      "items": {
        "$ref": "CompilationMessage.schema.json"
      }
    }
  }
}