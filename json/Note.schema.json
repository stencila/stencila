{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/Note.schema.json",
  "@id": "stencila:Note",
  "title": "Note",
  "extends": [
    "Entity"
  ],
  "category": "prose",
  "description": "A note associated with document content.",
  "$comment": "This is a type used in Stencila Schema for notes attached to document content.\n\nIt exists to represent note content structurally so it can be rendered as a\nfootnote, endnote, sidenote, or similar document note, depending on the target\nformat and note type. This preserves note semantics across transformations\nrather than reducing them to plain text markers.\n\nKey properties include `noteType` and `content`.\n",
  "analogues": [
    {
      "id": "html:aside",
      "notes": "Approximate HTML analogue for note content, though HTML has no dedicated footnote or endnote element and placement semantics are not built in."
    },
    {
      "id": "jats:fn",
      "notes": "Closest JATS analogue for document notes, particularly footnotes."
    },
    {
      "id": "pandoc:Note",
      "notes": "Closest Pandoc analogue for inline-referenced notes; Stencila additionally records note placement semantics explicitly via `noteType`."
    },
    {
      "id": "mdast:FootnoteDefinition",
      "notes": "Close MDAST analogue for defined footnote content, although MDAST models note references and definitions separately and does not cover sidenotes directly."
    }
  ],
  "jats": {
    "elem": "fn",
    "attrs": {
      "fn-type": "custom"
    }
  },
  "latex": {
    "derive": false
  },
  "markdown": {
    "derive": false
  },
  "proptest": {},
  "required": [
    "type",
    "noteType",
    "content"
  ],
  "core": [
    "id"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "noteType": {
      "@id": "stencila:noteType",
      "description": "Determines where the note content is displayed within the document.",
      "aliases": [
        "note-type",
        "note_type"
      ],
      "patch": {
        "formats": [
          "md",
          "smd",
          "myst",
          "ipynb",
          "qmd"
        ]
      },
      "jats": {
        "attr": "custom-type"
      },
      "proptest": {
        "min": {
          "description": "Fixed footnote type.",
          "value": "NoteType::Footnote"
        },
        "high": {
          "description": "Generate an arbitrary note type.",
          "strategy": "NoteType::arbitrary()"
        }
      },
      "$ref": "NoteType.schema.json"
    },
    "content": {
      "@id": "stencila:content",
      "description": "Content of the note, usually a paragraph.",
      "$comment": "Most notes will have a single paragraph but could have multiple\nparagraphs, tables and even figures.\n",
      "dom": {
        "elem": "aside"
      },
      "proptest": {
        "min": {
          "description": "Generate a single paragraph (with no `Note` to avoid recursion).",
          "value": "vec![p([t(\"Note paragraph\")])]"
        }
      },
      "type": "array",
      "items": {
        "$ref": "Block.schema.json"
      }
    }
  }
}