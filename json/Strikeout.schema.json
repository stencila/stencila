{
  "$schema": "https://stencila.org/meta.schema.json",
  "$id": "https://stencila.org/Strikeout.schema.json",
  "@id": "stencila:Strikeout",
  "title": "Strikeout",
  "nick": "stk",
  "extends": [
    "Mark"
  ],
  "category": "prose",
  "description": "Content that is marked as struck out.",
  "$comment": "This is a Stencila-native inline mark for struck-out text, with close\nanalogues in HTML\n[`<del>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/del), JATS\n[`<strike>`](https://jats.nlm.nih.gov/archiving/tag-library/1.2/element/strike.html),\nMDAST [`Delete`](https://github.com/syntax-tree/mdast#delete), and Pandoc\n[`Strikeout`](https://github.com/jgm/pandoc-types/blob/1.17.5.4/Text/Pandoc/Definition.hs#L258).\n\nIn Stencila Schema it is used as a presentational and editorial inline mark\nfor text that should render with a strike-through. It supersedes the older\n`Delete` inline type because `Strikeout` more clearly describes the mark itself\nrather than an editing action.\n\nKey semantics are inherited from [`Mark`](./mark.md), with this type chiefly\nsignaling strike-through rendering for its `content`.\n",
  "analogues": [
    {
      "id": "html:del",
      "notes": "Close HTML analogue for struck-through text, though Stencila `Strikeout` is used as a general strike-through mark rather than specifically representing deletions."
    },
    {
      "id": "jats:strike"
    },
    {
      "id": "pandoc:Strikeout"
    },
    {
      "id": "mdast:Delete",
      "notes": "Closest MDAST analogue, although the MDAST name emphasizes deletion semantics rather than the visual mark."
    }
  ],
  "dom": {
    "elem": "s"
  },
  "html": {
    "elem": "s"
  },
  "jats": {
    "elem": "strike"
  },
  "latex": {
    "command": "st"
  },
  "markdown": {
    "template": "~~{{content}}~~",
    "escape": "~"
  },
  "proptest": {},
  "required": [
    "type",
    "content"
  ],
  "core": [
    "id"
  ],
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The type of this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\n",
      "type": "string"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item.",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "strip": [
        "metadata"
      ],
      "html": {
        "attr": "id"
      },
      "jats": {
        "attr": "id"
      },
      "type": "string"
    },
    "content": {
      "@id": "stencila:content",
      "description": "The content that is marked.",
      "dom": {
        "elem": "none"
      },
      "proptest": {
        "min": {
          "description": "Generate a single fixed text value.",
          "value": "vec![t(\"text\")]"
        },
        "low": {
          "description": "Generate a single arbitrary, non-recursive, inline node",
          "strategy": "vec_inlines_non_recursive(1)"
        },
        "high": {
          "description": "Generate up to two arbitrary, non-recursive, inline nodes",
          "strategy": "vec_inlines_non_recursive(2)"
        },
        "max": {
          "description": "Generate up to four arbitrary, non-recursive, inline nodes",
          "strategy": "vec_inlines_non_recursive(4)"
        }
      },
      "type": "array",
      "items": {
        "$ref": "Inline.schema.json"
      }
    }
  }
}