{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/stencila/stencila/main/json-schema/DateTimeValidator.schema.json",
  "title": "DateTimeValidator",
  "@id": "stencila:DateTimeValidator",
  "extends": "Validator",
  "status": "stable",
  "description": "A validator specifying the constraints on a date-time.",
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The name of the type",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\nIt is extended with the titles of all descendant types during\nthe generation of schema.json files.\n",
      "type": "string",
      "enum": [
        "DateTimeValidator"
      ],
      "default": "DateTimeValidator",
      "from": "Entity"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "type": "string",
      "from": "Entity"
    },
    "minimum": {
      "@id": "stencila:minimum",
      "$ref": "DateTime.schema.json",
      "description": "The inclusive lower limit for a date-time.",
      "$comment": "A date-time is valid if it is greater than, or exactly equal to, `minimum`.\n",
      "from": "DateTimeValidator"
    },
    "maximum": {
      "@id": "stencila:maximum",
      "$ref": "DateTime.schema.json",
      "description": "The inclusive upper limit for a date-time.",
      "$comment": "A date-time is valid if it is less than, or exactly equal to, `maximum`.\n",
      "from": "DateTimeValidator"
    }
  },
  "source": "https://github.com/stencila/stencila/blob/main/schema/yaml/DateTimeValidator.yaml",
  "children": [],
  "descendants": [],
  "type": "object",
  "additionalProperties": false,
  "required": [
    "type"
  ],
  "propertyAliases": {}
}