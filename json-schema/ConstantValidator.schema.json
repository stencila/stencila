{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/stencila/stencila/main/json-schema/ConstantValidator.schema.json",
  "title": "ConstantValidator",
  "@id": "stencila:ConstantValidator",
  "extends": "Validator",
  "status": "unstable",
  "description": "A validator specifying a constant value that a node must have.",
  "$comment": "A node will be valid against this validator if it is equal to the\n`value` property. Analogous to the JSON Schema [`const` keyword](https://json-schema.org/draft/2019-09/json-schema-validation.html#rfc.section.6.1.3).\n",
  "properties": {
    "type": {
      "@id": "schema:type",
      "description": "The name of the type",
      "$comment": "This is a special property analogous to JSON-LD's `@type` keyword.\nIt is extended with the titles of all descendant types during\nthe generation of schema.json files.\n",
      "type": "string",
      "enum": [
        "ConstantValidator"
      ],
      "default": "ConstantValidator",
      "from": "Entity"
    },
    "id": {
      "@id": "schema:id",
      "description": "The identifier for this item",
      "$comment": "This is a special property analogous to JSON-LD's `@id` keyword.\n",
      "type": "string",
      "from": "Entity"
    },
    "value": {
      "@id": "schema:value",
      "description": "The value that the node must have.",
      "allOf": [
        {
          "$ref": "Node.schema.json"
        }
      ],
      "from": "ConstantValidator"
    }
  },
  "required": [
    "type",
    "value"
  ],
  "source": "https://github.com/stencila/stencila/blob/main/schema/yaml/ConstantValidator.yaml",
  "children": [],
  "descendants": [],
  "type": "object",
  "additionalProperties": false,
  "propertyAliases": {}
}