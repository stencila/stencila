import requests

r = requests.{{=data.method.verb}}('{{=data.url}}'{{?data.requiredParameters.length}},
  params = {
{{~data.requiredParameters :param:index}}    '{{=param.name}}': {{=param.exampleValues.json}}{{?index < data.requiredParameters.length-1}},{{?}}{{~}}
  }{{?}}{{?data.allHeaders.length}}},
  headers = {
{{~data.allHeaders :hdr:index}}    '{{=hdr.name}}': {{=hdr.exampleValues.json}}{{?index < data.allHeaders.length-1}},{{?}}{{~}}}
  }{{?}})
r.json()
