// A tsc config for builds to avoid compiling TypeScript test files to JavaScript
//
// Having `*.test.ts` under `src` and then excluding them from builds like this
// seems to the best approach because it means test files have the same
// TypeScript config as source.
{
  "extends": "./tsconfig.json",
  "include": ["./src/**/*"],
  "exclude": ["./src/**/*.test.ts"]
}
