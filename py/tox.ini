[tox]
envlist = py27

[testenv]
deps = pip>=8.1.2
       pytest
commands = py.test

[testenv:cover]
deps = pip>=8.1.2
       pytest
       coverage
       pytest-cov
commands = py.test --cov={envsitepackagesdir}/stencila --cov-report=xml
